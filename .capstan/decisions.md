---
capstan_type: decision-log
---

# Decisions

| # | Date | Decision | Status |
|---|------|----------|--------|
| 983 | 2026-09-24 | `agent-models` is for plugin installs only, as the board skill is (943). The override marker and the drift flag need the plugin version, which a manual install has no `plugin.json` to supply (review agent-models-1 ST5). Its Precondition says so, and `docs/manual-install.md` says so, which adds that file to the `agent-models` slice's Owns. No other slice owns it | accepted |
| 982 | 2026-09-24 | `agent-models` writes nothing before the operator's yes: no scratch folder and no `.gitignore` line (review agent-models-1 SP1). Scout returns stay in the run's context and are not filed, and each proposal row cites its source URL instead. This narrows spec requirement 4's "files their returns", because a filed return is a write the operator never approved and a no would leave it behind | accepted |
| 981 | 2026-09-24 | Supersedes 964 on shape only. `agent-models` is two runs and one gate, the shape `quick` has, and still holds no claim. The first run scouts and proposes, then ends at the gate. The operator's yes, naming the seats that get a Claude override, starts the second run, which writes. A fresh invocation re-scouts rather than resuming. The stop is a Gate in the glossary's sense, so calling the skill gate-free was wrong (review agent-models-1, vocabulary) | accepted |
| 980 | 2026-09-24 | `agent-models` round 1: every finding is actioned, as fix dispatch 1, except SP5. SP5 is accepted rather than cut: README's one-line pointer to `agent-models` is where a reader of costs looks. Two blocked. ST1: on yes the skill overwrote an unmarked project agent of a role's name without naming it. It now lists unmarked files beside the tables, and writes a seat only where the path is absent or already marked, unless the operator names that seat. SP1 is ruled at 982. The rest: rules are discovered, not pinned to today's lineup (ST2); no decision numbers in shipped skill text, per 94 (ST3); `decision-record` and `capstan-log next` before the row (ST4); plugin-only per 983 (ST5); the gate asks for the override seats (ST6); an override copies the whole file (ST7); point at `setup` for the document home and write a row only where `decisions.md` exists (ST8, SP2); a Done when and a close telling the operator to start a fresh session (ST9, N3); point at the crew section for spawns (ST10); literal TOML strings (N1); name the `permissionMode` difference (N2); an existing override's values are current (N4); Codex ids confirmed against OpenAI's pages (SP3); the top tier only on the Reviewer (SP4) | accepted |
| 979 | 2026-09-24 | Supersedes 976 for manual installs; 976 stands for plugin installs. The `override-spawn` Reviewer (SP1) found the collision 976 said to reopen on. Under a manual install the crew is plain `builder` in `~/.claude/agents/`, and Claude Code ranks a project `.claude/agents/builder.md` above it whatever its first line says (scout/plugin-agent-overrides.md), so no instruction can make the Architect ignore an unmarked one. The crew section says so, and the Architect reports an unmarked project file of a role's name before spawning under a manual install | accepted |
| 978 | 2026-09-24 | `override-spawn` round 1: every finding is actioned, none dismissed, as fix dispatch 1. ST1: the rule checks for a file under `<working copy>` but can only spawn agents the session has loaded, and a session rooted elsewhere, or started before `agent-models` wrote the file, has not loaded it. So an unscoped override is spawned only when the marked file exists and the name is among the session's listed agents; otherwise the run stops and says to restart the session in the working copy, never falling back silently, the pattern effort's `## Before you start` uses for a board skill. ST2: quick steps 6 and 7 dropped the manual-install form; they now point at the crew section for every form. ST3 and ST4: wording. The glossary's Agent override row now names spawning by the unscoped name, without the Namespace, settling the Reviewer's vocabulary note | accepted |
| 977 | 2026-09-24 | 974's spawn rule lives once, in the crew section of `skills/effort/SKILL.md`, which every run reads whole (545) and which covers every phase's spawns. `PHASE-3-BUILD.md` gets no second copy, narrowing spec requirement 3, because the same rule in two files drifts. `quick`, a separate front door, points at it in one sentence. The override marker is an HTML comment on the first body line, `<!-- capstan-override: capstan@<version> agents/<role>.md -->`, and a TOML comment on the first line of the Codex file | accepted |
| 976 | 2026-09-24 | The spawn rule in 974 checks the override header, not the file name, so an unrelated user agent called `builder`, `reviewer`, `scout` or `courier` without the marker is ignored and `capstan:<role>` is spawned as before. Defaulted. Reopen on the first report of a collision | superseded by 979 |
| 975 | 2026-09-24 | `agent-models` records what it changed as one row in the target project's decision log when that project has a Capstan document home, and writes no log otherwise, since it holds no claim and a project without Capstan artifacts has no log to write. Defaulted, not asked. Reopen if an operator wants tuning recorded somewhere else | assumed |
| 974 | 2026-09-24 | A Capstan agent override is a file in the working copy's `.claude/agents/` named for its role, whose header marks it as a Capstan override and names the plugin version its body was copied from. Where one exists, `effort`, `quick` and `PHASE-3-BUILD.md` spawn its unscoped name rather than `capstan:<role>`. That is one sentence in each of the three, extending the existing "spawn whichever form your install produced" rule | accepted |
| 973 | 2026-09-24 | Supersedes 963. Plugin agents register namespaced, so a project `.claude/agents/builder.md` is a separate agent `builder` beside `capstan:builder`, not an override of it. No settings key changes a plugin agent's model or effort (anthropics/claude-code#79866, open), and the per-invocation `model` parameter has no `effort` counterpart (scout/plugin-agent-overrides.md). `agent-models` still writes overrides into a user project's `.claude/agents/`, only for seats the operator picks, with the version header and drift flag 963 described; 974 makes the crew spawn them. In Capstan's own repository it edits `agents/*.md` directly | accepted |
| 972 | 2026-09-24 | Running Capstan's `effort` inside Codex is out of scope: its skills spawn `capstan:*` Claude agents through Claude Code plugin paths, and making that work is a separate port. `agent-models` runs in Claude Code and writes the files for both platforms | accepted |
| 971 | 2026-09-24 | Codex seats: Reviewer `gpt-6-sol`/`xhigh` read-only, Builder `gpt-6-sol`/`high`, Scout `gpt-6-luna`/`medium` read-only, Courier `gpt-6-luna`/`medium`. Astra stays out, as Fable does under 961: OpenAI puts Sol at roughly 90–95% of Astra's capability for about 20% of the cost per task (scout/codex-models.md). The exact id strings are medium confidence in that return, so the Builder confirms them against platform.openai.com before writing | assumed |
| 970 | 2026-09-24 | Capstan commits no Codex TOML. Codex requires a custom agent's `developer_instructions` inline (checked against learn.chatgpt.com/docs/agent-configuration/subagents.md on 2026-09-24), so committed files would be a second copy of every agent body. `agent-models` generates `.codex/agents/capstan-<role>.toml` in the target project from the current `agents/*.md` bodies whenever it runs, each file carrying the plugin version it was copied from, as 963 does for Claude overrides | accepted |
| 969 | 2026-09-24 | Scout and Courier stay `sonnet`/`medium`. Haiku is refused for any seat: it takes no `effort`, its retirement floor is 2026-10-15, and Scout citations need accuracy (platform.claude.com/docs/en/models/overview, checked 2026-09-24) | accepted |
| 968 | 2026-09-24 | Builder moves from `sonnet`/`high` to `opus`/`high`. Every fix dispatch costs a Builder pass and an `xhigh` Reviewer pass, and Opus 5.5 is positioned for long-running agentic coding at twice Sonnet 5's price, so one saved fix round pays for it. Unmeasured on Capstan: reopen if a `bench` run shows fix dispatches per slice do not fall | assumed |
| 967 | 2026-09-24 | Reviewer stays `opus`/`xhigh`. Opus 5.5 beats Fable 5.1 on all seven benchmarks Anthropic's Opus 5.5 launch page lists, at 40% of the price, so 961's condition for Fable is not met. `max` is refused on Anthropic's own diminishing-returns warning (scout/claude-models.md) | accepted |
| 966 | 2026-09-24 | The Architect's own model stays out of the files, because the Architect is the operator's session and no frontmatter governs it. The `agent-models` report carries one line recommending a session model and effort | accepted |
| 965 | 2026-09-24 | Capstan ships Codex agent definitions if Codex supports named agents with a per-agent model and reasoning effort, so the skill keeps both platforms in step. If it does not, the mapping is documented and nothing is shipped. Which branch applies waits on the Codex Scout | accepted |
| 964 | 2026-09-24 | The skill is `/capstan:agent-models`, operator-invoked only (`disable-model-invocation: true`, like `setup`), a fourth front door. One run with no gates and no claim: it scouts the current lineups, proposes a seat, model and effort table with reasons, waits for the operator's yes, then writes | superseded by 981 |
| 963 | 2026-09-24 | In a project other than Capstan's own repository, `agent-models` writes override agents into that project's `.claude/agents/`, only for seats the operator picks. Each override carries a header naming the plugin version it was copied from, and the skill flags drift when that version moves. The plugin's installed files are never edited, because an update overwrites them. In Capstan's own repository it edits `agents/*.md` directly | superseded by 973 |
| 962 | 2026-09-24 | Agent frontmatter names models by alias (`opus`, `sonnet`, `haiku`), not pinned ids. An alias follows a new release on its own; the skill exists for deliberate tier and effort changes, not for chasing ids | accepted |
| 961 | 2026-09-24 | Model choice is quality-first on the Reviewer only, the seat that catches mistakes. Scout and Courier go as cheap as their output allows. Fable 5.1 is considered only for the Reviewer and only on a real capability gap the Scout can cite | accepted |
| 960 | 2026-09-24 | Effort `agent-model-tuning` opens: tune the four agents' model and effort for Claude as of 2026-09-24, add the Codex equivalent, and add an operator-invoked skill that repeats the tuning on demand. The operator named it `agent-model-optimiztion` "or something of the sort"; 964 settles the name | accepted |
| 959 | 2026-09-23 | This repository's decision log is rotated for the first time, as R1.9 and 901 asked, by `PHASE-4-DELIVER.md` step 5 run from the working copy's merged helper. The installed plugin is still 2.25.1 and has no such step. Before rotation, `decisions.md` was 453,543 bytes and 958 rows. The rotation wrote `decisions/archive/decisions-0001-0908.md`: 857 rows, 382,898 bytes. It kept 101 rows in `decisions.md`: every open and assumed row, plus 909 to 958. The active file is now 70,772 bytes. That is larger than the roughly 57 KB 889 estimated, because this effort's own rows run long. `capstan-log check` passes on 958 rows. A sorted-multiset `cmp` of every row line before and after, taken outside the helper, is identical. `find 15` resolves row 15 in the archive, with 569 superseding it. The link check stays green. The untracked `decisions 2.md` (955) is untouched, and the rotation neither read nor moved it | accepted |
| 958 | 2026-09-23 | The knowledge-base note is accepted after note fix dispatch 2. The Architect checked the return against round 2's findings and the source rows, and did not send a third review. SP1, the one blocking finding, is closed: the six items now map one to one onto the spec. The standing rule states both directions, each open row carries its own reason, and the rotation prediction states its premise. Two small residuals the Architect saw are left for the operator, since they change no fact. The opening paragraph repeats the README cut in a second sentence, and the 945 paragraph keeps a garden-path clause, "the stop `effort` itself now has". The note is in the vault, uncommitted, per the no-git rule | accepted |
| 957 | 2026-09-23 | Note round 2: SP1 still blocks, since the note regrouped the six items again. All round-2 findings are actioned as note fix dispatch 2, none dismissed. The list after "acted on all six" maps one to one onto the spec's numbering. The standing rule states both directions, with 0006's reasons. Each open row carries the reason its own row gives. The rotation prediction states its premise, "as the log stood at row 956", because more rows land before step 5 and each pushes one more out. The closing range reads "rows 883 onward", since this effort keeps writing rows through its close-out. A paragraph records what the Courier checked itself, as the folder's neighbours do | accepted |
| 956 | 2026-09-23 | The note review found two blocking Spec defects. The note listed rejected alternatives no source records, in place of 0006's four. It also regrouped the review's six items, dropping branding and the README cut. All findings are actioned as note fix dispatch 1, none dismissed. The rotation paragraph must say this log's rotation runs after the note, and which rows it will move. Every open and assumed item cites its row, with its own reason. Separately, `CONTEXT.md`'s `Effort` row is amended in place per 705: it called an effort "one run", which the `Run` row forbids. The note Reviewer found the contradiction | accepted |
| 955 | 2026-09-23 | A sync copy, `.capstan/decisions 2.md`, appeared beside the log during the wait at gate 3. It is untracked and 449,966 bytes. It matches the committed `decisions.md` except for lacking row 954, so it is the file as it stood before that row was written, copied by iCloud; the working copy lives under `~/Documents`. The committed file is the one git tracks, and phase 4 builds on it. Deleting the copy is outside the gitignored scratch, so it is the operator's call under the Authority table: it is named in the close-out and left in place. Every commit in this phase names its paths explicitly, so the copy is never staged. `capstan-log` reads only `decisions.md` and `decisions/archive/`, so rotation does not see it | accepted |
| 954 | 2026-09-23 | The integration was verified at `d854341`, every slice merged plus the 3.0.0 bump, against all three checks `.github/workflows/checks.yml` declares. `tests/run.sh` passed 158 tests across 8 files under bash 5.3 and again under `/bin/bash` 3.2. The id-shim step passed under both shells with exactly one `skipped (root)`. The root job, replayed as root in `ubuntu:24.04`, passed the same 158, with its two root skips by design. `actionlint` and `claude plugin validate` were clean on both plugins and the marketplace. `d854341` is recorded as the verified commit. Not observed: a live Actions run, since nothing is pushed, and the root job's `safe.directory` step, since the replay was not a git checkout | accepted |
| 953 | 2026-09-23 | Version 3.0.0 for core, confirming 899 at phase 3 step 8, before verification, per 870. `capstan-board` ships at 1.0.0. It is a major because a project whose `capstan-tracker` names GitHub now stops, with "surface not installed", until the second plugin is installed. That is a break an existing operator sees, not new behaviour they opt into. `/capstan:quick`, `capstan-log` and rotation, the deny example and `bench/` all ride the same number. Committed on `main` and not pushed: pushing publishes to every install through the marketplace, and that is the operator's | accepted |
| 952 | 2026-09-23 | `readme` fix dispatch 3 is verified by the Architect under the Reviewer-prescribed case. The README, DESIGN.md and docs changes are the round-3 moves in the Reviewer's own words, checked line by line. The `test_links.sh` refactor is the prescribed `broken_links` extraction and prune loop. The suite is green under both shells. `readme` merged at `4a3e8c5`, and every slice in `plan.md` now reads `merged` | accepted |
| 951 | 2026-09-23 | The design question fired on `readme`'s third fix dispatch, and the answer is to keep patching. Round 3 gave a "merge as is" verdict, every round-2 finding closed, and all six new findings are small. Four trace to the Builder's own word-budget trims and two to round 2's prescriptions, not to the shape of the slice. Every move is prescribed with wording: the scope instruction covers each install command, the quick's stop keeps its timing and drops its mechanism, three trimmed sentences go back, the rotation test calls one `broken_links` function instead of copying the scan, `PRUNED_PATHS` is built in a loop, the header and comment are corrected, and the merge-conflict line says neither Builder can be asked. DESIGN.md's `[896]` link becomes a link to record 0007, since phase 4's rotation moves row 896 out of `decisions.md`. The Architect verifies the return itself | accepted |
| 950 | 2026-09-23 | Record `0007-move-the-board-surface-to-a-second-plugin.md` is written by the Architect. Spec R5.6 asked for it, no slice in `plan.md` owned it, and nothing caught the gap until the `readme` round-3 Reviewer found DESIGN.md linking a row where a record belonged. The decision passes all three gates. It is hard to reverse, since existing installs break on upgrade. It is surprising without context, since a plugin gets split. And it is a real trade-off, since the board skills carry a copy of the document-home rules they must keep in step with core. The miss is a planning defect: a required artifact that no slice owns is a requirement nothing builds | accepted |
| 949 | 2026-09-23 | `tests/test_links.sh` resolves anchors with an ASCII-only slug rule, not GitHub's fuller one, and says so in its header. It fails loudly, never silently, on a heading the narrower rule mis-slugs (non-ASCII, trailing hashes, links inside headings). No heading in this repository needs more today. Recorded because the plan asked for GitHub's rule and the narrowing was never decided anywhere. Reopen when a real heading trips it | assumed |
| 948 | 2026-09-23 | All `readme` round-2 findings are actioned, none dismissed, as fix dispatch 2, each with the Reviewer's prescribed wording. Five blocked. SP1: adding `.capstan` to the link scan would turn the suite red the moment phase 4 rotates this log. Rows 14, 85 and 151 link to `decisions/000N` records by a path that does not resolve from the archive folder, and R1.3 forbids editing them. So the scan prunes `.capstan/decisions/archive`, `.capstan/effort` and `.capstan/quick`, and a test keeps the suite green after a rotation. SP2 to SP5 are false statements in the public README: the board limit, a Haiku claim moved under the wrong heading, the three-effort ceiling credited to a helper that counts nothing, and a quick said to stop before its interview when 945 says after. SP6 restores the full branding line. The 1,400-word ceiling counts prose, not the Ko-fi button's HTML. SP7: the marketplace-move steps reinstall `capstan-board` too. The Architect verifies the return against the prescriptions and the tree | accepted |
| 947 | 2026-09-23 | `readme` round 1 gave a "merge as is" verdict, but the README is the most public text this project ships and carried factual errors. It said any destructive action stops the run, dropping the scratch carve-out 637 restored. It called the board read untested, where 879 ran the live trial. It said five roles and named four. It said CI runs bash 3.2. So all round-1 findings are actioned as fix dispatch 1, none dismissed. S9 is settled by cutting rather than by asking the operator: the path from the top of the page to the gate table targets under 400 words and the whole page under 1,400. The credits become one line each, the repeated triad goes, and "Your first run" is trimmed; nothing the spec requires is removed. `test_links.sh` gains reference-style links, `.capstan/` and `CLAUDE.md` as scan roots, and a failure on a missing root. The upgrade note covers a quick too, per 945 | accepted |
| 946 | 2026-09-23 | `board` fix dispatch 2 is verified by the Architect under the Reviewer-prescribed case. The diff is 944's list across four files and nothing else. The quoted stop message is back at `effort/SKILL.md:105`. The phase-1 check runs before `acquire` and invokes nothing. Board setup no longer points at a table it cannot load. Tests are green under both shells, and `claude plugin validate` passes. `board` merged at `a88173c`. `CONTEXT.md` is amended in place per 705: `Helper` names three helpers in core and `capstan-tracker` in the board plugin. `Teardown` says that removing an item is a delete, as 742 settled. `Conforming comment` points at the board's `tracker` skill. `Namespace`, `Setup` and `Front door` cover the board plugin | accepted |
| 945 | 2026-09-23 | `/capstan:quick` has no early check for a `github:` tracker whose board skill cannot load. Its first tracker write comes at step 4, after up to two interview rounds, so an operator upgrading core without `capstan-board` loses an interview before the stop. Row 942 names `effort` only. Left open with 929's quick findings, as the first thing to add when `skills/quick/SKILL.md` is next touched. Found by the `board` round-2 Reviewer | open |
| 944 | 2026-09-23 | `board` round 2 gave a "merge as is" verdict. Every round-1 finding is closed except ST1's second site. All round-2 findings are actioned as fix dispatch 2, except SP5, which goes to 945. The Architect verifies the return itself. SP1: the stop keeps its diagnosis, "surface not installed: install capstan-board@bytesnation", then says to reload plugins and re-run the phase. Row 942 replaced the next step and never meant to drop the message the spec quotes, which verify greps for. ST1: board setup drops its pointer to a table it cannot load. ST2: the phase-1 check tests whether `capstan-board:tracker` is among the skills the session lists, rather than invoking it, and points at `## Tracker` rather than restating it. SP4: that check runs before the claim is taken, so a stop leaves no lock. SP3: core setup's unfinished-migration stop moves after its same-home exit. SP2: an intro-only `AGENTS.md` is cleaned up as core does. ST3 to ST8: the ordering, the hedge, the inline wording, "otherwise", the effort-or-quick condition, the lowercase sentence and the unreachable manual-install branch | accepted |
| 943 | 2026-09-23 | `capstan-board` is plugin-install only. Under a manual install, its `setup` skill collides with core's in `~/.claude/skills/setup/`, and neither `capstan-board@bytesnation` nor `/capstan-board:setup` exists there. Renaming the skill to dodge the collision would buy a supported path for a manual install nobody has asked for. So core's stop drops the "`tracker` under a manual one" form, and the readme slice's manual-install page says the board needs the plugin install. Found by the `board` Reviewer. Reopen if someone asks for the board on a manual install | accepted |
| 942 | 2026-09-23 | The not-installed stop names the real next step. Every project that can reach it already has a configured board, so the step is: install `capstan-board@bytesnation`, reload plugins, and re-run the phase that stopped. It is not `/capstan-board:setup`: re-asking the surface question there, and answering the default, would start a reverse migration. Setup is named only for changing surface. The same check moves into `effort/SKILL.md`'s "Before you start". A `github:` tracker with the board skill unloadable then stops before the interview, not at phase 2 after a wasted gate 1. Found by the `board` Reviewer walking an upgrade without the second plugin | accepted |
| 941 | 2026-09-23 | All `board` round-1 findings are actioned, none dismissed, as fix dispatch 1. SP8 is settled at 943 and SP7 at 942. SP1 blocked: board setup read `capstan-document-home` with fewer rules than core. A relative path was accepted and an unmounted vault never stopped, so a forward migration would strand `tracker.md` in the vault while the board read empty. It now carries core's four rules and cites `effort` for the disagreement stop. SP2: `skills/quick/SKILL.md:18` still points at `TRACKER-GITHUB.md`, which this slice removed from core. The plan's Owns and Must do contradicted each other there, so the slice's Owns widens by that one line, in a file whose own slice is merged and so collides with nothing. SP3: core setup stops on an unfinished board migration before moving the home. SP4: core setup restores the empty-file delete. SP5: board setup restores replace-in-place and the header for a created file. ST1: the reverse leg states its approvals inline, because the Authority table sits in a skill board setup cannot load. ST2: Done when is restored. ST3: the moved tracker skill drops its decision numbers per row 94. ST4: the stale step number. ST5 to ST7: the carried-over lines, stale comments and the doubled "a fourth" | accepted |
| 940 | 2026-09-23 | `CONTEXT.md` is amended in place per 705 for the three merged slices that made terms true. `Front door` counts three, adding `quick`. `Helper` counts four, adding `capstan-log`; `board` will change it again when `capstan-tracker` leaves core. `capstan_type` gains `decision-archive`. `.capstan/` names a Quick's scratch. `Run` covers a Quick's two invocations. New rows: `Active log`, `Archive`, `Rotate` and `Quick`, the gaps the `log-helper`, `log-readers` and `quick` Reviewers named. The `Quick` row settles 906's open wording: a Quick is not an Effort, is not counted against the ceiling, and its `merged` row is final | accepted |
| 939 | 2026-09-23 | `log-readers` fix dispatch 2 is verified by the Architect under the Reviewer-prescribed case. The diff is the five moves 938 lists and nothing else. It adds no scope. It is checkable against the cited `--help` usage and writing-for-agents' pointer rule. `log-readers` merged at `92c09e9`, and `tests/run.sh` is green on `main` | accepted |
| 938 | 2026-09-23 | `log-readers` round 2 gave a "merge as is" verdict, re-testing the standing rule on real archived rows. It is closed for 102, 222, 880 and 36, 43 and 57, and for a record-number false positive the prose already covers. All round-2 findings are actioned as fix dispatch 2 with the Reviewer's prescribed moves, and the Architect verifies the return itself. N1 is worth doing: `two-axis-review` told the Reviewer to "run `find`" with no path and no arguments, and the Reviewer preloads neither `decision-record` nor a `<bin>`, so it spells out `capstan-log find <document home> <NNN>` at `../effort/bin/`. N2: the reason for rotating after the note becomes the true one. The note's Reviewer is handed `decisions.md` to grade the Spec axis against, and this effort alone wrote 55 rows. N3: the commit is conditional on an archive having been written. N4: "a log row", not "an old file". N5: two unslop fixes in 0006 | accepted |
| 937 | 2026-09-23 | `bench` fix dispatch 5 is verified by the Architect under the Reviewer-prescribed case, per 933, and all three conditions hold. The diff applies round 5's moves and 933's `check:` to `doubt:` rename to three files, and nothing else. It adds no scope. It is checkable: the suite is green under `/bin/bash` 3.2 and bash 5, no `check:` cost label survives, and the Builder's run on a real fallback session shows the doubt kept on a fully unpriced row, naming both models. `bench` merged at `4725c2a` after five fix dispatches, the limit. The round-5 verdict was "merge as is" | accepted |
| 936 | 2026-09-23 | `skills/diagnosing-bugs/SKILL.md` checks `decisions.md` for the area being touched, so after rotation it misses archived decisions. Found by the `log-readers` Reviewer, and outside that slice's Owns. The file is vendored with three lines repointed, so fixing it means a fourth repointed line and a `CREDIT.md` entry. That is a widening of the slice this run does not take unattended. Left open for a later quick | open |
| 935 | 2026-09-23 | All `log-readers` round-1 findings are actioned except S5, which goes to 936, as fix dispatch 1. Two blocked. S1: the prose said to decide an archived row's standing by `capstan-log find`, never by its own status cell. But 41 of the 84 rows a rotation archives with `superseded by NNN` carry no `Supersedes` phrase, so `find` reports them as standing, and `find` also lists rows that merely quote the phrase. The rule is now two-directional. An archived status reading `superseded by NNN` is authoritative. Any other archived status may be stale, so `find` runs and each listed row is read to confirm it really supersedes. `two-axis-review` checks a found decline's standing the same way. T1: the exit-7 row told the crew to delete a document-home file, which the Authority table reserves to the operator, and two of the three exit-7 paths land no archive at all. Exit 7 is now handled like exits 2 to 5: stop, quote the message, and name removal as the operator's call. The rest: at the default home the rotation gets its own commit and step 7 names it. 0006's Decision gains the trigger and the helper choice, and describes 879 as an agent's ad hoc script. `next` gets a true reason. `<bin>` is defined correctly and handed to the Courier. PHASE-4's pointer and its completion criteria cover the rotation outcome. The update-both rule points at its archive exception. `grep -ri` replaces `grep -i` on the archive directory. 0006 names `external-review` | accepted |
| 934 | 2026-09-23 | Row 933 was written across several lines, and the `log-readers` Reviewer found that `capstan-log check`, `next` and `rotate` all refuse the log with exit 4 while it is. It is repaired in place to one line: a factual repair, not a decision change, so the number and status stand. The Architect wrote a bullet list into a table cell. The helper refusing the log is the check doing its job on the first real malformation it met | accepted |
| 933 | 2026-09-23 | `bench` round 5 gave a "merge as is" verdict, having checked every field the script reads against 129,525 real assistant lines. The design question fired on the fifth and last fix dispatch. The answer is to spend it on the Reviewer's prescribed moves and verify the return in place of a review. If the return breaks anything, `bench` merges at `469e727`, the tip this verdict covers, and the round-5 findings stay open. The moves: A fully unpriced row keeps its doubt: the real fallback turn lost `iterations disagree` because its own model was unpriced. The reason names the fallback element's `model`, and a fixture is built on the real two-element fallback shape. `PROTOCOL.md:56` gets back the object of its instruction. The window listing becomes `ls -trU "<dir>"/*.jsonl`. The cost cell takes everything before the doubt, and Notes takes the rest. A fast-mode doubt says whether that arm stands. Fixtures carry `cache_creation_input_tokens`. The comments and unslop leftovers are fixed. The `check:` label is renamed `doubt:`. "Check" already meant a declared check, a pre-flight check and the `/plugin` check in one file, and 930's word choice was the Architect's | accepted |
| 932 | 2026-09-23 | `log-helper` fix dispatch 3 is verified by the Architect under the Reviewer-prescribed case, and all three conditions hold. The diff applies the round-3 moves as the Reviewer wrote them. It adds no scope beyond two test renames that describe the new fixtures. It is checkable against the cited guarantees, and the Architect checked the one 931 turned on by running it: the new 350-row `find` test, with round 2's helper under `/bin/bash`, fails with "find should not abort on descriptors" and row 188 missing, and the tip passes. The suite is green in the worktree under both shells. `log-helper` merged at `3e25155`, and `tests/run.sh` is green on `main` under `/bin/bash` 3.2 and bash 5 | accepted |
| 931 | 2026-09-23 | `log-helper` round 3 gave a "merge as is" verdict, and the design question fired on its third fix dispatch. The answer is to keep patching once before merging. The code is right under both shells and as root, but two tests fail to hold what 925 recorded them as holding. The 350-row `find` test passes against round 2's crashing helper, because the superseding row comes first and the runner's descriptor limit is high. The SIGTERM test moves no rows, so dropping the archive temp from the trap passes the whole suite. A record saying `/bin/bash tests/run.sh` guards a fix it does not guard is the false claim this project keeps paying for, so it is fixed now, not carried. Every move is prescribed exactly by the Reviewer. Row 188 is written last, the test runs under `ulimit -n 256`, and the exit is asserted. The interrupt lands on the archive rename of a rotate that moves rows. Exit 7 is relabelled and names the archive to remove. The tab fixture gets a leading tab. `trim` sets a global instead of forking per row. The trap comment says what the code relies on. The Architect verifies the return itself under the Reviewer-prescribed case, then merges. One note goes to `log-readers`: `find` lists rows by text match, so a quoted example counts, and a listed row is a match to confirm, not a verdict | accepted |
| 930 | 2026-09-23 | The design question fired on `bench`'s fourth fix dispatch, and the answer is to keep patching. The blocking defect is a guessed schema, not the design. The round-3 Builder assumed each `usage.iterations` element nests a `usage` object, and said so without checking. The Architect checked one real file before dispatching the review, and the Reviewer confirmed it across 59,919 elements: the fields sit directly on each element. The check therefore flagged all 43,241 real turns and missed the five it was built for, printing a false $0 on one. Fixes: one jq `def` for the five-field tuple, used on the top level and on each element; no `// 0` inside the sums, so a missing field refuses; a fixture on the real shape; and a test that agreeing iterations stay unflagged. The Reviewer's other findings are actioned, none dismissed. The operator passes files oldest-created first, and the header says ownership follows argument order. For Spec 3, option (a): the wall-clock figure includes time inside a file, gate waits included, and the protocol says so and drops "never appears". A plain resume writes into the same file, and only a fork makes a new one. The `/plugin` check passes a plugin that is not installed. "Partial" keeps its one meaning, a component left unpriced. A fast-mode, US-only or iterations doubt prints as `check:` with its reason, and the protocol says what to record for each. The rest: every argument prints its span, the stragglers and the unslop items, dead code removed, and a pricing-file test helper | accepted |
| 929 | 2026-09-23 | Three worth-doing findings from `quick`'s round 6 stay open, because the slice spent all five fix dispatches and none of them blocks. S1: the merge check compares `rev-parse` output with `concept.md`'s third line as text, and nothing says that line must hold the full commit id. With an abbreviated id, a quick nobody built passes as merged: `verify` goes green on nothing and the approved concept is deleted. This needs an abbreviated line three, a stop in a window of minutes, and a next invocation, all three. The fix is to name the full id at the two writes, or to resolve both sides with `rev-parse --verify`, as `capstan-claim` does. S2: three sentences disagree with the table: the `discard` stop, and two `Done when` bullets. The table is more specific, and each disagreement ends in a stop. S3: small wording items, and a report that should name an unfinished merge. These are the first fixes whenever `skills/quick/SKILL.md` is next touched, and they go in the gate-3 brief | open |
| 928 | 2026-09-23 | `quick` merged at `960e9a2` on the round-6 Reviewer's verdict, "merge as is". That round reproduced every git state the lifecycle table meets. It confirmed that nothing in the file leads to a claim command, and that the crew deletes only the quick's own gitignored folder and its own worktree, never a branch. The third generic-agent read-back, at `review/quick-readback-3.md`, found the flow correct end to end. The edge ambiguities it found overlap round 6's S2 and S3. Five fix dispatches were spent, the limit. 919's simplification turned the growth: 956, 1,557, 1,786, 1,337, 1,479, 1,542 words, and no new lifecycle case after round 4 | accepted |
| 927 | 2026-09-23 | The design question fired on `bench`'s third fix dispatch, and the answer is to keep patching. The design is sound: a protocol, one script, a pricing table and a template. Round 2's blocking finding is closed, and round 3 found nothing blocking. Every finding is actioned, none dismissed. Wall-clock time gets one definition. `session-usage` prints each argument's first and last timestamp over the turns it kept from that argument, so a forked file's copied turns do not count its original's time twice; that widens its output, and the Architect accepts it. Steps 3 and 4 record a window, not a time. bat's citation loses the submodule step its build job never runs. The second-failure rule becomes "any second repository". A path that yields no transcript fails loudly. The baseline's plugin-disable line states the general fallback rule and gains a `/plugin` check before the task is given. The same step disables `capstan-board@bytesnation`, which this effort ships: the bench slice owns the protocol, and `board` does not touch it. A turn with fast mode, US-only inference or `iterations` that disagree with its top-level usage marks the total partial, and "fast mode off" joins what is held identical. The rest: GOBIN, click's confidence, the root skip's string compare, and the listed unslop and glossary stragglers | accepted |
| 926 | 2026-09-23 | The design question fired on `quick`'s fifth and last fix dispatch, and the answer is to keep patching, once, with a stated exit. Round 5 found one blocking defect, S1: the crash check `merge-base --is-ancestor` gives the wrong answer in two windows, and each time takes a destructive action, either finishing a quick nobody built or offering to drop shipped work. The Reviewer judged the rest cheap: about 75 to 90 words, mostly moved, and no new case. P1 goes option (a): the merged `quick/<slug>` branch is not deleted by the crew, and the closing brief names it for the operator. The Authority table reserves deletes other than gitignored scratch, and 924's split between merged and unmerged branches was the Architect's misreading of that table. Dropping the delete also closes S1's first window. The row moves to `merged` straight after the merge commit. The merge check requires the branch to have moved past step 5's commit. A folder whose row is not `planned` is settled by its table row before the argument is read, and `discard` never applies to a `merged` row. `concept.md` reserves line three, and the contradicting clauses go. If round 6 finds anything blocking, the limit stops the run and the slice goes to the operator with its findings. The run does not recut it unattended | accepted |
| 925 | 2026-09-23 | `log-helper` round 2: every round-1 finding is closed under bash 5, and all round-2 findings are actioned, none dismissed, as fix dispatch 2. The blocking finding is a regression the fix introduced. `find` opened one process substitution per row, and bash 3.2 never closes them, so it aborted with SIGABRT at about 256 descriptors. Every `find` on this repository's log crashed under `/bin/bash`, the shell 0004 targets, and the list-form case 912 asked for never ran there. The fix is a here-string or an array, plus a `find` fixture over 300 rows so `/bin/bash tests/run.sh` holds it. The trap caught INT and TERM and let the rotate carry on without its temp files. The Reviewer produced a rotate that exited 0 having dropped the frontmatter. So the trap is on EXIT alone, the idiom `template.sh` already uses. Status matching compares the whole status cell, not its first word. The immutable-flag test becomes a PATH shim for `mv`: it ran only on macOS, never in either Linux job, and an interrupt left a file the runner could not delete. The SIGTERM test becomes deterministic through the same shim, asserting a byte-identical log and a full `check` count afterwards. The multiset verify is then deliberately untested beyond the backslash test. Once a signal ends the run before either rename, the verify guards only against a future byte-level bug in the move loop, and no black-box test can reach that without one. The rename-failure exit stops colliding with "not found" | accepted |
| 924 | 2026-09-23 | The design question fired on `quick`'s fourth fix dispatch, and the answer is to keep patching. 919's simplification worked. Round 4 is the first round the file shrank, from 1,786 words to 1,337. The Reviewer judged that the state space is now closed: one folder, five statuses, a table row each. It found nothing blocking. What remains are crash windows inside one run, which are closed by reordering writes the flow already makes, not by adding cases. All round-4 findings are actioned, together with the second read-back's. The folder is deleted last on every exit. Step 5 records its branch commit as `concept.md`'s third line, and the row moves to `building` before `worktree add`. A request stops while any folder exists. The `none`, `building` and `merged` table rows handle the crash states, `building` checking `merge-base --is-ancestor` first. The clause keeping `verify` off the claim is restored: this round's rewrite cut it, reopening 913's lesson through the same pointer. The other fixes: stops for `resume` or `discard` with nothing parked and for a `resume` whose answer changes the build; "nothing running" dropped, since a quick cannot know it; one hand-off sentence covering every dispatch in the run. From the read-back: `discard` must name the slug, because the leading-word grammar would otherwise read "discard the stale cache entries" as deleting the only copy of an approved concept. The merge uses `--no-ff`, so a merge commit always exists. The merged `quick/<slug>` branch is deleted at close with `git branch -d`. An unmerged branch left by a stop is kept and named, since deleting it is the operator's. If this round comes back as new paragraphs rather than reordered sentences, the growth has restarted and the slice goes to the operator | accepted |
| 923 | 2026-09-23 | `bench` round 2: S1 is closed and independently confirmed. The Reviewer checked 8,611 ids carrying several `stop_reason` lines and found each is one turn, which settles the Builder's 112. All round-2 findings are actioned, none dismissed, as fix dispatch 2. The blocking finding is that pre-flight could not yield three repositories. So click's command sets `TOX_ENV` to one environment as its CI does, and cobra's install step adds `richgo`. The currency bar returns to the Scout's three months, which row 900 chose cobra against. The operator warms caches with one networked test run before the timed offline one, bat uses its build job's command, and a second failed repository means stop and reopen 900. `pricing.tsv` cites the platform pricing page, which does give 1-hour write rates and an Opus 5 row, so both are filled from it. `session-usage` takes several session paths and dedupes by `message.id` across them, because `/branch` and `--fork-session` copy turns into a new file and 573 ids on this machine appear in two. An unreadable file fails loudly again. The arm order stays baseline first per 911, and the sentence justifying it is corrected: the operator has seen the baseline before the Capstan interview, which favours the Capstan arm, and Notes records that. The rest: copy `review/` inside step 4 at gate 3, `--model` with a full id plus `claude --version` per arm, a stated wall-clock rule for closed sessions, and the Scout's confidence on every line count. `PROTOCOL.md` calls the commands declared checks, and says "arm" throughout | accepted |
| 922 | 2026-09-23 | `deny` fix dispatch 3 is verified by the Architect under the Reviewer-prescribed case, and all three conditions hold. The diff applies the round-3 Fix lines in the Reviewer's own wording, including the rename to `malformed_entries`, the fixtures built from a known-valid file, and the four rewritten sentences. It adds no scope, and `settings.deny.json` is untouched. It is checkable against the cited standards: the shape check's interface and the unslop patterns. The suite passed in the worktree under `/bin/bash` 3.2 and again on `main` after the merge at `9c61880` | accepted |
| 921 | 2026-09-23 | The design question fired on `deny`'s third fix dispatch, and the answer is to keep patching. The design of one example file, one README and one test stands, and every round-2 finding closed. Severity is converging: blocking in round 1, worth doing in round 2, and now two noted Spec items with a worth-doing rename and one prose rewrite. Every move is prescribed in wording by the round-3 Reviewer: the hook sentence says the `-C` rules must be deleted first, since a hook cannot reopen a denied command; the over-match sentence says "separate word"; `every_entry_has_valid_shape` becomes `malformed_entries`; the self-tests build fixtures from a small known-valid file; and the four sentences are rewritten as given. So the Architect verifies the return itself under step 5's Reviewer-prescribed case, provided nothing is written beyond the prescriptions | accepted |
| 920 | 2026-09-23 | All `quick` round-3 findings are actioned, none dismissed, as fix dispatch 3 under 919's simplification. The worst Standards finding was blocking: resume and discard had no form an operator could type or would ever see. The skill's argument now keys on a leading word. `resume` or `discard` resumes or discards the one parked quick, a bare invocation resumes it, and anything else is a new request. The gate brief and the parked report print both forms verbatim. Text after `resume` is the operator's gate answer, recorded before building. Step 5 branches from `HEAD` at resume and uses that commit as the review's fixed point and verify's baseline. That matches 903's own wording, "the commit its branch started from", and ends the stale-tree, misattributed-red failure that 915's pin created. The re-read gains an outcome: when what landed does or conflicts with the concept, the quick stays parked and the run reports. The row is looked up by effort and slice both, and a new slug is checked against existing rows and branches. `Done when` covers the discard and no-resume exits, and the dangling exceptions clause, the doubled hand-off sentence and the one-line heading go | accepted |
| 919 | 2026-09-23 | Supersedes 915 in part; 914 stands, and a red verify after merge still leaves the row `merged`. The design question fired on `quick`'s third fix dispatch, and the answer is that the design is wrong, but in content, not in shape. So the slice keeps its one file, its Owns, its seam and its place in the graph, and the run continues. The round-3 Reviewer showed where the words go: about 40 percent of the file rebuilds in prose the claim that R3.3 excludes (`base_commit`, `phase`, the lock, acquire, release), one lifecycle case per review round. So it grows each round and would keep growing. It is simplified toward the operator's own R3 flow, which ends at "a closing brief. Done". At most one parked quick per working copy. A new request while one is parked stops and names it. A red verify after merge stops, leaves the row `merged`, writes one decision line and names the fix as the next quick; no post-merge fix loop. The quick's folder is deleted on every exit, since the decision line records the worktree, branch and row. The lifecycle is stated as one table keyed on row status. Each of these narrows an Architect ruling (915's many-parked and dropped-as-record, 903's red-check-as-fix-dispatch), not an operator decision | accepted |
| 918 | 2026-09-23 | `root-test` fix dispatch 2 is verified by the Architect rather than a Reviewer, under `PHASE-3-BUILD.md` step 5's Reviewer-prescribed case, and all three conditions hold. The diff is the one prescribed line in 916, character for character. It adds no scope, touching one line of one file. It is checkable against the cited `set -e` behaviour: `grep -c` with `|| true` still leaves the printed count in the variable. The slice merged at `7f0b587`, and `tests/run.sh` is green on `main` after the merge | accepted |
| 917 | 2026-09-23 | `deny` round 2: every round-1 finding is closed, nothing blocks, and all round-2 findings are actioned as fix dispatch 2, pending a free place. The JSON keeps `//**/.env.*`. The README's remedy for templates is replaced, because the docs say a `!` carve-out cannot reach a `//`-anchored rule and an allow never carves out of a deny. It now gives the two remedies that work: drop the line, or switch to a cwd-relative rule followed by a carve-out in the same file. The `-C` stash and push rules stay, because the matcher has no one-word wildcard to tighten them with. The README names their over-match instead: a `git -C` commit message containing "stash" is denied, which this effort's own commit `7c16505` would have hit. The README also names the open token reads `gh auth status -at` and `gh auth git-credential get`, moves the per-agent limit under what the list cannot enforce, and fixes three dense sentences and the test's shape-check message. The Architect verifies the return itself if every move is applied as prescribed. Anything written fresh goes to a Reviewer | accepted |
| 916 | 2026-09-22 | `root-test` round 2: every round-1 finding is closed and nothing blocks. Standards 1 is actioned as fix dispatch 2, with the Reviewer's prescribed one-line move. The shim step's `grep -c` exits 1 on a zero count, and the default `bash -e` shell then ends the step before its error message prints, so the likeliest failure would show no reason; the move is `|| true`. The Architect verifies the result itself under `PHASE-3-BUILD.md` step 5's Reviewer-prescribed case: the move is prescribed verbatim, adds no scope, and is checkable against the cited `set -e` behaviour. Standards 2, moving the shim script under `tests/` so a local run exercises it, is dismissed on 907's reasoning: it widens Owns for one call site, and actionlint covers the YAML. Spec 1 is recorded rather than fixed. The non-root walkthrough output gains one `ok   test_write_fails_on_readonly_env` line and goes from 18 to 19 tests by design of the 907 split, so verify must not read that line as a regression against plan.md's "unchanged from base" | accepted |
| 915 | 2026-09-22 | All `quick` round-2 findings are actioned, none dismissed, as fix dispatch 2. It waits for a free Builder place. One blocked on each axis. P1's defect started in the Architect's own round-1 prescription, not in the build: the resume pointed into `effort/SKILL.md`'s re-read section, which opens with `capstan-claim acquire`. The Reviewer showed that following it creates a lock that then blocks the next real effort. So a quick writes its own re-read, a `git log <base>..HEAD` and a `git status`, and the "never contradicts" promise is qualified. On the other axis, the resume trigger read any leftover concept folder as a resume. It now keys on the folder's Tracker row: `planned` is parked at the gate, `dropped` is a record. An invocation carrying a new request starts a new quick, and a declined gate gets an explicit discard. The rest: the branch is pinned at the recorded base, so a commit landing between the two runs cannot enter the diff. A red verify recreates the worktree on the existing branch. Both hand-offs say the concept is the whole spec, and that `.capstan/effort/` belongs to someone else. `Done when` covers every stop, and the file drops its self-repetition | superseded by 919 |
| 914 | 2026-09-22 | Supersedes 905 on stops. A quick's row moves to `dropped` on a stop after dispatch and before merge. A stop after merge, which can only be a red verify, leaves the row at `merged` with its commit and reports the red check, because the merged commit shipped and a tracker that marks it dropped cannot answer what shipped. The rest of 905 stands: `planned` when the gate brief posts, `building` at dispatch, `merged` at merge. Found by the `quick` round-2 Reviewer and, separately, by the generic-agent read-back | accepted |
| 913 | 2026-09-22 | The `quick` round-2 blocking Spec finding traces to the Architect's round-1 prescription, not to the Builder, who applied it as written. `PHASE-3-BUILD.md`'s Reviewer-prescribed licence exists for exactly this: a prescription passed on to a fix inherits no review of its own. The round-2 review is what caught it. The same pointer-reaches-claim shape was already in the round-1 finding list as S2's fix. Recorded so the lesson attaches to where it happened: a prescribed pointer into another skill carries everything that skill's section says, not just the part the prescription meant | accepted |
| 912 | 2026-09-22 | All `log-helper` round-1 findings are actioned, none dismissed, as one fix dispatch. It waits for a free Builder place. The blocking finding: `rotate` renamed the active log before the archive. The Reviewer killed it between the two renames, and 812 rows were left only in a hidden temp file that `check` could not see; `check` printed ok and a rerun did nothing. That is the one loss 898 admitted the helper to prevent, so the archive is renamed first, and an interrupted run then refuses on the duplicate. The Reviewer showed eight of nine mutants passing the tests, so the fixtures are extended until each guarantee is held by a test. `find` loops over every `upersedes` phrase on a line and parses the list form rows 58 and 188 use (`Supersedes 36, 43 and 57`), rather than `decision-record` banning a form the log already carries. The other fixes: `check`, `next` and `find` require `decisions.md`, since a wrong-path `next` printed 1 and would double row 1. `trim` strips tabs. `--keep 0` no longer dies on BSD `head`, and missing flag values get `need_value`. The quadratic scans are replaced, because `next` took 3.7 s and every new row calls it. The row pattern lives in one variable. `--help` names the tools actually used | accepted |
| 911 | 2026-09-22 | All `bench` round-1 findings are actioned, none dismissed, as one fix dispatch. The count goes to one. Two blocked. The first is S1, settled at 910. The second is S2, `pricing.tsv` carrying prices under ids no source supports. The Opus 5.5 figure sat on `claude-opus-5`, a different model that real transcripts use, while the real ids use hyphens (`claude-opus-5-5`, `claude-fable-5-1`). So rows are keyed to real ids and an unsourced rate is left empty. `session-usage` treats an empty rate as unpriced and marks the total partial, and prints `unpriced` rather than $0 when nothing priced. The protocol is made runnable: literal commands, dependency install separated from the timed offline test run, one CI-derived test command per repository, one wall-clock definition, "same model" defined as the main session's model pinned the same way in both arms, a fixed arm order, and review returns copied before phase 4 deletes them. `bench/transcripts/` gains a `.gitignore`: this repository is public, and a real transcript carries the operator's email and global instructions. `PROTOCOL.md` uses `arm` rather than `run` and drops "declared" for commands an agent picked, which keeps CONTEXT.md's meanings | accepted |
| 910 | 2026-09-22 | Supersedes 895 on deduplication; the path layout and the fail-loudly rule stand. In subagent transcripts a turn's lines do not carry the same usage: earlier lines hold a partial `output_tokens` and the line with `stop_reason` holds the final count. The `bench` Reviewer found 5,678 of 6,104 repeated ids differing in subagent files, against none of 2,086 in main-session files. The Architect confirmed it on this session's own Scout transcripts: 51 of 52 repeated ids differ. So `session-usage` keeps, per `message.id`, the maximum of each usage field, which is the `stop_reason` line when there is one. Keeping the first line undercounted output four to seven times. That error lands almost entirely on the Capstan arm, because every crew role is a subagent, so it would have flattered the arm being measured. 895's premise came from one main-session sample, which could not show this | accepted |
| 909 | 2026-09-22 | The `deny` Builder's return said its suite run included "log-helper's tests". No `tests/test_log.sh` exists on that branch. The Reviewer reran the suite and it was green, so nothing shipped on the false line. The line matters anyway: a Builder's return is an account the Architect cannot see behind, and one false detail in it means the rest is unverified until someone reruns it. Caught by checking the return against the branch before dispatching the review, per the duty to check a return | accepted |
| 906 | 2026-09-22 | A quick is not an Effort. It holds no claim, does not count against the three-effort ceiling, and its slug fills the Tracker's Effort column the way an effort's slug does. `CONTEXT.md` gains a `Quick` row and `Front door` counts three when the slice merges. It is assumed, not settled, because running a quick beside three live efforts adds Builders and briefs the ceiling was meant to cap. Reopen if quicks start being used to get round the ceiling | assumed |
| 900 | 2026-09-22 | The benchmark proposes pallets/click #373, tj/commander.js #2346 and spf13/cobra #739, with sharkdp/bat #1611 as the spare. That gives Python, JavaScript and Go. Licences and activity were checked against GitHub's API. Suite runtimes and commander.js's test command were not, so `PROTOCOL.md` makes confirming them the operator's first step. Reopen if any of those fails the five-minute, no-network bar | assumed |
| 892 | 2026-09-22 | The Reviewer and `two-axis-review` read the active log in full and grep the archive for the names of the standards they grade. A repository declines a preloaded standard with an `accepted` line, and rotation moves accepted lines, so reading only the active log would silently un-decline it. Reopen if a decline turns up that is phrased without naming its standard | assumed |
| 890 | 2026-09-22 | Archived rows are never edited, including their status cell. A new row superseding an archived one opens `Supersedes NNN.`, and whether an archived row still stands is answered by grepping every log file for that phrase. This relaxes `decision-record`'s "update both files" rule for archived rows only, and the relaxation is the price of an archive nothing rewrites. The phrase is already the convention but not a consistent one: 35 of the 84 superseded rows name their successor that way today. Those older rows are not repaired, because a superseded row already carries its forward link in its own status cell | assumed |
| 889 | 2026-09-22 | Rotation keeps every `open`, `assumed` and `unformed` row plus the 50 most recent rows, and runs in phase 4, after the Courier, only when the active log is over 100 KB. Everything else moves to one new file per rotation, `decisions/archive/decisions-<lo>-<hi>.md` in the document home, newest-first, byte-identical, `capstan_type: decision-archive`. Row numbers stay global: a new row takes one more than the highest number in any file. Measured on this log: 50 recent rows are about 42 KB and the 38 older open or assumed rows about 15 KB, so the active file lands near 57 KB, about 15k tokens, against 392 KB today. The archive sits inside `decisions/`, so `setup`'s existing move of `decisions/` carries it. Reopen if 50 rows turns out too short a window for what an Architect needs unprompted | assumed |
| 871 | 2026-09-22 | Fan-out inside an effort is bounded: three Builders in flight and five fix dispatches per slice, and on the note, by default, through `capstan-max-builders` and `capstan-max-fix-dispatches` in the same two files as the other keys. `capstan-claim dispatch` enforces both from counts held in the claim record, so they survive a run boundary and no run starts a slice at zero. Reaching a limit refuses the dispatch, changes nothing, and ends the run reporting what remains; a run never resets a count to keep going. The defaults are assumed rather than measured: three is as many returns as one Architect reads without one waiting unreviewed, five is past the point where the design question has fired three times. Reopen if a real effort is held below three Builders by the limit rather than by its graph, or a slice legitimately needs a sixth round | assumed |
| 870 | 2026-09-22 | The version decision moves from phase 4 step 1 to phase 3, before verification, and `verify` records the commit it passed against in the claim as `verified_commit`. Phase 4 opens with `capstan-claim drift`, which lists every change since that commit and exits 8 when any sits outside the document home, committed or not; that sends the run back to `verify` rather than shipping on an earlier green. 860 is the case this answers: a merge landed on the verified tree and was re-verified partially with no rule requiring even that. A commit confined to the document home, a decision row say, does not invalidate the verification, since the declared checks do not read it; this is assumed, and reopens the first time a repository's checks lint its `.capstan/` | assumed |
| 856 | 2026-09-09 | `~/.capstan-worktrees/bin/reverse.sh` has been sitting outside the repository since 2026-09-02, left by the effort that built the reverse migration. It is not a worktree, so `git worktree list` never showed it, and `PHASE-4-DELIVER.md`'s teardown reaches only `<working copy>/.capstan/effort*`, so nothing in Capstan was ever going to remove it. Found while checking this effort's own worktrees were gone. It is a delete outside the gitignored scratch, so it is the operator's under the Authority table and is reported rather than removed. Worth more than its size: 852 accepts a residual whose worst outcome is a stray worktree nobody knows about, and here is a seven-day-old artifact of exactly that class found by looking rather than by any rule firing | open |
| 854 | 2026-09-09 | `Red at base` in `CONTEXT.md` names a slice's seam check failing before the slice started, and `test-first` now uses that same observable state to mean a red that is not the Builder's. The text avoids the term so there is no naming contradiction to cite, but the concept sits under two names in one repository. Raised by the round 4 Reviewer as worth the Architect's eye rather than as a defect. Not settled here: this effort has already coined nothing under time pressure at 835 and the same restraint applies | open |
| 853 | 2026-09-09 | The declared-checks handling sentence states two tests as one, temporal and ownership: `red for a reason predating your slice` and `belongs to a different slice`. They diverge on a slice shape Capstan documents. A Builder dispatched from a verify return to fix a red check finds that check red at its branch point, because that is the slice, so the temporal test labels it pre-existing while the ownership test correctly does not. Carried since round 2 and surfaced only now because round 3's determination sentence made the temporal test operational. Risk judged low by the Reviewer, since the dispatch and the demonstration both pull against the wrong reading, and the repair is free: lead with ownership rather than timing whenever the paragraph is next edited | open |
| 850 | 2026-09-09 | The test suite has the same pre-existing-red gap the declared checks just closed, and it is left unplaced deliberately. `test-first` line 54 says to run the full suite once near the end and has never said what a Builder does with a failure that predates its slice. That gap predates this effort entirely. 848 removes the clause that tried to cover it from a paragraph gated on declared checks, where it could never fire; the right home is beside line 54, which governs the suite, and the right route is a requirement and a seam rather than a clause smuggled into a neighbouring rule. Not built here because the operator locked a three-slice shape and this is a fourth change to a file already carrying two fix rounds | open |
| 836 | 2026-09-09 | `DESIGN.md` line 48 and `README.md` line 61 summarise `test-first` as red, green, refactor with tests at pre-agreed seams, and neither now mentions that a Builder runs the repository's declared checks in the same loop. Neither is false, because neither claims completeness, and both are incomplete in the way a reader would feel: asked where Capstan says a Builder runs the linter, `DESIGN.md` gives no pointer. Requirement 6 enumerated rows 55 and 57 only, so row 48 belongs to no requirement and no slice. 743's shape a second time in this effort, a defect existing only where slice 2 and slice 3 meet, and raised here rather than folded into the fix dispatch already running on that file | open |
| 835 | 2026-09-09 | The repository has no word for the division of responsibility between a Builder running declared checks on its own slice and `verify` running them on the integration. `spec.md` requirement 3, `plan.md` and the built skill each reached for `seam`, which the glossary already defines as the test boundary agreed in the plan and which `codebase-design` defines as a place behaviour can be altered without editing there. Three reaches for one wrong word is the signal a term is missing rather than the signal it is settled, so this is recorded open rather than coined at speed. Found by the `declared-checks` Reviewer counting seven uses of `seam` in one file and finding the seventh meant something else | open |
| 818 | 2026-09-09 | A reader arriving at record 0003 directly still reads that `codebase-design` is preloaded on the Reviewer, with nothing on the page pointing at 810. Row 151 carries `superseded by 810` and 151 is what links to 0003, so the path in through the log is intact and the path in through the record is not. Raised rather than fixed because fixing it means either editing an accepted record, which 817 just refused, or a mechanism for annotating records that this project does not have | open |
| 816 | 2026-09-09 | Slice 2's instruction is stack-agnostic and names no commands. The operator's other project, where the sloppy-code symptom was observed, was not named and its stack is unknown, so nothing here is tuned to it; `verify` already owns discovery across CI files, task runners, commit hooks and the contributing guide, and the instruction points at that rather than at any tool. Revisit if a Builder is observed finding declared checks and not running them, which would mean the discovery order is right and the instruction is not | assumed |
| 795 | 2026-08-31 | The forward leg carries the same class of exposure 787 found in the reverse, and it is recorded rather than fixed. `**Onto the board.**` reads the board and the milestones through the same `gh` calls, and nothing in it checks whether those reads succeeded either; a refused read there would look like a board with no matching milestone or issue and would create duplicates rather than strand rows. The `empty-vs-unread` Builder found it while fixing the reverse and left it alone because the slice was scoped to the reverse leg, which was the right call and is the third Builder flag this effort. Left open rather than widened into the same slice: it is a different failure with a different consequence, and this effort has already added one slice after its own gate | open |
| 790 | 2026-08-31 | Three agents read the document home and none is reached by 786's warning, which is the residual this effort leaves rather than a gap in it. `agents/builder.md`, `agents/reviewer.md` and `agents/courier.md` each send an agent to read `CONTEXT.md` or `decisions.md` there, and a `decisions.md 2` beside the real one leaves all three with the same undirected choice the Architect now gets warned about. 786 scoped the warning to the writer and called that the half that matters, which settles it against fixing here. Recorded so it is visible rather than found later, and raised by the `vault-write-warning` Reviewer reading the agent files rather than the diff | open |
| 779 | 2026-08-31 | Two vocabulary gaps raised by the two Reviewers, left open rather than coined on the spot. `CONTEXT.md` names `Load point` for a continuous *discipline*, a skill reached at a named moment, and has no term for a continuous *duty* the Architect owes at every phase, which is what 773 and 774 create; the second Reviewer says that absence is why the placement had nowhere obvious to go. And `Verify` is glossed as running the checks the repository declares against the merged result, while `PHASE-3-BUILD.md` now also uses "verify" for an Architect reading a diff in place of a review. The collision is inherited from 592 and 768 rather than introduced here, and the same file uses both senses eleven lines apart | open |
| 770 | 2026-08-31 | `CONTEXT.md` has no word for reconnaissance the Architect runs itself and files under `scout/`. `Scout` names a role that is dispatched, read-only, and never decides; this effort's `sync-probe.md` says on its face that the Architect ran it, and the knowledge-base note calls it "a probe" because nothing better exists. The gap is new: no previous effort had the Architect gather external facts with its own hands rather than sending someone. Raised by the note Reviewer as a vocabulary gap rather than a finding, and left open rather than settled by coining a word on the spot | open |
| 748 | 2026-08-30 | The forward leg reads `git -C <working copy> rev-parse HEAD` fresh, so a migration interrupted and resumed in a later session names a different commit in issues created after the gap than in ones created before. Recorded rather than fixed: it is a real inconsistency in shipped code, the forward path is not this effort's to change beyond the format declaration, and the value is a provenance stamp rather than anything read back. Raised by the `reverse-step` Builder about a file it was told to treat as its model | open |
| 744 | 2026-08-30 | A torn-down issue closes with reason `completed` regardless of the status its row carried, and the cost is named rather than hidden. Assumed rather than settled: GitHub offers two reasons and neither means what the close actually means, which is that the row left the board. `not planned` would be false of a `building` row that later ships; `completed` is false of a `planned` row that never did. `tracker.md` survives carrying the true status, so this is not 656's failure of a tracker unable to answer what shipped, but it does badge a public issue with something untrue. Reopen if anyone reads a closed board issue as a claim about what shipped rather than as a row that moved | assumed |
| 734 | 2026-08-30 | A milestone whose rows have all returned to `tracker.md` is closed rather than deleted, the same call 729 makes for the issues under it. Assumed rather than settled: nobody has asked what a milestone should mean after a reverse migration, and closing is the least destructive reading that still stops it from looking live. Reopen if an operator wants the milestone kept open because they reuse it | assumed |
| 733 | 2026-08-30 | `tracker.md` is written whole and verified before any teardown begins, and the teardown is resumable, which is 712's rule pointed the other way. This picks the safe failure deliberately: an interrupted reverse leaves both surfaces populated for a while, which is drift a re-run closes, rather than leaving neither, which is state nobody can recover. Assumed rather than settled because it accepts a window in which two records exist, and 727 was just recorded refusing exactly that as a steady state. The distinction is that this one is transient and self-closing; if that turns out to be a distinction without a difference in practice, it reopens | assumed |
| 696 | 2026-08-30 | A migrated row's issue body records that the row came from `tracker.md` and names the commit the file held it at, and nothing else. The four columns carry no description of what a slice did, so any richer body would be invented rather than migrated, and 653's reason for migrating at all is answering what shipped rather than retelling it. Assumed; reopen if a reader of the board finds the bodies too thin to be worth having | assumed |
| 686 | 2026-08-29 | The knowledge base is per-operator configuration living in a per-project system, and nothing addresses that. `capstan-document-home` and `capstan-tracker` genuinely differ per repository; a vault does not, so 685's ask means typing the same absolute path into every project the operator ever sets up, and README's own rule forbids reading a user-level `CLAUDE.md` for these keys. Raised while settling 634 rather than folded into it, because it is a different question and folding it would have let 685 look cheaper than it is. Adjacent to 531, which asks a version of the same thing about a value that is derivable | open |
| 659 | 2026-08-29 | The working copy has a GitHub remote, and the board belongs to that remote's owner. Defaulted rather than settled: a repository with no remote, or one hosted anywhere else, cannot use this surface at all and nothing in the design tells such a user why. Reopen the moment somebody asks for a board that is not the working copy's own remote, which is a reasonable thing to want and is not built | assumed |
| 658 | 2026-08-29 | `gh` is installed and authenticated on any machine running an effort whose tracker is on GitHub. Defaulted because every path in this design calls it and no path checks for it, which is a dependency Capstan has never had before: the crew has needed `git` and nothing else. Reopen on the first report of the surface failing where `gh` is absent, which 648 will surface as an unreachable-GitHub stop rather than as a missing dependency, and that misdiagnosis is the cost being accepted here | assumed |
| 622 | 2026-08-29 | 609's stated reason for placing the version step first, so the note can name the version the work shipped in, is not achieved by this slice and is recorded rather than fixed. The step makes the version available before the Courier runs; nothing consumes it, because `agents/courier.md` names no version and is outside this slice's Owns. Widening to a third file would recut the shape the operator locked at gate 2, which `PHASE-3-BUILD.md` says ends the run. The ordering is still right and the Architect can name the version in the dispatch | open |
| 577 | 2026-08-29 | Whether Capstan gains hooks, and the operator's position is that a hook enforcing beats a discipline missing. This contradicts `effort/SKILL.md`'s closing line, which says no hooks flatly, and `DESIGN.md`'s opening, which makes the absence of an operating layer the project's thesis and which 76 calls the only sentence giving a stranger a reason to choose Capstan. Its own effort per 573, and open rather than assumed because no default is in force: nothing is built either way until it is settled | open |
| 561 | 2026-08-28 | Whether `courier.md` needs a rule against inheriting a predecessor note's sentences. Three notes running have carried a claim verbatim from the note before them, true there and false where it landed: 522 caught it once, 560 catches it again, and both were the blocking finding of their review. `courier.md` names the mechanism for frontmatter, that a neighbour's value is always plausible and nothing surfaces the mistake, and the body has no equivalent. Reading neighbours for convention is required and is exactly what carries the sentences | open |
| 558 | 2026-08-28 | Whether the other four bullets in `## Standards you enforce` need the look 549 gave the `decision-record` one. That bullet framed a discipline by its rarest tier and 557 found the human-facing `DESIGN.md` had the framing right all along, so the class is a payload summary drifting from the discipline it names. Nobody has checked the neighbouring four. Raised because the note put the question in the permanent record while `decisions.md` did not carry it, which would have left the two disagreeing about what is open | open |
| 545 | 2026-08-28 | `SKILL.md` is read whole at the start of every run. Defaulted from how the effort ran today, where invoking the skill loaded the file and each phase file then pointed back into it. An operator who resumes a phase by opening the phase file alone would not load it, and nothing measures which happened. Revisit if a run is observed reaching phase 3 without `SKILL.md` in context | assumed |
| 544 | 2026-08-28 | Naming a moment causes the discipline to load. Defaulted rather than settled, and it is the whole claim of this effort: 533 infers it from three disciplines that loaded at single steps against one that did not, which is an explanation of the past rather than a test of the future. Revisit at the first effort that writes rows after this ships, by asking whether `decision-record` was loaded | assumed |
| 541 | 2026-08-28 | Whether `unslop` has the same missing load point as `decision-record`. The operator's global `CLAUDE.md` instructs applying it to every piece of prose a person reads, unasked and in full, which is maximally continuous, and it was not invoked once across this session's three gate briefs and every message. Same defect class as 533, found by that diagnosis rather than by looking. Carried from 540 | open |
| 503 | 2026-08-28 | Qualifying costs nothing on density. Thirteen longer paths across four already dense files is defaulted as acceptable rather than measured; `PHASE-3-BUILD.md` is at 1205 words against siblings at 313 and 414 before this effort adds to it. Revisit if the result reads worse than what it replaced | assumed |
| 502 | 2026-08-28 | The five sites already glossed in words need nothing. A gloss such as "in the main working copy" is defaulted as being as clear to an agent as the symbolic `<working copy>/` prefix, which is untested. Revisit if a review or a build shows an agent missing one | assumed |
| 481 | 2026-08-28 | `README.md:247` describes the claim's `next` line as what the run after this one picks up, which stays true and is now incomplete, since `next` also carries each live slice's fix-dispatch count. It sits in the borrowed-ideas list as provenance rather than as a specification, and 461 refused README work in this effort. Found by the verify sweep for 410's shape, and the only thing that sweep turned up | open |
| 467 | 2026-08-28 | Widening the claim's `next` line to carry a dispatch count per live slice does not degrade it. Defaulted so 459 could proceed; `next` was deliberately one line and this makes it several. Revisit if a later effort finds `next` has become something an Architect skims rather than reads | assumed |
| 466 | 2026-08-28 | Three fix dispatches is the threshold. Defaulted rather than settled: 430 named a design fault at three, while 269 and 58 ran to nine and seven, so three is the earliest point the record supports and nothing tests it. Revisit if slices trip at three routinely and the answer is always "the implementation" | assumed |
| 442 | 2026-08-24 | Whether the organisation tag is the right one for a Capstan note. `courier.md` says the tag comes from the project, never the organisation that owns the folder, and the folder's notes are not consistent on this. Searching the organisation tag today returns two projects at once, which is the symptom that rule names. Folder-wide and not this note's to fix | open |
| 356 | 2026-08-24 | Whether one repository can carry a document home per sub-project. A monorepo has one `CLAUDE.md` and 234 reads only the working copy root, so frontend and backend in one repository cannot split their logs today. Separable as rows in one tracker via the Effort column, which may be enough | open |
| 343 | 2026-08-24 | `durable artifact` is a counted noun with no glossary row, now hard-coded as "four" across two files. The next artifact makes it five and nothing says what qualifies, which is how the three-becomes-four sweep happens again. Raised by the slice 3 review | open |
| 303 | 2026-08-23 | The knowledge-base note is called three things across the files this effort touched: the knowledge-base note, the permanent note, the permanent record. A glossary row names it; the three sites are not rewritten here | open |
| 280 | 2026-08-23 | Specifying sections changes what an agent writes. Untested, and the same class of gap as 229. Revisit if a later effort produces a spec that ignores the sections | assumed |
| 266 | 2026-08-23 | A Courier told to satisfy a knowledge base's own health check can be pushed into editing notes an earlier decision forbids it to touch. Surfaced to the operator, because the health rule lives in their configuration rather than in Capstan. Redacted per 382; the specifics were theirs | open |
| 93 | 2026-08-21 | `spike` is named inside `interview`'s existing stall route rather than replacing it. The route already asks who builds the throwaway; it gains a name and a discipline | assumed |
| 92 | 2026-08-21 | Both new skills are model-invoked, like every other discipline. Only `effort` is a front door | assumed |
| 26 | 2026-08-21 | `Upgrading` documents the orphan-on-refresh in one clause. Defaulted after two unanswered rounds; revisit if the section should stay shorter | assumed |
| 19 | 2026-08-21 | Dropped "and waits for your go" from the description. It duplicated the closing line. Revisit if the approved wording is wanted verbatim | assumed |
| 13 | 2026-08-21 | The in-session equivalent of `claude plugin update <plugin>@<marketplace>`. Not verified; the CLI form is documented instead | open |
| 8 | 2026-08-21 | The README recommends leaving auto-update off rather than presenting both paths neutrally. Revisit if steering the reader is unwanted | assumed |
