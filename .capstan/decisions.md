---
capstan_type: decision-log
---

# Decisions

| # | Date | Decision | Status |
|---|------|----------|--------|
| 828 | 2026-09-09 | `gh project item-list` returns 30 items by default and `TRACKER-GITHUB.md` declares the read without `--limit`, so on a board holding more than 30 rows the declared read silently returns a subset, exits zero, and looks complete. Board 4 holds 42. The file's own discriminator under Unreachable stops the run says a zero exit is a genuine empty result, which is right for empty and wrong for truncated: a partial read is neither empty nor unreachable and nothing in the file has a name for it. The reverse migration is where this costs most, since it reconstructs `tracker.md` from that same call and would drop twelve rows off this board while reporting success, and 784 deliberately left reversing board 4 untested. 787's shape a third time, a plausible result that is silently wrong, found by reading a board back and getting nothing for three rows that were set. Outside this effort's three slices and not widened into them | accepted |
| 827 | 2026-09-09 | `TRACKER-GITHUB.md`'s write path guards steps 1 and 2 against re-running and leaves step 3 unguarded. Step 1 says to check `Capstan Status` exists before creating it and gives the reason, that a second `field-create` is not obviously a no-op. Step 2 says to check the milestone. Step 3 creates the issue with no check at all, so running the path twice produces a second issue per slice silently, with both carrying the milestone and neither knowing about the other. 712's class exactly: re-runnability needs a check that tests completion rather than trusting the run, and that lesson was learned on the forward migration and never carried to the write path it sits beside. Found by 826 rather than by reading the file | accepted |
| 826 | 2026-09-09 | The phase 2 tracker writes ran twice and opened six issues where three were intended: 58, 59 and 60 at 15:51:30, then 61, 62 and 63 at 15:52:08. The Architect issued one command and saw one result, the second run's, in which the milestone POST failed `already_exists` because the first run had just created it. That failure was the only visible sign anything had happened twice, and it reads as a stale-read problem rather than as a duplicate. Why the command executed twice is not established and is recorded as unknown rather than guessed at. The duplicates stand on a public repository until the operator decides, per the Authority table, and closing rather than deleting is what 729 settled for issues on this surface | accepted |
| 825 | 2026-09-09 | The marketplace named `bytesnation` installs from `BytesNation/capstan`, which GitHub redirects to `MokeyBytes/capstan`, this repository's own origin. One repository under a renamed organisation, not two. Recorded because the Architect had begun recording the opposite, that pushing origin would never reach the marketplace, and checked before writing it. The practical consequence is the ordinary one: an operator who pushes and then runs `claude plugin update` gets this work, and nothing needs a second remote | accepted |
| 824 | 2026-09-09 | Slice 2's instruction reaches its Builder through the dispatch, not through the installed plugin, per 250. A subagent loads skills from the plugin version cache and cannot see an edit sitting in a worktree, so a Builder dispatched during phase 3 would run 2.23.0's `test-first` no matter what the slice wrote. What the slice therefore demonstrates is that the instruction produces the behaviour, observed against 814's disposable repository. Whether the preload fires unattended is a separate claim and it waits on a release the operator pushes at phase 4, which no gate in this effort reaches. The limit is recorded rather than designed around, and it is the same wall 387 hit when a release slice's demonstration was the installed cache holding the new skill | accepted |
| 823 | 2026-09-09 | The version bump leaves the slice cut. `PHASE-4-DELIVER.md` step 1 already owns it, asking whether the effort changes the version and recording the answer either way including a no, so a slice owning `.claude-plugin/plugin.json` would duplicate a step the delivery phase performs. Spec requirement 7 stays a requirement of the effort and is settled at delivery instead. Found while cutting slices, by reading the phase file that owns the file rather than assuming the bump needed a home in the plan | accepted |
| 822 | 2026-09-09 | `Declared check` and `Vendored` gain glossary rows, amended in place per 705. Both were coined elsewhere and decided on repeatedly rather than settled once: `declared check` is load-bearing in `verify`, in `PHASE-3-BUILD.md`'s declared-check case and now in slice 2, and had no row; `vendored` is what 809 turned on and what 85, 147, 148, 153, 161 and 165 each decided an instance of, with the rule stated only inside each folder's own `CREDIT.md`. 665, 674 and 739's shape, a term earning a row once the repository has decided on it more than once | accepted |
| 821 | 2026-09-09 | The tracker for this effort is board 4 with approvals batched at phase boundaries. `MokeyBytes/capstan` is public, so the Authority table makes every board write the operator's, every time, and three slices is roughly a dozen prompts. Skipping the board for one effort was the alternative and it is what 727 refuses: slice state in two places is the condition that rule exists to prevent, and the cost of the prompts is smaller than the cost of a tracker that cannot answer what shipped | accepted |
| 820 | 2026-09-09 | The Architect ran `cd` while checking which skills are vendored, against this skill's own Precondition, which says never to change directory and gives the reason: a session can be rooted anywhere and `git -C` plus absolute paths make its location stop mattering. The session's working directory moved to `skills/` and moved back only because a later command reset it. Nothing was written from the wrong place, so the cost was zero this time. Recorded because the rule is one the Architect enforces on Builders in every dispatch and broke on its first convenient loop, which is 771 and 808's shape again on a rule small enough that nobody would have noticed | accepted |
| 819 | 2026-09-09 | Version 2.24.0, a minor, per 7 and 386 and the same call as 806. Builders now receive two standards they have never been given and run the repository's declared checks inside their own loop, which changes what happens on every slice and is observable to an operator as different Builder behaviour rather than as a repair. 767's patch test does not hold here: an operator does learn something new, that the writer is now given the standard the grader uses | accepted |
| 818 | 2026-09-09 | A reader arriving at record 0003 directly still reads that `codebase-design` is preloaded on the Reviewer, with nothing on the page pointing at 810. Row 151 carries `superseded by 810` and 151 is what links to 0003, so the path in through the log is intact and the path in through the record is not. Raised rather than fixed because fixing it means either editing an accepted record, which 817 just refused, or a mechanism for annotating records that this project does not have | open |
| 817 | 2026-09-09 | Reverses this session's own recommendation on how to reverse 151. The Architect proposed amending record 0003's Decision sentence in place and cited 705, which governs the glossary; `decision-record` says plainly never to edit an accepted record, and it says so two sections after calling the glossary the one file that is never superseded. The two rules are opposites and were conflated. So 0003 is left exactly as written, standing as the history of the original placement, and 810 supersedes row 151 alone. Found by loading the discipline before writing the first row, which is the load point 544 assumed works and this is one instance of it working | accepted |
| 816 | 2026-09-09 | Slice 2's instruction is stack-agnostic and names no commands. The operator's other project, where the sloppy-code symptom was observed, was not named and its stack is unknown, so nothing here is tuned to it; `verify` already owns discovery across CI files, task runners, commit hooks and the contributing guide, and the instruction points at that rather than at any tool. Revisit if a Builder is observed finding declared checks and not running them, which would mean the discovery order is right and the instruction is not | assumed |
| 815 | 2026-09-09 | This repository declares no checks at all: no CI workflow, no task runner, no commit hooks, no contributing guide. So `verify`'s no-declared-checks branch governs this effort's own integration and the gate-3 brief will say the verification was degraded rather than clean. Worth stating at gate 1 rather than discovering at step 8, and it is the reason 814 exists: an effort that adds a rule about running declared checks cannot demonstrate that rule against itself | accepted |
| 814 | 2026-09-09 | Slice 2 is demonstrated by executing against a disposable repository carrying one real linter, not by reading the instruction back. 815 means this repository can never fire the rule, so the alternative was shipping prose nobody has watched work. That is verbatim what 808 recorded eight days ago: a rule was written down, the failure it addressed was already on the record twice, and the next effort repeated it. 677, 712 and 787 each cost this project the same lesson. The repository is deleted at teardown | accepted |
| 813 | 2026-09-09 | The comment rule is Capstan-authored and sits in the same register as Fowler's twelve: a labelled heuristic stated as what it is followed by the move that fixes it, never a hard violation. Importing an external comment standard would make Capstan hold a second outside position on code and would need a second `CREDIT.md` and a second refresh contract, where 0003 was careful that the deep-module position was the first one and said so. Ousterhout-derived in spirit, like the twelve are Fowler-derived, and owned here | accepted |
| 812 | 2026-09-09 | The declared-checks instruction goes in `test-first`, which is Capstan-authored and already preloaded on the Builder and already says to typecheck often, and not in `agents/builder.md`, which slice 3 owns. Two slices owning one file breaks the one-file-one-slice rule and the collision only surfaced once the vendoring check settled where slice 1 could go. It stays a discipline instruction rather than a hook, so 577 stays open rather than being settled sideways by a slice nobody scoped as answering it. `verify` keeps the integration and 190 is unchanged | accepted |
| 811 | 2026-09-09 | `two-axis-review` is preloaded on the Builder. Its own description already says to read it before building to know what you will be judged on, and no decision has ever placed it, so this contradicts nothing and is the cheapest of the three changes. It also carries 809's comment rule to the Builder transitively, which means the writer receives the rule in the same change that gives the grader something to cite | accepted |
| 810 | 2026-09-09 | Supersedes 151 on its placement. `codebase-design` is preloaded on the Builder as well as the Reviewer. 0003 rejected on-demand loading with the reason that a discipline which has to be remembered is a discipline that gets skipped at exactly the moment it matters, and that argument points harder at the writer than at the grader: preloading on the Reviewer alone is that same failure one seat over, with the standard present only where the work is judged and absent where it is done. 639 repaired `DESIGN.md` to say Reviewer-alone and was right about the state of the files; this changes the state rather than the description. 0003's thesis, deep modules as the design standard, is untouched | accepted |
| 809 | 2026-09-09 | The comment rule goes in `two-axis-review`, not in `codebase-design`. `skills/codebase-design/SKILL.md` is vendored byte-identical to upstream under MIT and its `CREDIT.md` states the refresh contract as a re-fetch plus one line re-applied, so a new section there turns every future refresh into a merge, which is exactly what 153 refused for the TypeScript examples. `two-axis-review` is Capstan-authored and its smell-baseline section is already where Capstan supplies code standards the repository did not write. The Architect recommended `codebase-design` to the operator before reading `CREDIT.md`, and the recommendation was wrong for a reason one file read would have caught | accepted |
| 808 | 2026-09-02 | Five Reviewer returns were dispatched on this effort and none was filed, which is the same failure 755 recorded and 768 recorded, on the effort immediately after the one that shipped rules about the Architect skipping steps it owns. `PHASE-3-BUILD.md` step 4 has always said the Architect writes each return verbatim to the scratch. The rule existed, the failure was recorded twice, a whole effort was spent on the class, and the next effort repeated it. That is the finding: writing a rule down did not change the behaviour, and nothing fires at the moment a return arrives to make it. The cost landed on the note Reviewer, which found `review/` and `scout/` empty and said plainly it could grade every claim of the form found by a Reviewer against the log alone and never against a return. 771 said nothing checks the Architect; this is what that looks like when the Architect has just finished writing that sentence down | accepted |
| 807 | 2026-09-02 | The note describes one of 802's two collisions twice and erases the other, so the 787 reintroduction never reaches the permanent record. 802 names the missing-scope branch breaking first-time setup, and separately the mis-keyed `Capstan Status` query that exits zero and returns empty against a field that is set. The note attached the scope probe to the reintroduction and then named the scope probe again as the second item, which leaves the mis-key, the actual reintroduction and the sharper of the two, unrecorded. It also cites 803 as blocking when 803 calls itself the cheaper half. Same shape as 782 and 688, in the section that exists to say where the discipline sat, and the second time in three efforts a note has inverted that section | accepted |
| 806 | 2026-09-02 | Version 2.23.0, a minor, on 780's own reasoning rather than 767's. Nothing here configures differently and no operator learns anything new, which is 767's patch test, and it changes what the Architect does on every slice, which is what made 780 a minor: a fix a verify return prescribes now takes a Reviewer where the Architect had been verifying it, and every return is checked against its artifact and its dispatch. An operator observes that as more review rounds. The repairs to the reverse ride along and would have been a patch alone | accepted |
| 805 | 2026-09-02 | `gh api rate_limit` reports the primary limit full while calls fail on the secondary one, so an agent that checks it before proceeding gets a false green. Observed twice today: 5000 of 5000 remaining, and `gh project item-list` refused on the next line. This is the same class as the `unknown owner type` message 787 turned on, one level out: the signal an agent would naturally consult to decide whether it can proceed is itself unreliable, and the reliable signal is still the call's own exit status. It also settles that no wait-and-retry built on that endpoint would work, which is another reason 648's no-retry rule holds rather than a reason to revisit it. 804's owed verification of the four board statuses stays owed | accepted |
| 804 | 2026-09-02 | The four `soak` rows were moved to `merged` and their issues closed and commented, and the status writes are unverified. The rate limit was exhausted again by the same run that made them, so the issue side is confirmed through REST — all four closed, zero open issues on the repository, the milestone closed, which it only does at zero open — and the `Capstan Status` field values are not, because project fields are GraphQL-only and GraphQL is what is limited. Recorded as unverified rather than assumed, which is the distinction 778's artifact half exists for and the one this effort spent four slices on. The next run's re-read of the world confirms or corrects it, and that is the right place for it rather than a retry here | accepted |
| 803 | 2026-09-02 | The enumeration in slice 4's rule names three reads where the file documents thirteen, and the one it omits is the one 795 holds open. 756 deleted a three-item list on this project six decisions ago for exactly this, and 343 records the counted-noun class still open. The universal `Every read against this surface` does carry the rule and the list only appears to widen it, which is why this is the cheaper half of the same cut: dropping the enumeration closes the Standards finding and the Spec one together, since 799 claims the rule reaches the forward leg and the list is what undercuts the claim | accepted |
| 802 | 2026-09-02 | Slice 4's rule reintroduces 787 through a door its own file documents, and it breaks first-time setup on the way. Two collisions, both blocking. `skills/setup/SKILL.md` handles a missing `project` scope by walking the operator through `gh auth refresh`, which is a nonzero exit that is not unreachable and must not stop the run; a blanket nonzero rule overrides that and tells a first-time operator GitHub is unreachable instead of helping them. And `TRACKER-GITHUB.md` warns one section away that the board read is keyed `capstan Status` rather than `Capstan Status`, so a mis-keyed query exits zero and returns empty against a field that is set — which the new rule declares a genuine empty result, so the reverse removes the key and strands every row. That is 787's consequence exactly, arriving through the trap the same file spends a paragraph on. Found by the `surface-unreachable` Reviewer reading the file's other sections rather than its diff | accepted |
| 801 | 2026-09-02 | The reverse's teardown was interrupted a second time, by a real rate limit rather than a simulated stop, which is stronger evidence than the test it interrupted. 733's window held on real data: 38 rows in `tracker.md`, 33 still on the board, nothing lost and nothing unrecoverable. The limit was exhausted by the teardown's own loop, three or four calls a row against 33 rows, which is the shape 787 was found in and is worth naming as a property of this surface rather than an accident: a reverse migration of any size is the most call-hungry thing Capstan does, and the run it is most likely to be interrupted partway through. Also worth recording is that this failure announced itself accurately, as a rate limit, where the one that produced 787 did not, so the discriminator being keyed on exit status rather than message is what makes both cases the same case | accepted |
| 800 | 2026-09-02 | 784 and 785 are satisfied for everything except the teardown running to completion on real data, and the gap is named rather than glossed. Established by execution against 38 real rows carrying real merge commits: the read succeeded and returned all 38; validation passed on all of them including the `dropped` row 785 put there, which is the first time `dropped` has been exercised anywhere outside a five-row scratch; the reconstruction wrote all 38 and verified them back; the interruption left rows in both places per 733; and the resume-completion test fired correctly, where reconstructing instead would have dropped five rows. What is still not established is a teardown that finishes, because the rate limit stopped it partway both times it has been tried. That is a real gap and it is the same gap either way: the reverse has never emptied a board end to end on real data | accepted |
| 799 | 2026-09-02 | A fourth slice, `surface-unreachable`, owns `skills/effort/TRACKER-GITHUB.md` and settles 796 and 797, added after gate 2 on the operator's approval and the second slice this effort has added that way. It states once, in the file that owns the unreachable stop, how an agent knows a read failed: the call's exit status and never its message. That reaches every caller rather than the one call site 787 happened to walk through, which is what lets slice 3 shrink to the consequence it owns and what closes 797's per-issue sweep and reaches 795's forward leg without touching either | accepted |
| 798 | 2026-09-02 | Worktrees under `/private/tmp` do not survive a run boundary, and this effort's three were prunable after two days. Nothing was lost, because a worktree is a checkout and the branch holds the commits, but a Builder handed one of those absolute paths would have found an empty directory and no instruction covers that. The path was chosen to keep worktrees out of the iCloud-synced tree 392 records, which was right, and `/private/tmp` was the wrong way to get it. A durable unsynced path under the home directory satisfies both, since only Desktop and Documents are synced. Recorded because the failure is silent until a dispatch lands in a directory that is not there | accepted |
| 797 | 2026-08-31 | A second read on the reverse leg believes a zero it has not demonstrated, and it fails safe while reporting the wrong cause. The leg checks each `merged` row's issue against the merge-commit comment format, which `TRACKER-GITHUB.md` calls one further call per slice, and the stop there turns on a count of conforming comments. A limit exhausted partway through a 38-row sweep returns no comment, which reads as zero, which stops the run — so no key is removed and no row is stranded. What it gets wrong is what the operator is told: that a row's issue is malformed, when the board went unreachable. 787's shape one level in, found by the `empty-vs-unread` Reviewer counting the leg's reads rather than reading its prose | accepted |
| 796 | 2026-08-31 | The discriminator between an unreachable board and an empty one belongs in `TRACKER-GITHUB.md`, not at the one call site that needed it first. It is a fact about the GitHub tracker surface, true for every caller, and `TRACKER-GITHUB.md` already owns the unreachable stop while never saying how an agent knows a read failed, which is the precise gap 787 walked through. Placed where slice 3 put it, the rule reaches the reverse leg's first read and nothing else: not step 4's scope probe, not the forward leg, not the per-issue sweep 797 records, not a future third caller. 673's class, and the file it was written into is already the largest skill in the repository | accepted |
| 795 | 2026-08-31 | The forward leg carries the same class of exposure 787 found in the reverse, and it is recorded rather than fixed. `**Onto the board.**` reads the board and the milestones through the same `gh` calls, and nothing in it checks whether those reads succeeded either; a refused read there would look like a board with no matching milestone or issue and would create duplicates rather than strand rows. The `empty-vs-unread` Builder found it while fixing the reverse and left it alone because the slice was scoped to the reverse leg, which was the right call and is the third Builder flag this effort. Left open rather than widened into the same slice: it is a different failure with a different consequence, and this effort has already added one slice after its own gate | open |
| 794 | 2026-08-31 | `empty-vs-unread`'s Tracker row is owed rather than opened, because the Tracker is unreachable and 648 stops the writes rather than the work. Phase 2 says to open a row at `planned` before a slice is dispatched, and the board cannot be written while `gh project` is refused. The claim carries the row's absence and what it owes, which is the run-boundary record the claim exists to be, and the row opens when the surface returns. Recorded rather than left implicit because a board that is missing a slice looks identical to an effort that never cut one, which is the same class of invisible failure 787 is about | accepted |
| 793 | 2026-08-31 | A third slice, `empty-vs-unread`, owns `skills/setup/SKILL.md` and closes 787, added after gate 2 on the operator's approval because it changes the shape they locked. 666 and 718's precedent. The reverse's empty branch removes `capstan-tracker` and continues when no board item carries a `Capstan Status`, and it cannot tell that answer from a read that failed, so a refused read strands every row and drops the key silently. The rule cannot turn on recognising the failure, because the failure this was found by reported itself as `unknown owner type` rather than as a rate limit: what has to change is that zero is believed only when the read demonstrably succeeded, and anything short of that is 648's unreachable stop | accepted |
| 792 | 2026-08-31 | `CLAIM.md` read `phase: concept` and `No slices cut` while two slices were built and under review, which is the fifth step this session the Architect skipped that it owns. `PHASE-3-BUILD.md` step 10 exists to prevent exactly this, and it is the file one of the two slices was editing at the time. `plan.md`'s red-at-base was wrong in the same pass, stating a base count of one against an actual three. Both were found by a Reviewer looking past its diff at the artifacts the effort runs on, and neither would have been found by anything else, since nothing reads the claim but the next run | accepted |
| 791 | 2026-08-31 | Six `verify-fix-licence` findings actioned, none dismissed, as one fix dispatch. The blocking one is invisible from inside this repository, which is why it took a Reviewer to see it: the declared-checks exception grants a licence whose three conditions cannot be evaluated in the case it grants. A failing test suite prescribes no moves and cites no standards, so conditions 1 and 3 have no referent, and an Architect in a repository with CI reads that a licence reaches it and finds two thirds of it inapplicable. It then either treats the exception as dead or invents a bar, which is 772's failure and 775's one level in. The Reviewer's own words are the record worth keeping: the home repository never reaches this branch, which is why the diff looks sound from inside it, and the file ships to repositories that do | accepted |
| 790 | 2026-08-31 | Three agents read the document home and none is reached by 786's warning, which is the residual this effort leaves rather than a gap in it. `agents/builder.md`, `agents/reviewer.md` and `agents/courier.md` each send an agent to read `CONTEXT.md` or `decisions.md` there, and a `decisions.md 2` beside the real one leaves all three with the same undirected choice the Architect now gets warned about. 786 scoped the warning to the writer and called that the half that matters, which settles it against fixing here. Recorded so it is visible rather than found later, and raised by the `vault-write-warning` Reviewer reading the agent files rather than the diff | open |
| 789 | 2026-08-31 | No `spec.md` was written for this effort, and phase 1 requires one in six named sections. The Architect went from the interview straight to `plan.md`, and the miss surfaced only because a Reviewer went looking for the spec its slice was built against and reported grading a partial Spec axis instead. That is the fourth time in this session the Architect has skipped a step it owns, after 755 and 768, and the second time the cost landed on a Reviewer rather than on the Architect. The rule the effort just shipped is the one that would have caught it: a return checked against the dispatch that asked for it, since the plan cites requirements the spec never stated | accepted |
| 788 | 2026-08-31 | Four `vault-write-warning` findings actioned, none dismissed, none blocking, as one fix dispatch. The one that matters is the no-op: the sentence names a hazard and directs nothing, so an Architect meeting a ` 2` sibling still has no move and the default is to merge it, prefer the newer, or say nothing. 786 settles that there is no guard and does not settle that the line must be actionless, and the space between those is a handling clause, which prescribes no scan and builds no operating layer. The handling is 730's shape: name it in the report and do not guess which copy is current. The Reviewer also verified the default-case contrast by running it rather than reading it, confirming a duplicated durable artifact does appear in `git status` at the default | accepted |
| 787 | 2026-08-31 | Running the reverse against real data found a defect no reading had, and it is the one 700 exists to prevent, reintroduced by the fix for it. The GraphQL rate limit was exhausted by the 38-row forward migration, and `gh project` reports that as `unknown owner type` rather than as a rate limit. The reverse read the board, got nothing, and could not tell a genuinely empty board from a read that failed: it reported `read 0 rows`, `validation passed` and `reverse complete`. The shipped step in `skills/setup/SKILL.md` says that if no items carry a `Capstan Status` there is nothing to migrate, so remove the key and continue. On a real run that sequence removes `capstan-tracker` while every row is still on the board, writes no `tracker.md`, and says nothing: rows stranded and the key gone, which is 700's complaint word for word. 648 already says a rate limit ends a run rather than slowing it; what is missing is that a read returning nothing must be proven empty rather than assumed empty, because the failure arrives disguised as a legitimate result. Found by executing on 38 real rows after a five-row scratch had passed the same path cleanly, which is 784's whole reason and the fourth time this project has learned that reading is not verification | accepted |
| 786 | 2026-08-31 | The vault hole gets an agent-facing line in `skills/effort/SKILL.md`, which is the option 761's framing missed. 761 asked guard or warning and took the warning, and the warning it took reaches the operator once, in the README, while choosing where the document home goes. Nothing reaches the agent that writes there afterwards, which is the half that matters, and 761's own words say so. A sentence in the document-home section the Architect reads every run is not the operating layer 98 and 644 refused; it is the same shape as every other stop and caveat that section already carries. It does not close the hole and is not offered as closing it | accepted |
| 785 | 2026-08-31 | `dropped` is not manufactured on board 4. All 35 rows this project has ever produced are `merged`, and a synthetic dropped row would exercise the mechanism while teaching nothing and would sit permanently in the tracker as a slice that never existed, which is the two-records-that-disagree problem in a different costume. The disposable board 784 builds carries a synthetic `dropped` row instead, where it is free. On board 4 the mechanism waits for a real dropped slice, and the wait is the honest state rather than a gap | accepted |
| 784 | 2026-08-31 | The reverse gets its real-data evidence on a disposable second board built from this repository's own rows, not by reversing board 4. Reversing board 4 would undo the migration the operator asked for, destructively, to prove the undo works. The second board carries the real 35 rows, their real milestone spread, and the real merge-commit comments including the two repaired under 750, which is materially more than the five-row scratch the round trip ran on. What it does not test is reversing board 4 itself, and that stays untested deliberately rather than by omission | accepted |
| 783 | 2026-08-31 | Settles 776, and the answer is that the practice was wrong rather than the rule. 592's licence does not reach a fix a verify return prescribed. Step 8 makes verify the Architect's own act and dispatches nobody, so a verify-driven fix the Architect also verifies has no independent eye at any point: the same role found it, wrote the dispatch, and graded the result. A Reviewer-prescribed fix differs exactly where it matters, since an independent grader wrote the moves and the Architect only checks they were applied. One exception is named because it is real: where the verify return is the repository's own declared checks failing, the grader is not the Architect. This repository declares none, which is why the hole went unseen in the effort that created it | accepted |
| 782 | 2026-08-31 | The Architect's own verify return carried a false claim into the note, and it is 752's mechanism at one more remove. `verify-1.md` says both reviews returned blocking findings that landed on the Architect's reasoning rather than a Builder's. The `architect-duties` return, which the Architect wrote itself, carries two blocking findings and both are defects in what the Builder built: the placement under a heading that fires nothing, and the duplicate site with the weaker bar. The 773 over-claim was marked `Noted, routed to the Architect`. So the note promoted a noted item to blocking, dropped two blocking items, and organised itself and its title around the inversion. The Architect also told the operator the same thing at gate 3. What makes it worth a row rather than a correction is the direction: over-owning the errors erased two real Builder-construction defects from the permanent account and credited the Architect's reasoning with catching everything, which is 688's understating-the-discipline exactly, reached by excessive self-blame rather than by flattery. Caught by the note Reviewer opening the returns instead of reading the verify summary, which is 778's artifact half firing a second time in one effort | accepted |
| 781 | 2026-08-31 | The return-check duty caught a false claim on the first return after it shipped, which is the third instance of the pattern 726 and 754 recorded and the first one caught by a rule rather than by luck. The Courier reported that both `CLAUDE.md` and `AGENTS.md` carry `capstan-knowledge-base` and that there was therefore no conflict to stop on; `CLAUDE.md` carries only `capstan-document-home`, and the knowledge base is in `AGENTS.md` alone. The outcome was right and the account was wrong, which is the harmless shape of the same defect and the reason it keeps surviving: a report that reaches the correct answer is not read again. Recorded as evidence rather than as a finding against the note, since nothing downstream depended on it, and because 778's artifact half firing immediately is worth more as a data point than as a complaint | accepted |
| 780 | 2026-08-31 | Version 2.22.0, a minor rather than a patch, and the line between them is worth stating because 767 called the last effort a patch on a test this one fails. 767's test is whether an operator has to learn something new, and nothing here configures differently or asks anything new. What makes it a minor is 687's other half: it changes what the Architect does on every slice. An initial build that might have been Architect-verified now takes a Reviewer, and every return is checked against its artifact and its dispatch before it is accepted. An operator will observe that as more review rounds and slower efforts, which is behaviour changing rather than a repair landing. 386 forces the bump rather than folding it into 2.21.1, which is published | accepted |
| 779 | 2026-08-31 | Two vocabulary gaps raised by the two Reviewers, left open rather than coined on the spot. `CONTEXT.md` names `Load point` for a continuous *discipline*, a skill reached at a named moment, and has no term for a continuous *duty* the Architect owes at every phase, which is what 773 and 774 create; the second Reviewer says that absence is why the placement had nowhere obvious to go. And `Verify` is glossed as running the checks the repository declares against the merged result, while `PHASE-3-BUILD.md` now also uses "verify" for an Architect reading a diff in place of a review. The collision is inherited from 592 and 768 rather than introduced here, and the same file uses both senses eleven lines apart | open |
| 778 | 2026-08-31 | Supersedes 773. A return is checked against its artifact **and against the dispatch that asked for it**, because the two catch different failures and 773 only named one. Its framing, that a report claiming an edit the file does not carry is caught by opening the file, fits 726 exactly and does not reach 754 at all: 754 was an omission, an item neither applied nor declined, so the report and the artifact agree and checking one against the other comes back clean. What catches it is asking whether every item sent came back accounted for. 773 asserted it settled both and settled one. Found by the `architect-duties` Reviewer routing it to the Architect as a spec question rather than building around it, which is the second Reviewer this effort to decline to paper over a defect in the Architect's own reasoning | accepted |
| 777 | 2026-08-31 | Four `architect-duties` findings actioned, none dismissed, as one fix dispatch. Two block. Requirement 5's counting test fails inside one file: the new general duty at line 27 and the pre-existing Scout text at line 186 are two sites for one rule, and the older one carries a weaker bar, "verify anything load-bearing before a decision rests on it" against "check it before you accept it". Neither 726 nor 754 was load-bearing, so the second site licenses precisely the incidents this effort exists to stop, and it is the site an Architect looking for permission will find. The other is placement: both duties landed in `## The crew`, a section about spawning, where nothing fires them, and `CONTEXT.md`'s own `Load point` row states the reason a duty attached to no moment never fires | accepted |
| 776 | 2026-08-31 | Whether 592's licence extends to a fix prescribed by a verify return rather than by a Reviewer. `PHASE-3-BUILD.md` now requires a full Reviewer round for every verify-driven fix return, and this session's Architect self-verified several, so the file as written is already at odds with practice. 592 and 551 speak only to a Reviewer's own prescribed moves, so extending it would license what no decision has licensed, and the `review-boundary` Builder was right to flag it and stop. Recorded open so the next Architect that wants it finds a question rather than a blank, which is the mechanism 772 credits for stopping 768 recurring | superseded by 783 |
| 775 | 2026-08-31 | Five `review-boundary` findings actioned, none dismissed, as one fix dispatch. One blocks, and it is the failure the spec predicted in the direction it predicted. Condition 3 was built as "checkable by reading the diff against those moves", which collapses it into condition 1: a fix that applied the prescribed moves verbatim satisfies it automatically, so the licence asserts three conditions and has two. 592 read the diff against the three disciplines the fix had to satisfy, not against the instruction it was given. The widened form has the Architect confirm the instruction rather than the artifact, which is the exact inverse of the duty this same effort is writing at 773, and it would pass a fix that transcribes a move faithfully and is wrong at a site the move never named | accepted |
| 774 | 2026-08-31 | A dispatch that asserts a fact names where it came from, so the recipient can check it rather than trust it. Settles 752, whose whole mechanism was the Architect writing "three days later" into a Courier's brief with no source attached: the Courier had no way to check it and the note carried it into the permanent record. This is the one rule of the three with no existing form anywhere. The better dispatches this project has sent already do it, citing a decision number, a line, or a grep result, and the ones that did not are where 752 and its relatives happened | accepted |
| 773 | 2026-08-31 | The Architect checks a return against its artifact before accepting it. Settles 726 and 754, and it is the Architect's failure both times rather than a Courier's: a report claiming an edit that is not in the file is caught by opening the file, and nothing said to open it. Written once, as a duty the Architect owes, rather than into each agent's own file, per 673. Scouts, Reviewers and Couriers all return accounts and all three are already trusted this way; 677 and 712 taught the same thing about commands and 754 recorded it about reports without anywhere to put it | superseded by 778 |
| 772 | 2026-08-31 | Settles 768, and corrects it. `PHASE-3-BUILD.md` step 4 already says to dispatch a Reviewer as each Builder returns, unconditionally, so the Architect broke a clear rule rather than bending a vague one. What is missing is the boundary: 592 licenses the Architect to verify a fix that applies a Reviewer's own prescribed moves with no new scope, and that exception lives only in the log, so an Architect reasoning about when review is optional finds nothing written and invents a criterion. Writing 592's exception into the phase file gives the rule exactly one edge, and "the slice looked small" is visibly not it. The cost is real and accepted: three slices this session would have taken three more review round-trips | accepted |
| 771 | 2026-08-31 | Five decisions turn out to be one root: nothing checks the Architect. Reviewers check Builders by construction, and the Architect checks Reviewers and Couriers by reading their returns, and no role and no step checks the Architect, which is also the only role that can skip a step on its own authority. 726 and 754 are the Architect accepting a report without opening the file. 752 is a false claim entering a note through the Architect's own dispatch. 755 and 768 are the Architect deciding alone that a step did not apply to it. Every one was caught downstream or later by the Architect itself; none was caught by a check. Recorded as the root before the three rules that answer it, because each of those reads as a small procedural nicety on its own and they are not | accepted |
| 770 | 2026-08-31 | `CONTEXT.md` has no word for reconnaissance the Architect runs itself and files under `scout/`. `Scout` names a role that is dispatched, read-only, and never decides; this effort's `sync-probe.md` says on its face that the Architect ran it, and the knowledge-base note calls it "a probe" because nothing better exists. The gap is new: no previous effort had the Architect gather external facts with its own hands rather than sending someone. Raised by the note Reviewer as a vocabulary gap rather than a finding, and left open rather than settled by coining a word on the spot | open |
| 769 | 2026-08-31 | Ten note findings actioned, none dismissed, as one fix dispatch. Note count goes to one. Two block, and the sharper one is mine twice over. The note says the deletion step "used to leave an agent with no way to tell a false failure from a real one", and the pre-effort step carried no `rm` command at all, so the zsh no-match hazard was impossible before this effort: the Architect's own dispatch asked for the wildcard that created it, and fix dispatch 1 caught it. The note converted a self-inflicted regression into a pre-existing flaw the effort repaired, which is 688's shape pointed at the effort itself rather than at a person. The other blocking one is the same class smaller: both defects are credited to execution when 766 was found by reading a diff. The note also drops the scope limit that governs everything else it claims, that only iCloud was probed and that `effort*` matching Dropbox and Obsidian Sync conflict names is an assumption sitting inside a shipped instruction | accepted |
| 768 | 2026-08-31 | No slice in this effort was independently reviewed, and the departure is recorded rather than left to look like three clean slices. `PHASE-3-BUILD.md` step 4 says to dispatch a Reviewer as each Builder returns; all three were verified by the Architect instead, on the reasoning that each was a clause or two and each was checkable by execution. Two of the three then had defects the Architect found itself, the zsh no-match edge and the vault contents in the README, which says the verification worked and says nothing about what an independent reader would have found on top. Same shape as 755 one effort earlier, which makes it a pattern rather than a lapse: the Architect skips review when a slice looks small, and small is not the criterion the discipline names | superseded by 772 |
| 767 | 2026-08-31 | Version 2.21.1, a patch rather than a minor, and the first patch of the day against three minors. 687's test is whether an operator has to learn something new: 723 and 750 each added a capability and changed what `setup` asks or writes, and 758 and 756 shipped inside no release of their own. This effort repairs a deletion that missed sync copies and adds two ignore lines. Nothing is configured differently, nothing new is asked, and no operator changes what they do. 386 still forces the bump rather than folding it into 2.21.0, which is published | accepted |
| 766 | 2026-08-31 | One `sync-doc` finding actioned, none dismissed, as one fix dispatch. Count goes to one. The README would publish that the vault used for the test "now holds ten-plus ` 2` duplicates", which is the operator's own vault and its contents, in the most public document this project has. `decision-record` already forbids exactly this of the log, that a line records what the work taught the project and leaves the surface's own contents where they are, and the README is a stronger case rather than a weaker one. The reader needs that the mechanism fires repeatedly in practice; the count in a private vault is evidence the crew used and not information a reader gains. Everything else in the slice reads correctly, separates tested from untested honestly, and names the hole without promising a guard | accepted |
| 765 | 2026-08-31 | One `sibling-delete` finding, found by the Architect executing the step rather than reading it, goes back as fix dispatch 1. `rm -rf <working copy>/.capstan/effort*` errors in zsh when nothing matches, `no matches found`, exit 1, where bash exits 0 silently. The operator's shell is zsh. Step 5 runs with a scratch present on a first pass, so the error only fires on a phase 4 that resumes after the deletion already ran, which the step's own preceding paragraph contemplates by telling a run that ends early to update the claim. An agent meeting exit 1 at the last step of a delivery has no way to tell a false failure from a real one. The fix is the shape `skills/setup/SKILL.md` already uses for the same situation, saying nothing to do is a valid outcome, rather than a shell-specific incantation. Recorded because it is the third time this effort that running a command found what reading it did not | accepted |
| 764 | 2026-08-31 | One `sync-ignores` finding dismissed, none actioned, and the slice merges at zero fix dispatches. The `**/` on `**/.obsidian/` is redundant: git matches a slash-free pattern at any depth already, `.DS_Store` two lines above relies on exactly that, and substituting the bare form was tested against all three cases and behaved identically. Dismissed rather than fixed because the explicit form is defensible on its own terms, the Builder said why it chose it, and a dispatch for three redundant characters costs more than it returns. Recorded so the inconsistency is visible rather than silently accepted, which is the whole reason a dismissal goes on the record instead of nowhere | accepted |
| 763 | 2026-08-31 | `.gitignore` gains `.obsidian/` and `*.icloud`, and gains no pattern for ` 2` duplicates. The two are different things and only one of them is a dropping. An `.obsidian/` folder is editor configuration, and Capstan's document home is designed to be opened in a markdown viewer, so a default home invites Obsidian to write personal config into a repository this project publishes; an `.icloud` file is a placeholder for an evicted file rather than content. Neither is anything a reader needs to see. A ` 2` file is a real copy of real content, and 760 keeps it visible one message before this was asked for: hiding it would trade the only working signal on this side for tidiness. Added on the operator's request, which is also what approves the third slice it needs | accepted |
| 762 | 2026-08-31 | Settles 222, which has been `assumed` since before the document home shipped and said to revisit it before anyone pointed Capstan at a synced vault. Someone has: this repository's own working copy and the operator's vault both sit inside macOS Desktop & Documents sync, with `bird` and the FileProvider running. The vault half of 222 stays untested and now stays untested deliberately rather than by omission, per 761. What 222 asked for has happened, so it closes rather than staying parked | accepted |
| 761 | 2026-08-31 | A document home in a synced vault gets a warning and no guard, and the hole is named rather than closed. There is no git there to surface a duplicated `decisions.md 2`, because Capstan never runs git in a configured home, and the only guard available is listing the vault before every write and looking for ` 2` variants. That is the operating layer 98 and 644 each refused, and `SKILL.md`'s closing line refuses machinery to keep the workflow alive. The cost accepted is real and is the one place refusing machinery leaves a hole a reader cannot see: a README warning reaches the operator, and the agent about to write there reads nothing | accepted |
| 760 | 2026-08-31 | `.gitignore` keeps `.capstan/effort/` and is deliberately not widened to `.capstan/effort*`. A sync-duplicated scratch is not matched by the current entry, so it appears in `git status` as untracked, and that is the only place on the working-copy side where these files surface at all. Widening the entry would hide them, which trades the one working signal for tidiness. Leaving them visible is the feature rather than an oversight, and this line is what stops a later reader from "fixing" the entry | accepted |
| 759 | 2026-08-31 | Settles 392, and corrects its framing. The scratch hazard is persistence, not invisibility: a duplicated `.capstan/effort 2/` is not matched by `.gitignore`'s `.capstan/effort/`, so it does appear in `git status`, and it survives phase 4's `rm -rf .capstan/effort/`, so the stale spec the deletion exists to remove outlives the delivery. Both observed rather than reasoned, by creating the directory and running the commands. That makes 391's account exact and the fix one character in one file. The mitigation stops at the deletion and a README line; a scan for ` 2` siblings on every run is machinery the closing line of `SKILL.md` refuses, and a sync service Capstan does not control writing files it did not write is not a thing it can own | accepted |
| 758 | 2026-08-31 | Settles 740. `Run` widens to cover any single invocation that ends, an effort's phase or a `setup` invocation alike, rather than a second term being coined for the latter. `skills/setup/SKILL.md` already says "the run" at eight sites and says "phase" at none, so the prose was right and the glossary row was too narrow; coining a word would mean rewriting eight correct sites to use one nobody needs. The second half is a real defect rather than vocabulary: `TRACKER-GITHUB.md` titles a section "Unreachable stops the phase", and that file is read by `setup` as well as by the phase files, so it names a thing one of its two callers does not have. 648 wrote that rule when only efforts reached the surface. The two sites become caller-neutral, `phase` stays in `skills/effort/SKILL.md` where all five of its uses are correct, and nothing in `setup` changes | accepted |
| 757 | 2026-08-31 | 756 was repaired in place: it said the enumeration went stale "at three items when the file held four and again when it held five" and offered "correcting to five" as the alternative, and `TRACKER-GITHUB.md` holds seven sections. A factual repair rather than a decision change, so 756's number and status stand and nothing is superseded, and this line is the record that it happened. The repair also sharpens what the defect was: the list was never exhaustive, not even when written, so the problem is a three-item list reading as the whole of what is there rather than a count that fell behind. Caught by the Architect checking its own row against the file after the slice merged, which is later than it should have been | accepted |
| 756 | 2026-08-31 | Settles 741. Line 105's enumeration of what `TRACKER-GITHUB.md` carries is deleted rather than corrected, and replaced by a gloss of what the file is for. `AUDIT.md` says a count that only says how many items are in the list beneath it duplicates the list and should be deleted rather than corrected; a list of another file's contents is the same cache one step out. It names three things against a file that now holds seven sections, and it was never exhaustive even when written, which is the sharper problem: a three-item list reads as the whole of what is there and grows more misleading every time the target gains a section, as it did twice in two efforts. A pointer's job is to say what the material is and when to reach it, which a gloss does and an enumeration only appears to. Chosen over correcting the list, which buys one effort of accuracy and re-breaks on the next section. One clause in one file, so one slice and no fan-out, which `slicing` calls a correct plan rather than a failure to parallelise | accepted |
| 755 | 2026-08-31 | The Architect filed no review returns for this effort, and the cost landed before anyone noticed. `PHASE-3-BUILD.md` step 4 says the Architect writes each Reviewer return verbatim to the scratch; the previous effort filed five and this one filed only the verify return, keeping three slice reviews in a context window instead. The note Reviewer found the directory holding one file and said plainly that grading those reviews through their decision rows was weaker evidence than the returns. Nothing shipped wrong because of it, and the returns are deleted at delivery regardless, so what was lost is what a later reviewer could check during the effort rather than anything durable. Recorded because the failure is invisible from the outside: a scratch with one file looks like an effort with one review | accepted |
| 754 | 2026-08-31 | A Courier report omitted an item it neither applied nor declined, the second time this effort a Courier's account has not matched its artifact after 726 recorded the first. Both were caught by reading the file rather than the report, which is the same lesson 677, 712 and this effort's own round trip keep teaching at different scales. Recorded as a pattern rather than as a defect in either note, since both notes were repaired and neither shipped wrong. What it argues for is that a Courier's report is evidence to check rather than a result to accept, the same standing a Builder's and a Reviewer's already have | superseded by 773 |
| 753 | 2026-08-31 | 746's omission from the knowledge-base note is dismissed rather than actioned, the first finding this effort has dismissed. The note carries `format-contract`'s three fix dispatches, which is the observable fact; 746's reasoning, that three rounds reached the defect by three different methods and so the design was sound, lives in the log the note already points at. One home and a pointer, which is `CONTEXT.md`'s own rule. Dismissed on proportion too: a second Courier round for one sentence at close-out costs more than the sentence returns. Reopen if a reader of the note asks why a slice took three rounds and cannot find the answer | accepted |
| 752 | 2026-08-30 | 561 has a second mechanism nobody had named: a false claim reaching a note from its dispatch rather than from the note before it. The Architect wrote "three days later" into the Courier's brief, the note carried it faithfully, and the Reviewer caught it by checking the dates rather than the prose. 561 is recorded as notes inheriting a predecessor note's sentences, and every round of it so far has been note-to-note; this one is Architect-to-note, which no rule and no review step currently watches. The same review also found the mirror case, a sentence true in the predecessor and true here that was simply dropped. Both belong to 561 and neither is the shape it describes | superseded by 774 |
| 751 | 2026-08-30 | Fifteen note findings actioned, none dismissed, as one fix dispatch. Note count goes to one. Three block. The worst is the Architect's own and it travelled through the dispatch: "three days later" for 707 recurring is false, 707 and 742 carry the same date and their recording commits sit twenty hours apart, and the wrong number is exactly what blunts the finding, converting a same-day recurrence into a plausible lapse of memory. The second hands this effort's own coinage of `Conforming comment` to the previous effort, against 739's own words and against a grep returning zero before this effort and six after. The third pairs 737 and 749 as the Architect's own errors when only 737 is; 749's defect was a Builder's and the Architect only found it. Under it sits 688's shape again: the note credits Reviewers and the Architect and names no Builder at all, while the log records three Builder catches in this effort | accepted |
| 750 | 2026-08-30 | Version 2.21.0, a minor bump, per 7 and 386 and the same call as 687 and 723. `setup` gains a leg it did not have: a project on a GitHub board can return to `tracker.md`, which is new user-visible capability rather than a repair, and `TRACKER-GITHUB.md` gains a second declared comment format that the surface writes. Existing projects are untouched, since nothing reaches the reverse leg unless an operator chooses the default back on a project whose key names a board. The bump ships inside this effort rather than as a separate release commit, per 7. Also recorded here: `#25` and `#26` on this repository's own board were repaired in place to name their final-state commits, so every merged row now carries exactly one conforming comment, which is 735's rule holding on real data rather than only in the file that declares it | accepted |
| 749 | 2026-08-30 | Two `reverse-docs` findings actioned, none dismissed, as one fix dispatch. Count goes to one. The blocking one is a generalisation: `skills/setup/SKILL.md` says the item removal is the one delete *this leg* makes, and the README widened it to either direction, which its own bullet three lines above contradicts by saying moving onto GitHub deletes `tracker.md`. Same claim-contradicts-source class as 637, 639 and 679, and the second time this effort a scope word was widened past what the source supports, after 707. Caught by the Architect reading the diff against the merged step rather than against the dispatch | accepted |
| 748 | 2026-08-30 | The forward leg reads `git -C <working copy> rev-parse HEAD` fresh, so a migration interrupted and resumed in a later session names a different commit in issues created after the gap than in ones created before. Recorded rather than fixed: it is a real inconsistency in shipped code, the forward path is not this effort's to change beyond the format declaration, and the value is a provenance stamp rather than anything read back. Raised by the `reverse-step` Builder about a file it was told to treat as its model | open |
| 747 | 2026-08-30 | The reverse leg runs git at the confirmed path, and `skills/effort/SKILL.md` says Capstan never runs git in a document home configured away from the default. Both the commit of `tracker.md` and the SHA recovery for the resume break there, which takes out the whole vault branch. The fix mirrors the forward leg's own answer, already settled for this exact problem: the commit named is the working copy's own head, which is defined wherever the effort runs, rather than a commit of a file that may sit somewhere Capstan cannot run git. `tracker.md` is written and verified on every branch, and committed only at the default home, where step 8 already tells the operator a vault is theirs to commit. Found by the Builder flagging it rather than inventing machinery for it, which is the handling 678 asks for and the second time this effort a Builder has declined to paper over a gap | accepted |
| 746 | 2026-08-30 | The third-dispatch question fires on `format-contract` at dispatch three and the answer is to keep patching. The slice's design is right: one file declaring one contract is one vertical slice. What each round reached is different, which is the distinction 681 and 713 both turn on. Round one was the Builder grounding the format against the real board. Round two was the Architect's own error, inverting a stop without replacing it. Round three is a format that does not exist yet being needed by a second slice, which could only surface once that slice was written. Three rounds, three methods, no round repeating another's failure | accepted |
| 745 | 2026-08-30 | `Teardown` gains a glossary row, amended in place by the Architect per 705. The word appears eleven times across the reverse leg's new prose with no row, and it names a thing Capstan decides about rather than a GitHub object, which is the distinction 672 drew when it declined `GitHub primitive`. Raised by the `reverse-step` Reviewer as a vocabulary gap rather than a finding | accepted |
| 744 | 2026-08-30 | A torn-down issue closes with reason `completed` regardless of the status its row carried, and the cost is named rather than hidden. Assumed rather than settled: GitHub offers two reasons and neither means what the close actually means, which is that the row left the board. `not planned` would be false of a `building` row that later ships; `completed` is false of a `planned` row that never did. `tracker.md` survives carrying the true status, so this is not 656's failure of a tracker unable to answer what shipped, but it does badge a public issue with something untrue. Reopen if anyone reads a closed board issue as a claim about what shipped rather than as a row that moved | assumed |
| 743 | 2026-08-30 | The reverse's close comment needs its own declared format, and `format-contract` reopens at fix dispatch three to declare it. The slice's step currently points at `TRACKER-GITHUB.md` for a comment naming the commit `tracker.md` came back at, and that file declares exactly one format, `merged in <sha>`. Following the pointer would assert every torn-down row merged in the reverse-migration commit, and would post a second conforming comment on a `merged` row, which the same file makes a stop condition. The round trip in `spec.md`'s test seams would fail permanently on its first run. Found by the `reverse-step` Reviewer resolving a pointer rather than reading past it, and it is the second time this effort a review has caught a defect that only exists where two slices meet | accepted |
| 742 | 2026-08-30 | Sixteen `reverse-step` findings actioned, none dismissed, as one fix dispatch. Count goes to one. Four block and two of them are the same root reaching different consequences: the step characterises the Authority table as governing one row because reconstruction is a creation, and never notices that removing a project item is itself a delete, so a private repository tears the whole board down unattended and 700's stranding warning becomes unreachable there. 707 recorded that exact conflation in the other direction and closed by saying it is the kind that recurs. It recurred inside one effort. The tell was in the diff: the teardown enumerates the issue, the board and the field as things it does not delete, and omits the one thing it does. The fourth is the resume gate testing existence, which 712 already superseded a decision for, and here it is worse than a stall: a run resuming an interrupted forward migration meets the same signal, skips reconstruction, and tears the board down against a pre-migration file | accepted |
| 741 | 2026-08-30 | `skills/effort/SKILL.md` line 105 enumerates what `TRACKER-GITHUB.md` carries as three things, and the merge-commit comment format is now a fourth. No slice owns that file and `plan.md` reasoned it untouched because this effort changes neither the tracker's shape nor the Authority table, both true and neither covering an enumeration. Recorded rather than fixed: the pointer still fires on the right condition so nothing misroutes, and closing it needs a slice the operator has not approved. Same shape as 715, at lower severity. Found by the `format-contract` Reviewer outside its own diff | superseded by 756 |
| 740 | 2026-08-30 | `CONTEXT.md` has no word for a `setup` invocation, and three files now call it "the run". `Run` is defined as one session of an effort bounded by the gate it ends at, and `Setup` as operator-invoked and never part of an effort, so the two definitions exclude each other and the reverse migration falls between them. `TRACKER-GITHUB.md` now carries "stops the run" beside "stops the phase" with nothing saying they are different callers' stop conditions. Raised by the `format-contract` Reviewer as a vocabulary gap rather than a finding | superseded by 758 |
| 739 | 2026-08-30 | `Conforming comment` gains a glossary row, amended in place by the Architect per 705. The term is coined in `TRACKER-GITHUB.md` by this effort and slice 2 will lean on it, which is 665 and 674's shape: a term earns a row once the repository decides on it repeatedly rather than when it is first written | accepted |
| 738 | 2026-08-30 | Settles the contradiction between spec requirements 3 and 9. A non-conforming comment on a `merged` row does not stop the run; it is reported alongside the commit that was read. What stops is a count, zero conforming comments or more than one. This closes the hole the previous fix opened without restoring the rule it removed: making any commit-naming comment fatal would let a stranger asking whether something merged in `abc123` end an operator's run, and 731 wants an undeterminable commit stopped rather than every unfamiliar one. Reporting satisfies 731's own "report what was found" on the determinable-but-wrong case, which is the case this repository's own board is in on two rows | accepted |
| 737 | 2026-08-30 | Six `format-contract` findings actioned, none dismissed, as one fix dispatch. Count goes to two. The blocking one is the Architect's own: fix dispatch 1 inverted the stop condition so that non-conforming comments are ignored, and never replaced the protection it removed, so a row carrying one conforming comment and a correction naming a different commit reads back the stale value silently. That is verbatim the harm 735 was recorded to prevent, reintroduced by the fix for it, on this repository's own rows 25 and 26. The Reviewer found it by running the declared regex against all 28 issues rather than by reading the diff, which is the same execution-beats-reading lesson as 677 and 712 and the third time this project has learned it | accepted |
| 736 | 2026-08-30 | Two `format-contract` findings actioned, none dismissed, as one fix dispatch. Count goes to one. One came from the Builder, which grounded the format against this repository's real board rather than against the file and found the supersession comments 735 records. The other is the Architect's, reading the diff: the declaration makes any non-conforming comment on a `merged` row fatal, and a board's issues carry ordinary human discussion, so a single question typed by a teammate would stop a reverse migration. Both are the same root, a contract written from the write path alone without asking what a real issue actually accumulates | accepted |
| 735 | 2026-08-30 | A `merged` row's issue carries exactly one conforming merge-commit comment, and 675's correction is an in-place edit of that comment rather than a second one appended. The board had no way to express 675 at all: under `tracker.md` the Commit column is a cell that gets overwritten, and nothing said what the board's equivalent was. The Architect improvised, posting `Superseded by <sha>` comments on three issues during the forward migration, so this repository's own tracker now holds rows whose first conforming comment names a stale commit and whose current one is unparseable. A reverse migration would have taken the stale value silently. Editing in place mirrors the cell exactly, one value overwritten, and keeps parsing to a single conforming comment. The history of the correction lives in the log, which is where `tracker.md` already leaves it. Non-conforming comments are ordinary discussion and are ignored rather than fatal; zero conforming comments on a `merged` row, or more than one, is the stop condition | accepted |
| 734 | 2026-08-30 | A milestone whose rows have all returned to `tracker.md` is closed rather than deleted, the same call 729 makes for the issues under it. Assumed rather than settled: nobody has asked what a milestone should mean after a reverse migration, and closing is the least destructive reading that still stops it from looking live. Reopen if an operator wants the milestone kept open because they reuse it | assumed |
| 733 | 2026-08-30 | `tracker.md` is written whole and verified before any teardown begins, and the teardown is resumable, which is 712's rule pointed the other way. This picks the safe failure deliberately: an interrupted reverse leaves both surfaces populated for a while, which is drift a re-run closes, rather than leaving neither, which is state nobody can recover. Assumed rather than settled because it accepts a window in which two records exist, and 727 was just recorded refusing exactly that as a steady state. The distinction is that this one is transient and self-closing; if that turns out to be a distinction without a difference in practice, it reopens | assumed |
| 732 | 2026-08-30 | The merge-commit comment format is declared once in `skills/effort/TRACKER-GITHUB.md` and both directions read that declaration. Forward writes the comment and reverse parses it, so the string is a contract between two paths that no file currently names as one. Left undeclared, a change to the forward wording silently breaks the reverse, which is the class 673 and 652 already cost this project twice. Naming it is what makes 731's stop condition checkable: a comment that does not match the declared format is unparseable rather than merely unfamiliar | accepted |
| 731 | 2026-08-30 | The board is the source of truth at reverse-migration time, and unparseable state stops the run. A status outside the four, a row with no milestone, or a merge-commit comment that does not match 732's declared format each end the run with what was found reported, rather than being guessed at or repaired. Nothing is reconciled against git history: 644 warns that an issue anyone can edit by hand makes drift certain, and reconciling that drift is the operating layer this project refuses to build. Same shape as 648 and 240, a missing or self-contradicting source of truth stopping rather than being worked around | accepted |
| 730 | 2026-08-30 | The reverse reads only board items carrying a `Capstan Status` value, and everything else on the board is left untouched and named in the report. A board holds whatever anyone puts on it, which this project demonstrated on itself: fifteen non-slice issues were opened on this repository and deleted twenty minutes later, per 727, and its issue numbering already carries pull requests the migration never touched. Sweeping those into `tracker.md` would invent slices that never existed | accepted |
| 729 | 2026-08-30 | Reverse migration removes each row's item from the board and closes its issue with a comment naming the commit `tracker.md` came back at. Issues are not deleted. Deleting is irreversible and destroys a public record other people may have linked to, which is a worse cost than the one it avoids. The counter is real and recorded rather than resolved away: a closed issue still carries its `Capstan Status` and still answers what shipped, so it is arguably a second record and only deletion truly closes 727's rule. What decides it is that the board stops holding slice state once its items are gone, and the board is the tracker surface; the issues left behind are ordinary GitHub history rather than a surface Capstan reads | accepted |
| 728 | 2026-08-30 | Settles 700. `setup` gains a reverse migration reconstructing `tracker.md` from the board and then tearing the board down, on the operator's approval. 700 held that nobody had asked for the reverse and that building it for symmetry is how an effort doubles; the operator has now asked, which removes the reason it was declined rather than overturning it. The warning 700's own wording asks for, that rows are stranded with nothing saying so, is not a separate build: describing the batch before asking approval is already required, so declining the migration is what produces the warning | accepted |
| 727 | 2026-08-30 | One surface holds a record, never two, and the backlog is not an exception. Fifteen `open` rows were opened as GitHub issues pointing at the log and then deleted, on the operator's correction, twenty minutes later. 98 and 644 already settled this and the pointer framing did not escape either: a question can be closed on the issue and left `open` in the log, which is the drift they refuse. Two things make the log the surviving surface rather than a preference. `interview` reads `decisions.md` for `open` and `assumed` lines before its first round, so moving them breaks a discipline mechanically. And the open rows are not a separable artifact: they sit among 711 others in one file, so lifting them out splits one record across two surfaces, which is the same error one level down. The tell was in the build: the Architect finished by naming the drift it had just created and asking whether to tolerate it, which is the point the thing should not have been built | accepted |
| 726 | 2026-08-30 | Two note findings from the Architect's own verification: one actioned as fix dispatch 2, one dismissed. Note count goes to two. Actioned: the note attributes `CONTEXT.md` rows 28 and 38 to "705 and 721" when 721 amended those two and 705 amended the `capstan-document-home` row, which is the misattribution class the note review had just spent fifteen findings correcting and is worse in a permanent record than anywhere else. Dismissed: two colons the Reviewer graded as mid-sentence connectors read as colons before an elaboration, which `unslop` permits, and the call was borderline both ways. What is worth recording about them is not the style: the Courier's report stated both were rewritten as separate sentences and neither was, so an account did not match its artifact and was caught only by checking the file. Same lesson as 677 and 712 at a smaller scale | accepted |
| 725 | 2026-08-30 | Sixteen note findings actioned, none dismissed, as one fix dispatch. Note count goes to one. Two block. The note attributes 704's mid-effort widening to `skills/effort/SKILL.md` when 704 and requirement 11 both name `skills/setup/SKILL.md` step 1, which merges that decision into 699 and erases that a Reviewer found a gap in the Architect's own spec; and a sentence in `## Still open` lost its main clause, on the one carried item about data loss. The pattern under them is one thing, and it is 688 arriving again in a different shape: the note reports what the effort produced and under-reports the discipline that produced it, omitting 701, 705, 707's actual finding, 714, 721, 343's narrowing, three of five fix-dispatch counts, and that nothing is pushed. The Reviewer also cleared 561 on evidence rather than assertion, checking each sentence carried from neighbouring notes and finding every carried claim true here | accepted |
| 724 | 2026-08-30 | Records 690 through 723 were written dated 2026-08-29 and are repaired in place to 2026-08-30, the day they were actually settled. A factual repair rather than a decision change, so every number and status stands and nothing is superseded, which is the handling `decision-record` prescribes and this line is the record that it happened. The Architect carried the previous effort's date forward instead of reading the current one; the error was caught by the Courier dating the knowledge-base note correctly from the same source the Architect had ignored | accepted |
| 723 | 2026-08-30 | Version 2.20.0, a minor bump, per 7 and 386 and the same call as 687. `setup` gains a step it did not have, migrating an existing `tracker.md` onto a board and deleting it, and it writes a different value for `capstan-document-home` at the default than every previous release wrote. Both are user-visible and neither is a patch. Existing projects are untouched: an absolute path already written stays valid per 699, and a project that never chooses GitHub never reaches the migration step. The bump ships inside this effort rather than as a separate release commit, per 7 | accepted |
| 722 | 2026-08-30 | The third-dispatch question fires on `setup-migrate` a second time, at dispatch four, and the answer is again to keep patching. The rule says to ask every time it would fire and to record it whether or not the answer moved. The slice's design is still right and this round reaches it for a reason the previous three did not: `skills/setup/SKILL.md` lines 10 and 42 are framing prose stating the four artifacts unconditionally while the same file's steps 2 and 3 state the set conditionally and correctly, so the file contradicts itself rather than the world. Red at base, dating to the skill's creation, so it is neither this effort's regression nor a Builder's. Dispatched as an exhaustive sweep of the file for that one class with the bar that a second pass finds nothing, which is 681's shape and the one that just worked on `record-count` | accepted |
| 721 | 2026-08-30 | `CONTEXT.md` rows 28 and 38 are amended by the Architect, per 705, and the glossary contradicting itself is what forces it. Row 28 defined the Document home as where the glossary, the log, the records and the tracker live, unconditionally, while rows 29 and 30 two lines below state the surface choice correctly, so the file disagreed with itself and row 28 was false of this repository the moment the migration landed. Row 38's `.capstan/` listing is the weaker case, closer to the README layout block verify-1 judged correct, and is qualified rather than rewritten. Found by the `record-count` Reviewer reaching outside its own diff, the fourth time this effort a review has caught the Architect rather than a Builder | accepted |
| 720 | 2026-08-30 | Two `record-count` findings actioned, none dismissed, as one fix dispatch. Count goes to one. The Reviewer re-ran the sweep itself across all 153 lines and confirmed line 10 was the only site in the class, which is the evidence 681's bar asks for and the first time a sweep dispatch has been independently graded as exhaustive rather than taken on the Builder's account. Its worst finding is that the fix corrected a count where `AUDIT.md` says to delete one: the ordinal `a fourth` counts the list beside it in the same sentence that names another file as owner of that count, and it constrains nothing. Dropping it half-closes 343 in passing. The second move carves Capstan's own Tracker out of line 40's list of operator surfaces, since 644 makes board state the project's own and an agent could otherwise read that line as forbidding it to name what a board holds | accepted |
| 719 | 2026-08-30 | Approving a slice carries approval for that slice's own Tracker row transitions, and this extends 695 rather than reinterpreting 645. A row moving `planned` to `building` to `merged` is the mechanical consequence of a slice the operator already approved, not a separate judgment they are being asked to make, and 645's confirmation exists for content a third party sees rather than for state a decision already settled. Asking per transition is the 80-confirmations failure 695 already priced and refused. What is not covered stays uncovered: anything the operator has not approved as work, and anything whose content they have not seen, still asks. Recorded when the fifth slice's row had to be opened on a public board minutes after the slice itself was approved | accepted |
| 718 | 2026-08-30 | A fifth slice, `record-count`, owns `skills/decision-record/SKILL.md`, settling 715 on the operator's call because adding it changes the shape locked at gate 2. 666's precedent rather than 660's: 660 deferred new capability, while this is a release contradicting itself about a countable fact, which is what 673 and 679 each already cost. The contradiction is live rather than theoretical, since this repository is now the first project running on a GitHub surface and so ships the skill that contradicts it. The counter was named and not taken: V1 blocks no behaviour, unlike 692 and 531 which were pulled in because migration could not land beside them, and that difference is why it went to the operator instead of being added unattended. Dispatched as an exhaustive sweep of the file under both surfaces with the bar that a second pass finds nothing, per 681, rather than a point fix at the one line the sweep found | accepted |
| 717 | 2026-08-30 | A comment naming one slice's merge commit was posted to another slice's issue in error and was corrected in place rather than deleted. The correction names what was wrong, leaves the original merge commit standing, and states what is actually true of that row. Deleting would have been a delete on a public surface needing the operator under the Authority table, for a mistake the crew made and could repair itself, and it would have removed the evidence that the repair happened. Same reasoning the log already uses for repairing a line in place rather than rewriting history | accepted |
| 716 | 2026-08-30 | V2 goes back to `docs` as fix dispatch 2, unattended. `README.md` lines 107 and 127 both say `setup` moves the four artifacts and that `tracker.md` resolves to the document home, which is three and false respectively under a GitHub surface. Both sites sit in the `## Document home` section, which `plan.md` names as part of slice 3's seam, and inside the file slice 3 already owns, so nothing about the gate-2 shape changes. Not a review miss: the Reviewer graded requirement 9 and these are outside it | accepted |
| 715 | 2026-08-30 | V1 needs a fifth slice and goes to the operator rather than being added unattended, the same call 663 and 666 made. `skills/decision-record/SKILL.md` states unconditionally that the log, records, glossary and tracker are the four durable artifacts, which `skills/effort/SKILL.md` states conditionally and correctly, and no slice's Owns covers that file. It is red at base, byte-identical at `0325bb4`, so it predates this effort: it became wrong when the GitHub surface shipped in 2.19.0 and `github-surface` repaired one file and missed the other. This effort is only the first thing to make it visible, because this repository is now the first project running on that surface. `spec.md` put 343 out of scope on the reasoning that the count is already stated conditionally, which was true of the file checked and never checked against this one, so the out-of-scope reasoning was incomplete rather than any Builder's work being wrong | accepted |
| 714 | 2026-08-30 | The Architect executes `migrate-run` rather than dispatching a Builder, and the departure from `plan.md`'s slice shape is recorded rather than left as an inconsistency. Two reasons, both structural. `AGENTS.md` is gitignored, so it cannot exist inside a worktree and a Builder handed that slice could not write one of the three files it owns. And the operator's approval under 695 is held by the Architect, which is the role that asked for it. The trackable part still goes through a branch and a merge so the Commit column means the same thing for this row as every other | accepted |
| 713 | 2026-08-30 | The third-dispatch question fires on `setup-migrate` and the answer is to keep patching. Recorded because the rule says to record it every time it fires, whether or not the answer moved. The slice's design is right: one file owning the migration procedure is one vertical slice, and nothing about the cut produced this. Rounds 1 and 2 were review-driven readings and round 3 came from executing the procedure, which is a different method finding a different class of defect rather than the same method failing three times. That is the distinction 681 drew when it reshaped a dispatch instead of recutting a slice, and here the dispatch shape needs no reshaping either: execution found a named clause and the fix is that clause | accepted |
| 712 | 2026-08-30 | Supersedes 697 on its completion condition. Re-runnability needs the resume check to test completion, not existence: 697 held that `tracker.md` is deleted once every row has an issue carrying its status, and this migration produced a row that met that and was still unfinished. The real run timed out between `gh issue comment` and `gh issue close` on `setup-surface`, so the issue existed, carried `merged`, and stayed open; the resume skipped it on existence and would have left it open permanently. Found by executing the migration rather than by reading it, which is 677's rule holding a second time. 697's core claim, re-runnable rather than transactional, is unchanged and right: the resume correctly skipped the other 18 rows it had already finished | accepted |
| 711 | 2026-08-30 | `sentinel` gets no glossary row, and the README drops the word instead. 672's shape, declined rather than left unsaid. The value is `default` and that is what a reader needs; `sentinel` is a programmer's word for what the value *is structurally*, which is the kind of thing `CONTEXT.md` holds only when the repository decides on it repeatedly. It appears once in the README, where the code block under it already shows the value, and the agent-facing files that use it are read by agents who lose nothing. Raised as a vocabulary gap by the `docs` Reviewer, which graded nothing on it | accepted |
| 710 | 2026-08-30 | Seven `docs` findings actioned, none dismissed, as one fix dispatch. Count goes to one. Nothing blocked, which is the first slice this effort has had reach review clean on its blocking axis, and the Reviewer verified the two claims most at risk against their merged sources rather than against the diff. The one real accuracy defect is the README saying a refused migration "leaves nothing half-configured": true of the keys, which is all 708 settled, and false of the working copy, because step 3 moves three artifacts on its own approval before step 4 ever asks. The worst standards finding is a garden-path clause sitting on 707's distinction, which is the most consequential sentence in the diff. The spec finding and one standards finding are the same cut: line 91 is requirement 5's scope, outside the seam, and the second copy of a rule line 151 already carries | accepted |
| 709 | 2026-08-30 | The migration approval block's own guard is incomplete and goes back as fix dispatch 2 on `setup-migrate`, count to two. It states "Reached only when `tracker.md` exists at the home currently in force" and omits the surface condition, so a run choosing the default tracker on a project that has a `tracker.md` would be asked to approve a migration step 5 then correctly declines to run. No data consequence, and the approval is spurious rather than dangerous, but an operator asked to approve work that never happens learns to approve without reading, which is the failure 695's single batch approval was already balancing against. Found by the Architect verifying the fix rather than by a review, which is the first finding this effort has caught that way. The six review moves themselves all verified clean by reading | accepted |
| 708 | 2026-08-30 | The migration's visibility check and its batch approval move ahead of step 4's key writes, rather than the refusal rolling a write back afterwards. Settles P2. The Builder invented a rollback because it needed a refusal branch and none was specified, and its reasoning was sound: leaving `capstan-tracker` written while `tracker.md` still holds every row is the two-records-disagree state 644 refuses. The shape was the wrong answer to a right question. Asking first is what every other stop in `skills/setup/SKILL.md` already does, and step 4's own text gives the reason, that checking early would walk an operator through a grant for a run a later refusal could still abort. Reordering deletes the rollback, its under-specified restore target, and the only stop in the skill that left the working copy modified | accepted |
| 707 | 2026-08-30 | The gate on deleting `tracker.md` is not visibility-branched, and conflating it with the board-write gate is a defect rather than a simplification. 645 branches on visibility because a write to a public board is third-party-visible and a write to a private one is not. The Authority table's other row governs deletes, names no visibility at all, and 653 puts the whole migration on the operator's approval unconditionally. Two gates over one step, and the Builder folded them into one, so a private repository would have deleted a committed file unattended. Recorded because the two gates read alike at the point of use and the conflation is the kind that recurs | accepted |
| 706 | 2026-08-30 | Eight `setup-migrate` findings actioned, none dismissed, as one fix dispatch. Count goes to one. Both blocking ones are the same failure in two places: a single source of truth restated instead of pointed at, which is what 673 and 662 each already cost an effort. The Authority table's compression dropped its deletes row, and the write path's compression dropped `dropped`, so a migrated `dropped` row would stay open forever against a file that closes an issue in both terminal cases. The Reviewer also corrected the Builder's account of a clause it removed as unreachable: unreachable within one run, reachable across two, which is the distinction the account missed | accepted |
| 705 | 2026-08-30 | `CONTEXT.md`'s `capstan-document-home` row is amended by the Architect directly rather than assigned to a slice. The glossary is the one durable artifact edited in place rather than superseded, and no slice's Owns covers it, so routing it through a Builder would mean widening a slice's ownership to reach a file the `decision-record` discipline already puts in the Architect's hands. Found by the `home-sentinel` Reviewer, which noted `plan.md` reasoned explicitly about leaving `DESIGN.md` alone and said nothing about the glossary. The row said the key holds an absolute path, which 699 made false | accepted |
| 704 | 2026-08-30 | `spec.md` gains requirement 11 and slice 2's seam widens, rather than the gap waiting to be found at slice 4. `skills/setup/SKILL.md` step 1 ends the run on any value that is not an absolute path, so `setup` rejects the sentinel it will itself write and this repository could not re-run it after slice 4. Requirement 6 governs what `setup` writes and nothing governed what it accepts. 662's shape exactly: the plan is amended and the Builder is not charged with widening its own scope. Found by the `home-sentinel` Reviewer reaching outside its own diff, which is the third time in two efforts a review has caught the Architect rather than a Builder, after 663 and 671 | accepted |
| 703 | 2026-08-30 | Four `home-sentinel` findings actioned, none dismissed, as one fix dispatch. Count goes to one. The blocking one is that line 92 says a project already running on the default is asked the same way as one with no key, which was true when running on the default could only mean an absent key and became false the moment `default` was a value a project could carry. An Architect holding the sentinel would read a lexical match for its own situation and re-ask a settled question, which is 352's failure returning through the fix for it. Verified at the line before actioning. The standards finding is the two-line example block, copyable as the duplicate-key state this same section stops the phase for, which 413 tightened `setup`'s own criterion against | accepted |
| 702 | 2026-08-30 | `DESIGN.md` is deliberately untouched by this effort and the non-change is recorded rather than left silent. Its two relevant sections argue why the tracker gained a second surface and what the board's missing fixed point costs; migration changes neither claim, and both stay true with `tracker.md` gone from this repository. Recorded because 637, 639 and 679 were each `DESIGN.md` carrying a claim its own source contradicted, so on this file leaving it alone needs to be visibly a decision | accepted |
| 701 | 2026-08-30 | This effort migrates its own tracker rows, switching its surface mid-run. The four slices open as rows in `tracker.md` at phase 2 because the key is still unset; slice 4 then carries them onto the board with the 20 historical ones and deletes the file, so phase 3's remaining transitions and all of phase 4 read the board. Stated in `plan.md` rather than discovered in phase 3. It also makes the migration a better test than the historical rows alone: those are all `merged`, while this effort's rows are `planned`, `building` and `merged` when they move, exercising three of the four statuses on real data. `dropped` stays untested | accepted |
| 700 | 2026-08-30 | Migrating a board back to `tracker.md` is not built and is recorded rather than left unsaid. `setup` already lets a project choose the default back and removes the key when it does, so the rows stay stranded on the board with nothing saying so. 653 settled the forward direction only and nobody has asked for the reverse; building a second migration to make this effort feel symmetrical is how it doubles. Raised while settling 690's scope | superseded by 728 |
| 699 | 2026-08-30 | Settles 531. At the default `capstan-document-home` is written as the literal `default`, resolving to `<working copy>/.capstan/`, and `skills/effort/SKILL.md` line 61 accepts that one sentinel alongside an absolute path. All three states stay distinct, which is 352's whole requirement: absent means never asked, `default` means asked and chose the default, an absolute path means asked and chose that path. It publishes nothing, and it does not weaken the absolute-path stop, because the sentinel resolves against the working copy root the Precondition establishes absolutely rather than against the session's working directory that rule exists to forbid. The gain is not this repository's own exposure, which is already in its history and stays there: it is that every user who runs `setup` and takes the default currently commits their own home directory into their own repository. An absolute path already written stays valid and nothing has to change | accepted |
| 698 | 2026-08-30 | Settles 692. `setup` places the two keys per key rather than per file, which is what its own **Done when** already requires and what step 4's prose contradicted. On this repository that puts `capstan-document-home` in `CLAUDE.md`, which is committed, and `capstan-tracker` in `AGENTS.md`, which is gitignored. The reason is not secrecy, since a public board on a public repository discloses nothing: it is that a contributor who clones and runs an effort would otherwise be pointed at a board they cannot write to, and 648 would stop their phase. Reading no tracker key instead, they fall through to `tracker.md` and it is created on their first write, which is the correct experience rather than a degraded one | accepted |
| 697 | 2026-08-30 | Migration is re-runnable rather than transactional: every create checks for its own result first, by milestone title and by issue title within the effort's milestone, and `tracker.md` is deleted only after every row has an issue carrying its status. `TRACKER-GITHUB.md` already prescribes check-before-create for the field and the milestone, so this extends an existing shape rather than inventing one. Assumed rather than settled, because 648 stops the phase on an unreachable GitHub and a 20-row batch is long enough to be interrupted partway, leaving a public board half-populated with no other way back. Reopen if a re-run is ever observed creating a duplicate | superseded by 712 |
| 696 | 2026-08-30 | A migrated row's issue body records that the row came from `tracker.md` and names the commit the file held it at, and nothing else. The four columns carry no description of what a slice did, so any richer body would be invented rather than migrated, and 653's reason for migrating at all is answering what shipped rather than retelling it. Assumed; reopen if a reader of the board finds the bodies too thin to be worth having | assumed |
| 695 | 2026-08-30 | The migration's writes to the public board take one operator approval covering the whole batch rather than one per write. 645 puts the operator on every third-party-visible write, and 20 slices at roughly four calls each is 80 confirmations for a single reviewed operation whose plan is fixed before it starts. The approval is granted against that shown plan, so anything departing from it needs its own | accepted |
| 694 | 2026-08-30 | `.capstan/tracker.md` is deleted once its rows reach the board, per 653. The cost is real and named: the file is committed and public and is this repository's only worked example of the default surface, on the repository that documents that surface. Keeping it is the two-records-that-disagree state 644 refuses outright, and `skills/effort/SKILL.md` already carries a sample table inline for anyone who wants the shape | accepted |
| 693 | 2026-08-30 | 531 is settled inside this effort rather than left open another cycle. It is the sanitization the operator asked for, its scope is one key's written form, and 686 is the same shape one key over, so settling it here gives 686 a precedent to follow rather than leaving two adjacent questions both open | accepted |
| 692 | 2026-08-30 | `skills/setup/SKILL.md` step 4 contradicts its own **Done when**, and this effort repairs it. Step 4 states that one file carries both keys from here on; **Done when** requires exactly one `capstan-document-home` line and at most one `capstan-tracker` line across the two files combined, which is a per-key rule that permits a split. The contradiction predates this effort and surfaced only because a public template is the first case that wants the two keys in different files | accepted |
| 691 | 2026-08-30 | The board is a new public Projects v2 board named `Capstan`, not the existing untitled project 1. That project holds one unrelated draft item which would sit in the tracker permanently, and an untitled board makes its own purpose unreadable to the people this surface exists for. Public rather than private because `TRACKER-GITHUB.md`'s stated reason for the surface is visibility for people without a clone, and a private board on a public repository discards exactly that | accepted |
| 690 | 2026-08-30 | This effort does both halves: it builds the migration `setup` does not have, which 660 deferred into its own effort, and then runs it on this repository. Doing only the second leaves `skills/setup/SKILL.md` line 67 and `README.md` line 158 telling every reader that migration is not built while this repository quietly has it, which is the template documenting a state it does not hold. 653 already settled what migration does, so only building it and running it were open | accepted |
| 689 | 2026-08-29 | The note is verified by the Architect after one fix dispatch and the effort closes. Nine prescribed moves, no new scope, checkable by reading, and all three blocking claims re-checked against the log rather than against the report. The fix Courier flagged one citation it had not independently confirmed, 531's content behind 686's cross-reference; I checked it and 531 does turn on a value being derivable, so the citation holds. Flagging an unverified citation rather than asserting it is the behaviour 596 and 630 wanted and the first time a Courier has volunteered it. Note fix-dispatch count reads one, four, one, one | accepted |
| 688 | 2026-08-29 | Nine note findings actioned, none dismissed, as one fix dispatch; note count goes to one. Three block. The note says `capstan-knowledge-base` staying out of `setup` was "reaffirmed rather than reopened", and 685 reversed exactly that inside the range the note cites as complete, so a reader six months out would learn the wrong thing about a decision that has already flipped. It credits both out-of-diff catches to "the same reviewer" when 663 came from the `effort-read` Reviewer and 671 from the `tracker-mapping` one, which understates the discipline: two independent reviewers each reaching past their own diff is the stronger claim and the note recorded the weaker false one. And it drops `## Still open`, which 17 of the folder's 20 dated notes carry, leaving 686 unrecorded. The frontmatter and every verification claim came back clean, which is the two things the dispatch aimed the Reviewer at | accepted |
| 687 | 2026-08-29 | Version 2.19.0, a minor bump, per 7 and 386. This effort adds a third configuration key and a second Tracker surface, which changes what `setup` asks and what the Architect writes on every slice transition, so it is not a patch. 386 forces the bump rather than folding into 2.18.3, which is published and installed. The user-visible change is that `setup` now asks two questions where it asked one, and every existing project is untouched because unset still means `tracker.md` | accepted |
| 686 | 2026-08-29 | The knowledge base is per-operator configuration living in a per-project system, and nothing addresses that. `capstan-document-home` and `capstan-tracker` genuinely differ per repository; a vault does not, so 685's ask means typing the same absolute path into every project the operator ever sets up, and README's own rule forbids reading a user-level `CLAUDE.md` for these keys. Raised while settling 634 rather than folded into it, because it is a different question and folding it would have let 685 look cheaper than it is. Adjacent to 531, which asks a version of the same thing about a value that is derivable | open |
| 685 | 2026-08-29 | Settles 634. `setup` asks for `capstan-knowledge-base` and writes it, using `## The tracker-surface ask` as the template: a self-contained ask reporting its own current value, whose default branch writes nothing and removes any stale line. 654 declined this on 207, and 207 still forbids conflating features, but this effort built the non-conflating shape and 654's premise is gone. So is 634's own reasoning, which held that 352's ambiguity does not reach a key whose unset state is unambiguous; `capstan-tracker`'s unset state is equally unambiguous and `setup` asks about it anyway. 595 is the evidence: nothing declared the knowledge base, so a Courier would have reported none and skipped, breaking a run of nine notes, and it degraded into a skip that reads like a correct outcome. Built in one follow-up effort with the migration 660 deferred, since both touch `skills/setup/SKILL.md` and the one-file-one-slice rule makes them one slice regardless | accepted |
| 684 | 2026-08-29 | V2 fixed and merged; `effort-read` closes at three dispatches. 681's reshape is vindicated by count: the sweep found four surface-dependent sites where the dispatch named one, so the three previous diff-graded rounds would each have found one more and a fourth round was inevitable under the old method. The three the point fix would have missed are the document home's own artifact list, a "none of the four durable artifacts" count that is three under a board, and "one markdown table" conflated with the four columns that are true on both surfaces. The Builder also reported every site it examined and left alone with its reason, which is the evidence the sweep was exhaustive and is what makes a fourth round unnecessary rather than merely unscheduled | accepted |
| 683 | 2026-08-29 | V1 fixed and merged; `setup-surface` closes at two dispatches. The artifact set is now defined once, at the top of step 2, as the three that always exist plus `tracker.md` when the tracker-surface ask settled on the default, and every branch below reads from that one definition instead of naming a count. Verified by walking both surfaces: a GitHub project holding its three reaches Proceed, which was unreachable before; a project holding some but not all still routes to Stop, so the partial-merge protection is untouched and only what counts as complete moved. The Builder also generalised two stray counts it was not sent after, in step 1 and Re-running, which would have contradicted the new set | accepted |
| 682 | 2026-08-29 | Two verify findings actioned, none dismissed, as two fix dispatches: `setup-surface` to 2 and `effort-read` to 3. V1 is the one no review could have caught: `setup`'s step 2 branches on a destination holding all four durable artifacts, and a GitHub-tracker project has three, so "all four" is unreachable and every populated destination reads as a partial collision and routes to Stop. Relocating a document home becomes impossible for exactly the projects this effort exists to serve. Both sites' wording is byte-identical to baseline `52a8ecc`, so nothing here is red at head in the sense `verify` means: the text predates the effort and became wrong because of it, which is neither the baseline's fault nor a Builder's | accepted |
| 681 | 2026-08-29 | The third-dispatch question fires on `effort-read` and the answer is to keep patching, with the dispatch reshaped. The slice's design is not wrong: reading the key and acting on it is exactly one vertical slice. What is wrong is how each round was dispatched. All three rounds graded a diff, and all three found a different pre-existing unconditional `tracker.md` claim elsewhere in the same file, which is a method that finds one defect per pass rather than the class. So the dispatch changes from a point fix to an exhaustive sweep of the whole file under both branches, with the bar that a fourth round finds nothing. Recorded because the rule says to record it every time it fires, whether or not the answer moved | accepted |
| 680 | 2026-08-29 | `docs` verified by the Architect after one fix dispatch and merges, closing the slice graph. Nine prescribed moves, no new scope, two owned files. The blocking repair was checked at both sites and the capability framing replaced the behaviour claim in each. The Builder also declined to touch a pre-existing README line, that `setup` writes the answer down "so nothing asks again", noting `setup`'s own Re-running section re-asks every time. It was right to leave it: outside the nine findings and predating this effort. Recorded so it is not lost, since it is the same claim-contradicts-source shape 637, 639 and 679 all repaired | accepted |
| 679 | 2026-08-29 | Nine `docs` findings actioned, none dismissed, as one fix dispatch. Count goes to one. Blocking: `DESIGN.md` promises "an issue that closes itself when the pull request closing it merges", and merged `TRACKER-GITHUB.md` says the surface never relies on a pull request or on GitHub's automation, closing the issue itself in both terminal cases. Capstan opens no pull request anywhere; I checked, and the ten `gh pr` hits the Reviewer reported as zero are all `gh project` substrings, so its grep was loose and its finding was right anyway. The defect is a capability converted into a behaviour, and it is the third time this week this exact file has carried a claim its own source contradicts, after 637 and 639 | accepted |
| 678 | 2026-08-29 | The `docs` seam was stated wrong by the Architect and the Builder was right to depart from it. The dispatch set red-at-base as `grep -c capstan-tracker README.md DESIGN.md` returning zero on both, implying both should end non-zero. `DESIGN.md` cites no literal key name anywhere, including in the two sections it already carries for `capstan-document-home` and `capstan-knowledge-base`, verified by grepping for both and getting zero: it argues reasoning and leaves syntax to the README. A seam that would have forced the literal string in for the count alone. The Builder flagged the departure rather than satisfying the grep, which is the right handling of a defective criterion and worth recording as the second seam this effort has had to correct after 662 | accepted |
| 677 | 2026-08-29 | `tracker-mapping` is verified by executing it rather than by reading it, and merges. The fix added a whole write path with commands, which is new content rather than a prescribed wording, so 592's read-it-yourself route did not reach it and a second review would only have read it too. The scratch board from the spike was still live, so the documented path ran literally end to end on a third slice: field-list, milestone, issue create, item-add, three status writes, comment, close, and the documented read-back query. Every step worked as written. The read-back returned `merged` and `dropped` on separate slices, which is 656's whole claim observed a second time and on a closed issue. Executing beats reviewing wherever the artifact is a procedure and the environment still exists | accepted |
| 676 | 2026-08-29 | `phase-files` verified by the Architect after one fix dispatch and merges. Six prescribed moves, no new scope, three owned files, checkable by reading and by counting. The duplication cut is the measurable part: `tracker.md` mentions go from 2, 3 and 1 to 1, 1 and 1, one gloss per file with the single `## Tracker` pointer intact, which is what 652's future third scheme will cost one line to update instead of five. The blocking clause became "and no merge commit recorded yet", true of an empty Commit cell and of a board with no comment yet | accepted |
| 675 | 2026-08-29 | `effort-read` fix 2 verified and merged, one line, and its row's commit column moves to the second merge because that is the commit that merged the slice's final state. The Builder generalised rather than enumerated: the `except` clause now turns on a source of truth being missing or self-contradictory rather than merely unclear, which is what every stop case already reasons from. It also found more stop cases than 671 counted. My four came from grepping `stops the phase` line by line, and at least one of those lines carries two distinct cases, so the real number is higher and an enumerated clause would have been wrong again on the next one. The generalisation is the better answer for the reason my own count was wrong | accepted |
| 674 | 2026-08-29 | `Row` gains a glossary row: one slice's entry in the Tracker, whatever surface holds it. `CONTEXT.md` had `Tracker` and, since 665, `Tracker surface`, and no word for the entry itself, which under `tracker.md` is a table row and under a board is an issue plus its status value plus, at merge, a comment. The `phase-files` diff leans on the word at five sites and inherited rather than coined it, so this settles a gap rather than grading a defect. 665's shape exactly, one term later | accepted |
| 673 | 2026-08-29 | Six `phase-files` findings actioned, none dismissed, as one fix dispatch. Count goes to one. Blocking: `PHASE-2-PLAN.md` still says to open a row `with the commit column blank`, and 656 maps the merge commit to a comment, so under a board there is no such column and an Architect satisfying the instruction literally would create a field `spec.md` explicitly forbids. The Standards worst is the same lesson 662 taught one level out: the surface-selection rule now sits at five sites whose home is `SKILL.md`, and 652 already names the day they go stale, when a third scheme makes every copy read "when it names one" and mean the wrong thing | accepted |
| 672 | 2026-08-29 | `GitHub primitive` gets no glossary row, declined rather than left unsaid. `CONTEXT.md` describes Capstan's own vocabulary, and this names GitHub's objects rather than anything Capstan decides about. 134's original reasoning, which 635 overturned only because the location had become a named key Capstan owns; nothing here becomes Capstan's. Revisit if a second Tracker surface lands and the repository needs one word for what both map onto | accepted |
| 671 | 2026-08-29 | `effort-read` reopens for fix dispatch 2 though it is merged, which the `Live slice` glossary row provides for: a merged slice stays live while the effort is open and its count resumes rather than restarting. `SKILL.md`'s `Stop for consequence, never for ambiguity` carries an `except` clause naming the document home's stop case, and there are now four stop cases in that file: the document home, the `capstan-tracker` disagreement, the unrecognised scheme, and the unreachable GitHub surface. Verified by counting them. Found by the `tracker-mapping` Reviewer outside its own diff, the second time a review has caught the plan rather than a Builder | accepted |
| 670 | 2026-08-29 | Seven `tracker-mapping` findings actioned, none dismissed, as one fix dispatch. Count goes to one. The blocking one is that the file never names `gh project item-add`, so an agent following it creates an issue, creates the field, and then fails at the first status write because an issue carrying a milestone is not yet a project item. Confirmed against this effort's own spike script, which calls `item-add` twice and would not have worked without it: the mapping was written from the spike's conclusions and dropped a step the spike itself had to take. The read-back claim has the same shape, true of the `--format json` call the spike ran and false of the bare `item-list` the sentence describes | accepted |
| 669 | 2026-08-29 | `setup-surface` is verified by the Architect after one fix dispatch and merges, same call as 667 and on 592's distinction: five prescribed moves, no new scope, one file, checkable by reading. Each was confirmed against the file rather than against the Builder's account, and one confirmation failed on my own wrong grep string before the wording turned out to be there, which is the second time today a search miss nearly became a false finding. The fix-dispatch count for both frontier slices closes at one | accepted |
| 668 | 2026-08-29 | The custom single-select is named `Capstan Status`, a fixed literal Capstan owns, not a value `setup` asks for or stores. Same reasoning as the `capstan_` frontmatter prefix: a name nobody configures is a name nothing has to keep in sync, and a fourth key to hold it would earn nothing. Found by the `tracker-mapping` Builder, which wrote `however the setup step chose` and flagged that its own slice could not resolve it; `setup-surface` never names the field either, so nothing in the effort did. The mapping file carries the literal, and it also carries the spike's gotcha: `gh project item-list` renders the field as `capstan Status` in its JSON, lowercasing the first word only, which cost the Architect a wrong read during the spike | accepted |
| 667 | 2026-08-29 | `effort-read` is verified by the Architect after one fix dispatch rather than sent for a second review, and merges. 592's distinction applies cleanly: all four fixes were the Reviewer's own prescribed moves, none added scope, and the whole three-hunk diff is checkable by reading, which I did against both the set and unset branch. The escalation at 661 held up in the result: the wholesale rules import is gone rather than reworded, so 240's absolute-path stop can no longer fire on a valid `github:` value. Same call as 345, 409, 480, 515, 526, 556, 566 and 592 | accepted |
| 666 | 2026-08-29 | Settles 663. A fifth slice, `phase-files`, owns `PHASE-2-PLAN.md`, `PHASE-3-BUILD.md` and `PHASE-4-DELIVER.md`, blocked by `effort-read` and blocking `docs`. The operator's call at gate 2, because adding a slice changes the shape they locked. Chosen over widening `effort-read`, which is already on fix dispatch 1 and would have doubled while it was being repaired, and over deferring like 660, which would have shipped a release whose phase files tell the Architect to write a file the same release says does not exist | accepted |
| 665 | 2026-08-29 | `Tracker surface` gains a glossary row. `CONTEXT.md` defines `Tracker` as the artifact and had no word for the configured choice between surfaces, which `spec.md` and now `skills/setup/SKILL.md` both use as a proper noun. Returned by the `setup-surface` Reviewer as a vocabulary gap rather than a finding, since the diff did not coin it. 629 and 589's shape, a term earning a row once the repository decides on it repeatedly | accepted |
| 664 | 2026-08-29 | Five `setup-surface` findings actioned, none dismissed, as one fix dispatch. Count goes to one. Nothing blocked. The two questions the dispatch flagged for weighing both came back derived rather than invented, and the Reviewer's endorsement is the record for them: owner and repository come from 659, and telling a non-GitHub working copy why it cannot proceed closes the gap 659 names as its own cost. Worst on each axis: the scope probe gives `gh project list` two outcomes when it has three, so a network failure or an absent `gh` routes the operator into a scope grant that cannot fix it, which is not the misdiagnosis 658 priced; and a conditional commit subject nobody asked for, whose first branch is always true because 352 makes step 4 write the document home unconditionally, leaving the second dead | accepted |
| 663 | 2026-08-29 | The plan's own invariant is unmet and closing it needs a fifth slice, so it goes to the operator rather than being added unattended. `PHASE-2-PLAN.md`, `PHASE-3-BUILD.md` and `PHASE-4-DELIVER.md` instruct the Architect to open and move rows in `tracker.md` at six sites, and no slice owns any of those files, so a project configured to GitHub reaches phase 2 and is told to write a file 644 says does not exist. Found by the `effort-read` Reviewer as a gap outside its own diff, which is the review catching the Architect rather than the Builder. Verified independently before recording | superseded by 666 |
| 662 | 2026-08-29 | `plan.md` is amended rather than the Builder charged with widening scope: the visibility gate moves from slice 2's Demonstrated to slice 1's. 645 puts the rule in the Authority table and that table lives in `skills/effort/SKILL.md`, which only slice 1 owns, so the cut asked slice 2 to demonstrate something it could never touch. 236's shape, scope corrected rather than the Builder blamed. Slice 2 is now directed to point at the Authority table rather than restate it, which also settles the duplication the same review found | accepted |
| 661 | 2026-08-29 | Four `effort-read` findings actioned, none dismissed, as one fix dispatch. Fix-dispatch count for that slice goes to one. The blocking one is three statements in `skills/effort/SKILL.md` still describing `tracker.md` unconditionally, which 644 contradicts and which no other slice can repair under the one-file-one-slice rule. The Reviewer graded the rules-import finding non-blocking and I am escalating it: "the same rules as `capstan-document-home`" imports 240's absolute-path stop, and the value on the next line is `github:owner/repo#N`, so an agent following it stops the phase on a correctly configured tracker, which defeats requirement 1 outright rather than merely reading badly | accepted |
| 660 | 2026-08-29 | Migrating an existing `tracker.md` into the GitHub surface leaves this effort and becomes its own. 653 stays accepted and gains a not-yet: the decision about what migration does is unchanged, only when it is built. Forced by the plan rather than chosen: everything touching `skills/setup/SKILL.md` is one slice under the one-file-one-slice rule, so the fork, the scope check, the walkthrough handoff and the migration could not be split, and migration is the only one of the four carrying a destructive step and the only one no new project ever exercises. `spec.md` moves its requirement 8 to out of scope, per 236's shape of correcting scope rather than blaming a widened build | accepted |
| 659 | 2026-08-29 | The working copy has a GitHub remote, and the board belongs to that remote's owner. Defaulted rather than settled: a repository with no remote, or one hosted anywhere else, cannot use this surface at all and nothing in the design tells such a user why. Reopen the moment somebody asks for a board that is not the working copy's own remote, which is a reasonable thing to want and is not built | assumed |
| 658 | 2026-08-29 | `gh` is installed and authenticated on any machine running an effort whose tracker is on GitHub. Defaulted because every path in this design calls it and no path checks for it, which is a dependency Capstan has never had before: the crew has needed `git` and nothing else. Reopen on the first report of the surface failing where `gh` is absent, which 648 will surface as an unreachable-GitHub stop rather than as a missing dependency, and that misdiagnosis is the cost being accepted here | assumed |
| 657 | 2026-08-29 | Settles 650. The read-back half is measured rather than feared: Effort, Slice and Status all come back in one `gh project item-list` call and the tracker table reconstructs exactly, but the merge commit does not, because it lives in a comment and costs one further call per slice. So reading the tracker is 1+N calls against `tracker.md`'s one file read. The fixed-point half of 650 is confirmed rather than solved: the board still carries no history an agent can diff, and a resumed phase 3 cannot see what moved underneath it. Accepted with that cost named. Evidence on `spike/github-tracker-mapping` | accepted |
| 656 | 2026-08-29 | Supersedes 647 on its third column only. Status is a **custom** single-select that Capstan creates and names, never the built-in `Status` field. The spike closed two issues and GitHub's own Projects workflow set the built-in field to `Done` on both, overwriting what Capstan had written and collapsing `merged` and `dropped` into one value, which is precisely the case that makes a tracker unable to answer what shipped. The custom field carried all four statuses and survived closure untouched. `gh` has no `project field-edit`, so the built-in field cannot be repurposed from the CLI either. Slice to issue, Effort to milestone and commit to comment are unchanged from 647. Evidence on `spike/github-tracker-mapping` | accepted |
| 655 | 2026-08-29 | A spike settles 647's mapping before `spec.md` is written, 216's shape and operator-observed. Nothing in the mapping has been exercised: whether a Projects v2 single-select carries the four statuses usefully, whether an issue closed by a pull request leaves that field stale, and whether the board reads back well enough for a resumed phase 3 are all questions no amount of reading settles. It runs against a private scratch repository rather than this one, because 645 makes every write here third-party-visible. The word `validate` in the operator's request carried this and the Architect did not ask until round 4 | accepted |
| 654 | 2026-08-29 | 634 stays open and out of this effort's scope. `setup` gaining a tracker-surface question is not a reason to also make it ask about the knowledge base: 207 is explicit that conflating two features forces someone who wants one into configuring the other, and it is the decision this whole surface split rests on. Reported at every gate rather than quietly carried | accepted |
| 653 | 2026-08-29 | Switching an existing repository to the GitHub tracker migrates its `tracker.md` rows, on the operator's approval, creating one issue per row and then deleting the file. `setup` already has this shape for the four durable artifacts, and 389 is the reasoning: a tracker that omits what shipped cannot answer what shipped. Starting fresh and leaving the file behind is the two-records-that-disagree state this project refuses everywhere else | accepted |
| 652 | 2026-08-29 | The key is `capstan-tracker`, read from the same two files as the other two keys, and its value is scheme-prefixed: `github:<owner>/<repo>#<project-number>`. The prefix makes the default and a remote coordinate distinguishable on sight rather than by parsing, and a third surface later adds a scheme rather than a fourth key. Unset means `tracker.md` in the document home, which is what every existing project has and what nothing has to change to keep | accepted |
| 651 | 2026-08-29 | Supersedes 321. Its reasoning, that two settings means two things to keep in sync, was written about two roots holding the same file and does not reach one artifact living wholly elsewhere: under 644 the tracker is on GitHub and absent from the document home, so there is no second copy. A clean supersession on the merits rather than an exception carved into a rule that still holds for what it was about. 207, which 321 restated, is untouched and still governs | accepted |
| 650 | 2026-08-29 | The GitHub tracker has no fixed point, and this is the cost 643 buys. `tracker.md` at a commit is always the same table; a Projects board is mutable remote state with no history an agent can diff, so two runs cannot be told apart by reading it and a resumed phase 3 cannot see what changed underneath it. Exactly the degradation 275 created for the note, which 124 and 263 accepted knowingly, except the note is written once and the tracker is written per slice transition. Raised by the Architect rather than asked for, and recorded before the spec so it reaches gate 1 rather than a later review | superseded by 657 |
| 649 | 2026-08-29 | `setup` asks the tracker-surface question, checks the `project` scope when the answer is GitHub, and hands to a `walkthrough` stage that prints `gh auth refresh -s project`, stops, and confirms the grant landed before the key is written. The operator named setup and walkthrough together in the request and this is the shape that fits: an auth change is their hands, and a grant nothing verifies fails first in the middle of an effort | accepted |
| 648 | 2026-08-29 | GitHub unreachable stops the phase and says so, the same rule 240 gives an unreachable document home. A missing source of truth is not ambiguity. No retry: a bounded retry is machinery, and `SKILL.md`'s closing line refuses it. The consequence accepted here is that a rate limit or an expired token ends a run rather than slowing one | accepted |
| 647 | 2026-08-29 | The tracker's four columns map onto four GitHub primitives, each used for the job it was built for. Slice becomes an issue, Effort becomes a milestone grouping them, Status becomes a Projects v2 single-select carrying the four existing statuses, and the merge commit is posted as a comment. Issues rather than bare Project items, because an item that is not an issue cannot be closed by a pull request or linked from a commit, which is most of the reason to be on GitHub at all | superseded by 656 |
| 646 | 2026-08-29 | Projects v2 is in scope, against the recommendation to defer it. The operator grants `project` on their own token, so the board they want exists rather than being designed around. The cost is accepted rather than unnoticed: `gh project list` fails closed on an ungranted machine at the first read, so the surface ships with a setup step every user repeats and a failure mode invisible until first use. The grant is an auth change and the operator's own action, never the crew's | accepted |
| 645 | 2026-08-29 | Writes to the GitHub surface gate on repository visibility. A private repository is the operator's own and nobody else sees it, so writes are ordinary unattended crew work. A public one is genuinely third-party-visible, so the operator confirms. This keeps the Authority row honest instead of carving an exception into it, and it is the first rule in Capstan whose branch is a property of the remote rather than of the working copy | accepted |
| 644 | 2026-08-29 | Configured to GitHub means GitHub holds the state and the local file does not exist, exactly as a configured document home works. Not a mirror: 98 rejected two copies outright, because a sync problem needs the operating layer this project refuses to build, and an issue anyone can edit by hand makes drift certain rather than possible | accepted |
| 643 | 2026-08-29 | The GitHub surface carries the tracker alone. The glossary, the decision log and the records stay markdown in the document home. 323's shape, one surface at a time, and it avoids the objection `DESIGN.md` already records at 223: the log's whole value is scanning every decision in one pass, and Issues is one-note-per-item by construction, which is the bloat `decision-record` exists to prevent. The tracker has no such objection, being one row per slice already | accepted |
| 642 | 2026-08-29 | Version 2.18.3, a patch, per 386 and 638. Third patch today on the same rule, and the rule keeps earning it: 2.18.2 is published, so folding 641 into it would strand anyone who took it | accepted |
| 641 | 2026-08-29 | `DESIGN.md` gains `## Why the knowledge base sits outside the document home`, the gap 639 found and left. It carries the reasoning already settled elsewhere and nowhere explained: why the knowledge base is its own key rather than a corner of the document home, why unset is a complete answer where the document home's unset is ambiguous, 275's flat never-run-git rule beating the vault-detection step it replaced, the degraded review 124 and 263 accepted as its price, and the copy-versus-derive rule 315 and 143 paid for. A draft claimed the prose route had stood for months; the first commit is nine days old, so the duration went rather than being corrected, since nothing in the argument needed it | accepted |
| 640 | 2026-08-29 | Version 2.18.2, a patch, per 386 and on the same reasoning as 638. 2.18.1 is already published, so folding 639 into it would leave anyone who took it unable to reach the repair. Cheaper to spend a version number than to ship an un-updatable one | accepted |
| 639 | 2026-08-29 | Three factual repairs to `DESIGN.md`, audited the way 637 audited the README. It gave `codebase-design` to the Reviewer and the Builder, which contradicts 151 and record 0003, both of which preload it on the Reviewer alone, and `builder.md` has never referenced it. It named `effort` as the front door, singular, missing `setup` entirely and placing it in a table it is not in; `CONTEXT.md` has said two since `home-setup`. And its stop-the-line list omitted 631's scratch carve-out, the same gap 637 closed in the README. What held: the 204-line count is exact, the three preloading agents are right, no agent uses `isolation: worktree`, every link resolves, and `spike` on the Builder and `to-questionnaire` on the Architect both check out against `spike/SKILL.md` and 157 | accepted |
| 638 | 2026-08-29 | Version 2.18.1, a patch. 386 forces a bump rather than folding 637 into 2.18.0: `plugin update` compares version numbers rather than content, and 2.18.0 is already installed, so folding would leave it un-updatable. Patch rather than minor because 637 changes what the README claims and nothing an agent does | accepted |
| 637 | 2026-08-29 | Five factual repairs to the README, found by checking each claim against the file it describes rather than reading it for plausibility. The note review was documented as never skipped, which `PHASE-4-DELIVER.md` step 3 contradicts and 632 turned into an ordinary reachable state. The effort's document-home ask was called the same question `setup` asks, when it is narrower and cannot take a vault path. `setup`'s both-files-exist branch was missing. The scratch delete 631 carved out was absent from the four-things-stop-the-run list, so the README promised a halt on the one delete the crew makes unattended. And the upgrade section called `update` an install, next to the observation, made by running it, that `install` on an installed plugin reports success and leaves the old version running | accepted |
| 636 | 2026-08-29 | Version 2.18.0, per 7. A minor bump rather than a patch: 632 changes what the Courier reads and what an unconfigured project gets. The old prose carried two branches that contradicted each other, ask where to put the note and skip the note, which is the tension 595 found the hard way; the key leaves one. None of it is live until the marketplace updates and the session restarts, which is what 135 stands as the reminder of | accepted |
| 635 | 2026-08-29 | Supersedes 134, which declined `Knowledge base` a glossary row as ordinary English naming a setup-specific location. 632 takes the ground out from under that: the location is a named key now rather than prose, the key's own row has to say what it points at, and the same pairing that gave `capstan-document-home` a row beside `Document home` applies unchanged. 629's shape, a term earning a row once the repository has decided on it repeatedly | accepted |
| 634 | 2026-08-29 | Whether `setup` asks for `capstan-knowledge-base` and writes it, the way it already does for `capstan-document-home`. 352 forces the document home to be written because unset and asked-chose-default are otherwise the same state on disk; the knowledge base has no default, so unset is a complete answer and 352's reasoning does not reach it. Left to the operator's own hand and documented in the README, which is a weaker guarantee than a skill that asks | superseded by 685 |
| 633 | 2026-08-29 | Supersedes 594. This repository declares its knowledge base in a gitignored `AGENTS.md` rather than the committed `CLAUDE.md`, because the value is a path in the operator's own filesystem and this repository is public, which is what 382 spent fourteen row-repairs keeping out. The Courier reads a file by path and does not care whether git tracks it, so the location is readable rather than inferred and nothing is published. 594's own row is separately repaired in place per 348 and 382, dropping the literal path it named. 531 is the same question one key over, and stays open | accepted |
| 632 | 2026-08-29 | Settles 365. The knowledge-base location becomes the literal key `capstan-knowledge-base`, read from `<working copy>/CLAUDE.md` and `<working copy>/AGENTS.md` exactly as `capstan-document-home` is, carrying the two-files-disagree stop 233 gave that key. 595 is the reason it resolves this way: the prose route degraded into a skip indistinguishable from a project that genuinely has no knowledge base. Location only. The frontmatter schema stays prose, because matching neighbouring notes is a judgement no path can carry and `courier.md`'s copy-versus-derive rule already governs it | accepted |
| 631 | 2026-08-29 | The scratch is deleted outright, the first exercise of the carve-out this effort shipped. Yesterday the same delete stopped a delivery mid-teardown and was resolved by moving the scratch to the operator's Trash; today `SKILL.md`'s Authority table names it as the one delete the crew may make, so step 5 runs as written. The rule changed and the behaviour changed with it in the same day, which is the shortest loop between a defect being recorded and being closed that this log holds | accepted |
| 630 | 2026-08-29 | The note is verified by the Architect after one fix dispatch, against the four its predecessor took, and the difference is the strongest evidence yet for 601 and 623. Every one of the eight fixes was prescribed as a wording or a location rather than as a claim, and all eight landed correctly on the first attempt. The predecessor's four dispatches were driven by claims prescribed instead of wordings and by one wording prescribed at the wrong line. Same call as 566 and 602. The note fix-dispatch count now reads one, four, one | accepted |
| 629 | 2026-08-29 | `Duplication` gains a glossary row: one rule written in two places, which drift apart because nothing keeps them in step. `writing-for-agents` names it and distinguishes it from co-location, the within-file case, but `CONTEXT.md` held neither, and the absence is what let the note reach for the wrong label. Unlike 621's `wording`, this is a term the repository decides on repeatedly: 612, 536 and 435 all turn on it and 603 is an instance of it. 589 and 550's shape, a term earning a row on being decided on rather than on being coined | accepted |
| 628 | 2026-08-29 | Factual repair to 622 in place, per the log's repair rule, so the number and status stay. Its closing clause said an Architect can name the version in a Courier dispatch "as this effort's predecessor did by hand". No predecessor did: the `standards-bullets` note names a merge sha and no version, which is the observation 609 was built on, and the 2.16.0 bump was made by hand after delivery rather than named in any dispatch. The clause is deleted rather than reworded, since the claim it made has no true version. Found by the note review | accepted |
| 627 | 2026-08-29 | Factual repair to 625 in place, per the log's repair rule. It announced seven checks and enumerated six, omitting the scope check, and said all seven were red at base when four were. Three could not have been: the no-manifest check was trivially true when the file mentioned no version at all, the Authority-row check was a state check rather than a red-at-base one, and the scope check has no baseline. Same arithmetic class as 596 and as 597's finding on the previous note, in a row the Architect wrote and the note then copied faithfully. Third time in two efforts the log rather than the note was the source of a wrong number | accepted |
| 626 | 2026-08-29 | Step 1 of the new phase 4 fires for the first time, on the effort that wrote it. Capstan declares a version in `.claude-plugin/plugin.json`, this effort changes two skills, and 571's test makes that a minor, so 2.16.0 becomes 2.17.0. Committed and not pushed, per 610. Worth recording what the step felt like from inside, since 615 built it to catch the silent case: the question was unmissable where four consecutive efforts had missed it, and the reason is that a numbered step is read in order while a rule in a neighbouring file is read when someone remembers it | accepted |
| 625 | 2026-08-29 | The integration is verified on `verify`'s degraded branch, with the search stated at thirteen locations per 596's correction rather than the ten 593 originally claimed. Seven checks ran against `f6cb78b`, four of them red at base: the list runs 1 to 6 where it ran to 5, the version condition exists where nothing mentioned a version, `note's fix-dispatch count` appears twice in `SKILL.md` where it appeared none, and the claim write site exists where it did not. Three were not red at base and could not be: no manifest is named, which was trivially true when the file mentioned no version at all; `gitignored scratch` appears once per Authority row; and the scope check has no baseline. Nothing red | accepted |
| 624 | 2026-08-29 | Both fix dispatches on `deliver-steps` are verified by the Architect rather than sent for a second review, and the slice stops at two. Every fix in both rounds came from the round 1 review; none was new scope, which is the distinction 551 turned on. I read the whole diff against both files rather than the changed lines alone. Worth naming that the rework question this slice adds would not have fired here: two dispatches, and it triggers at three. The count across recent slices now reads three, two, one, one, two | accepted |
| 623 | 2026-08-29 | Fix dispatch 2 on `deliver-steps`, for one word, and the reason is worth more than the word. The reviewer flagged "where" doing conditional duty in the `Done when` line; I wrote the dispatch against step 1, which happened to open the same way, so the edit landed somewhere real and the flagged line stayed as it was. Fourth instance in two efforts of the pattern 601 named, and the first where the defect is the target rather than the wording: 600 and 601 were claims prescribed instead of wordings, this is a wording prescribed at the wrong line. The phase 4 question this very slice adds is aimed at exactly this, which is the strongest argument yet that it belongs in the file | accepted |
| 622 | 2026-08-29 | 609's stated reason for placing the version step first, so the note can name the version the work shipped in, is not achieved by this slice and is recorded rather than fixed. The step makes the version available before the Courier runs; nothing consumes it, because `agents/courier.md` names no version and is outside this slice's Owns. Widening to a third file would recut the shape the operator locked at gate 2, which `PHASE-3-BUILD.md` says ends the run. The ordering is still right and the Architect can name the version in the dispatch | open |
| 621 | 2026-08-29 | The `wording` collision is dismissed rather than glossed. `PHASE-4-DELIVER.md` uses "the wording it produced" for the note's prose, the thing not under suspicion, while 601 and 611 use "wording" for what an Architect should prescribe, the thing that works. The reviewer found the shipped sentence unambiguous in place, since "it" is the instruction. `wording` is ordinary English rather than project vocabulary, and a glossary row for it would formalise a word the repository does not otherwise lean on. 550 and 589 added rows for terms the project coined; this is not one | accepted |
| 620 | 2026-08-29 | Step 1 gains "per the repository's own convention", and the defect is in the spec rather than the build. 608 has two halves: the increment rule does not ship, and the step says to bump per the repository's own convention. Requirement 3 carried only the negative half, so the step as built tells an Architect to write a new value and gives no source for it, which is an unbound choice on any repository without a stated convention. Second thing this effort's spec dropped from a decision it cited, after 619. Also in the dispatch: `Where the repository declares one` becomes `if`, since "where" reads as a location immediately beside a sentence about recording | accepted |
| 619 | 2026-08-29 | Requirement 2 landed exactly as specified and does not achieve what 614 built it for. `SKILL.md` now declares that `next` carries the note's fix-dispatch count, and nothing directs a phase 4 Architect to write it: `PHASE-4-DELIVER.md` never mentions `CLAIM.md`, `SKILL.md` says to update `next` at every gate and phase 4 ends at no gate, and step 5 deletes the claim. The count is declared to live somewhere the phase that produces it is never told to write. The Architect's defect: the requirement was wrong, not the build. Phase 4 gains a claim-update instruction matching `PHASE-3-BUILD.md`'s line 39, inside the file the slice already owns, so this is not a recut | accepted |
| 618 | 2026-08-29 | The `CLAIM.md` template placeholder at `SKILL.md:130` still enumerates only slice counts while the prose six lines below now names the note's count too. The template is where the instruction binds, because it is what an agent copies and fills in. This effort reproduced the exact defect class it was chartered to remove: two lines in one skill saying different things about the same thing, six lines apart. Requirement 2 said "one sentence" and the Builder was literal, so the miss traces to the requirement | accepted |
| 617 | 2026-08-29 | The Builder's return is citable per requirement, and for requirement 6 it quotes `PHASE-3-BUILD.md`'s rework paragraph beside its own. 585 earned this immediately last effort. Requirement 6's test is parallel construction between two paragraphs in two files, which a Reviewer cannot judge from one side, so the citation carries both or the seam is missing its second half | accepted |
| 616 | 2026-08-29 | One slice, and for a different reason than 583's. That effort was single-artifact work where nothing could fan out; this one edits two files sharing no line, so a two-slice cut with no blocking edge was genuinely available. Refused on size, roughly ten lines fitting one context window, which `slicing` says not to cut at all, and more strongly on coupling by purpose: requirement 2, the note's dispatch count in the claim, exists only to serve requirement 6, the question that reads it, and they sit in different files. Split, neither Builder sees the pair. Checked and cleared: nothing outside `PHASE-4-DELIVER.md` cites its step numbers and it holds no internal ones, so renumbering breaks nothing | accepted |
| 615 | 2026-08-29 | The bump step asks rather than commands. If the repository declares a version, the Architect decides whether this effort changes it and records the answer in `decisions.md` either way. A step reading "bump the version" would produce a bump every effort, which is the opposite failure to the one being fixed. And the real defect across the four misses was not that the bump was skipped but that nobody decided: no row anywhere says an effort shipped without one. Recording the negative is what makes the step catch both halves | accepted |
| 614 | 2026-08-29 | The note's fix-dispatch count is carried in the claim's `next` line, and `SKILL.md`'s claim section is widened by one sentence to say so. The reasoning is already written there for slices: a count held only in a context window means the question fires only when one Architect happens to make three dispatches in one sitting, and phase 4 has the same exposure. This effort's four dispatches would have been invisible to a second run. Scope is now two files and three regions, still one slice | accepted |
| 613 | 2026-08-29 | Phase 4's rework question is written as prose after the numbered list, matching `PHASE-3-BUILD.md`'s construction, because it applies across steps rather than at one. It follows phase 3's rule of being written to `decisions.md` every time it fires, whether or not the answer changed. 600 and 601 are the evidence: two firings, two rows, the second sharper than the first, and a record that only appears on a changed answer is identical to a round where nobody asked | accepted |
| 612 | 2026-08-29 | The carve-out is written in `SKILL.md`'s Authority table row alone, and nothing is added to `PHASE-4-DELIVER.md` step 4. 536 refused a second home for a rule eight days ago and 435 puts a rule where its artifact is defined. Step 4 already says what is deleted; the table is where who-may-delete belongs, and duplicating it there is how the two lines came to disagree in the first place | accepted |
| 611 | 2026-08-29 | Supersedes 492, which asked whether the rework check extends to phase 4's note fix loop. It does, at three dispatches, matching `PHASE-3-BUILD.md`'s count, but pointed at a different suspect. Phase 3 asks whether the slice's design is wrong; a note has no design to be wrong, and 601 found three of this effort's four dispatches traced to the Architect's own instructions rather than to the Courier's writing. So phase 4 asks whether the instruction is wrong: whether a claim is being prescribed, which invites reconstruction, instead of a wording, which does not | accepted |
| 610 | 2026-08-29 | The bump step commits and does not push. Bumping and committing are crew work under the Authority table, but pushing the default branch publishes to every installed user through the marketplace, which the table puts with the operator as something a third party will see. The step ends at the commit and the close-out hands the push over, so phase 4 ends with two things in the operator's hands, the note and the push | accepted |
| 609 | 2026-08-29 | Supersedes 568. The version bump becomes a numbered step in `PHASE-4-DELIVER.md`, placed before the Courier as the new step 1 so the note can name the version the work shipped in. Today's note names a merge sha and no version, and a version is what a reader six months out can match against what they have installed. Four consecutive efforts missed the bump under 7's rule that it ships inside the effort, which is a mechanism failing rather than four lapses | accepted |
| 608 | 2026-08-29 | The increment rule does not ship in `PHASE-4-DELIVER.md`. The step says to bump per the repository's own convention, and 571's test, minor for a skill or the glossary changing and patch for metadata and prose, stays in this log as Capstan's own convention. Shipping our semver taste to every user is the same overreach as naming their manifest file, per 607 | accepted |
| 607 | 2026-08-29 | The bump step is conditional on the repository declaring a version, and it names no manifest file. `PHASE-4-DELIVER.md` ships to every Capstan user; Capstan is a versioned plugin and most repositories it runs in are not. An unconditional step would fire on every effort everywhere and produce either a wrong edit or a stalled Architect, and naming `plugin.json` over `package.json`, `Cargo.toml` or a bare `VERSION` file would be Capstan legislating somebody else's convention | accepted |
| 606 | 2026-08-29 | 603 reconciles by carving the gitignored scratch out of `SKILL.md`'s Authority table as the one delete the crew may make, rather than by stripping the delete from `PHASE-4-DELIVER.md` step 4 or by moving the scratch aside. The scratch exists in order to be destroyed and its unrecoverability is the design rather than a risk, since a stale spec surviving is exactly what step 4 prevents. Move-aside is the worst of the three as a shipped rule: "somewhere else" is platform-specific and only relocates the stale artifact. Supersedes the resolution I improvised at 603 | accepted |
| 605 | 2026-08-29 | 492 joins 603 and 568 in this effort. Same shape as both: a `PHASE-4-DELIVER.md` gap found by running the phase rather than reading it, and today it produced two data points, 600 and 601, only because the Architect chose to ask a question no file tells phase 4 to ask. Three single-step edits to one file is still one slice under 583, and splitting 492 out would queue a second effort for the same file. Not the 535 pattern of adding on symmetry: this is the same defect class in the same artifact | accepted |
| 604 | 2026-08-29 | Version goes to 2.16.0 for `standards-bullets`. Minor rather than patch by the test 571 stated: a minor is for a skill or the glossary changing, and this changes both, three lines of `effort/SKILL.md` and one `CONTEXT.md` row. Bumped by hand again, outside the effort, which is the third time in one day and the fourth instance overall for 568. 7 says the bump ships inside the effort and it has now been missed by four consecutive efforts, so the evidence for making it a numbered step in `PHASE-4-DELIVER.md` is no longer thin | accepted |
| 603 | 2026-08-29 | Two lines in `skills/effort/` disagree about who deletes the scratch. `PHASE-4-DELIVER.md` step 4 orders the Architect to delete `<working copy>/.capstan/effort/` outright; `SKILL.md`'s Authority table at line 208 puts "Deletes... anything hard to reverse" with the operator, every time. The scratch is gitignored, so the spec, the plan and both review returns exist nowhere else and the delete is unrecoverable. Superseded 54 made the same observation about a different directory and was never reargued when 58 replaced it for other reasons. Resolved here by moving the scratch to the operator's Trash rather than deleting it: the working copy is clean, so the stale-artifact reason for step 4 is fully served, and nothing is destroyed. Whether the two lines are reconciled, and in which direction, is its own effort | superseded by 606 |
| 602 | 2026-08-29 | The note is verified by the Architect after four fix dispatches rather than sent for a second review. Every fix was the Reviewer's own prescribed move or, in the last two rounds, a wording and then a deletion I supplied verbatim, and all four are checkable by reading, which I did across the whole note rather than at the changed lines. The note review itself was clean on frontmatter, `unslop` and vocabulary, and its two blocking findings were both arithmetic against the log. Same call as 566, at four dispatches rather than one, and 600 and 601 record why the count went up | accepted |
| 601 | 2026-08-29 | Fix dispatch 4 on the note, and the rework question fires again, as it must every time from three on. Answer: keep patching, and the note's design is not what is wrong. Three of the four dispatches trace to the Architect's own instructions rather than to the Courier's writing. Dispatch 2 said "state that 584 closed 558" without noticing the paragraph above already said it, dispatch 3 then corrected the copy I had commissioned and left the original standing, and dispatch 4 deletes that original. The pattern is that prescribing a claim invites reconstruction while prescribing a wording does not, and prescribing an addition without reading what stands above it duplicates. 492 now has a data point in each direction: a note reaches one dispatch when fixes are prescribed wordings, and four when they are prescribed claims. That is a stronger signal than the falling count 566 read | accepted |
| 600 | 2026-08-29 | Fix dispatch 3 on the note, and the rework question fires. `PHASE-3-BUILD.md` asks it of a slice at the third dispatch; 492 asks whether it extends to the note fix loop and is still open, so I asked it anyway. The answer is to keep patching, and the diagnosis is that the defect is mine rather than the note's. Dispatch 2 told the Courier to "state that 584 closed 558", which invited it to reconstruct 584's reasoning, and it reconstructed it wrong: the note now claims the audit produced four clean verdicts when it produced two failures and two passes. 584 says a clean sweep would also have closed 558, not that one happened. Dispatch 3 supplies the sentence verbatim rather than the instruction. A second data point for 492 from the opposite direction to 566's: the note loop reaches three dispatches when the Architect prescribes a claim instead of a wording | accepted |
| 599 | 2026-08-29 | Fix dispatch 2 on the note, for two small things. One prescribed fix landed half: 589's glossary row was narrated but the paired instruction, to say that 584 closed 558, was not, and 558 is the question the whole effort answers. And the note closes on "rows 573 to 595" while its own opening says "rows 573 onward", a range the rows written after the note drafted it have already made stale. Both places become "rows 573 onward", because a closed range in a permanent note goes stale every time the effort that wrote it records one more row, which is a defect the closing rows of an effort will reproduce every time | accepted |
| 598 | 2026-08-29 | A data point for 561, and it cuts both ways. The note review ran a sentence-level pass against seventeen siblings and found no verbatim carry: every high-similarity closer is true here, so the 522 and 560 pattern did not recur on the fourth note running. But a mutated form did, a numeric claim drifting from its source rather than being copied intact, and the predecessor note stated the same fact correctly a day earlier. So a rule against inheriting sentences would not have caught this one. 561 should be phrased against claims that drift from the record rather than against sentences carried from a neighbour | accepted |
| 597 | 2026-08-29 | Nine note findings actioned, none dismissed, as one fix dispatch to the Courier. Two blocking: a fix-dispatch claim that converted 592's falling count into an invariant the log never states and attached it to 492, which asks something else entirely, and a count of three announcing a list of four. Then the missing `## The part worth remembering` section, which fourteen of fourteen sibling Capstan notes carry; the absence of 589's glossary row and of 558 being closed, both work this effort did outside the slice; the ten-versus-thirteen repair; a sentence saying the Reviewer saw the Builder's return eventually, contradicting 587 and the note's own next paragraph; a citation naming 587 for a claim only 588 carries; a probe claim strengthened twice past 591; and a pointer at `verify-1.md`, a file the scratch deletion is about to remove | accepted |
| 596 | 2026-08-29 | Factual repair to 593 in place, per the log's repair rule, so the number and status stay and nothing is superseded. 593 said the verify search covered ten locations. `verify-1.md` and `skills/verify/SKILL.md` lines 17 to 20 both name thirteen, and no grouping of that list yields ten. The Architect's error, which reached the gate 3 brief and was copied faithfully into the note, so the log was the source rather than the note the inventor. Found by the note review | accepted |
| 595 | 2026-08-29 | 365 is no longer theoretical, and this is the evidence it was waiting for. It asked whether the knowledge-base location should become a literal key beside `capstan-document-home`, on the reasoning that the Courier reads it as prose from the same file. Today neither `CLAUDE.md` declares it in any form and no `AGENTS.md` exists, so a Courier following `courier.md:44` would have reported no knowledge base and skipped the note, breaking a run of nine notes whose most recent is a day old. The prose route did not degrade loudly; it degraded into a skip that reads like a correct outcome. 365 stays open and now carries a reason to settle it | accepted |
| 594 | 2026-08-29 | The knowledge base is supplied to the Courier in the dispatch rather than resolved from configuration, because no configuration declares it. Assumed rather than accepted: the evidence is nine notes from this repository in that folder, which is strong but is inference from neighbours, exactly the mechanism `courier.md` warns about for frontmatter. Revisit the moment 365 is settled, which supersedes this by making the location readable rather than inferred | superseded by 633 |
| 593 | 2026-08-29 | The integration is verified on `verify`'s degraded branch and the return says so in its own file. The repository declares no checks, searched in the skill's own order across thirteen locations. What ran instead: the slice's red-at-base pointer count, 4 at `eef5abf` and 5 at `8bca8cf`; each changed claim traced to the sentence in the discipline it names; and the diff confirmed at one file, three lines. Nothing red, so no baseline worktree was needed. Stated rather than implied, because silence reads as passing | accepted |
| 592 | 2026-08-29 | Fix dispatch 1 is verified by the Architect rather than sent for a second review, and the slice stops at one dispatch. The single fix was the Reviewer's own prescribed wording applied verbatim, with no new scope, which is the distinction 551 turned on when it did send a second round. Checkable by reading, and I read the whole three-line diff against the three disciplines it cites. Same call as 345, 409, 480, 515, 526, 556 and 566. Fourth data point for 492, and the fix-dispatch count keeps falling: three, two, one, one | accepted |
| 591 | 2026-08-29 | 587's two-set comparison earned its place on its first use, and the probe fired as designed. The Builder missed 579's known omission, the `test-first` bullet dropping the seam constraint, and reported three others instead; the Reviewer, deriving independently, found it and four more. Neither agent was handed it. So the omission list a single agent returns is incomplete rather than wrong, and the disagreement the two-set comparison exists to surface was real on its first outing rather than theoretical | accepted |
| 590 | 2026-08-29 | Five omissions collected under 579, which stays open: `Test-first` drops the seam constraint, drops red-green-refactor, and scopes itself to code where the discipline reaches documents and infrastructure; `Independent review` drops the two-axes property; `Verification before the claim` drops "an exit code is not an observation". All pass 574 and none is fixed. Three of the five sit on bullets this effort otherwise passed, which is the narrowing doing exactly what it was chosen to do | accepted |
| 589 | 2026-08-29 | The wide refactor exception gains a glossary row, named for `slicing`'s own heading. It had three surface names across three files and no row, and this effort just published a fourth site by putting it in a bullet every Architect reads. No file is renamed: the row names the term and records that "the expand-migrate-contract exception" describes the same thing by its sequence. 520 and 550's shape, where a term earned a row on being published rather than on being coined | accepted |
| 588 | 2026-08-29 | The Reviewer's blocking finding is actioned. `Verification before the claim` states an absolute that `verify` contradicts for a named case: line 63 gives a repository with no declared checks a degraded verification rather than none, so a brief can say the work is done with no checks having run. That is commission under 574, not omission. The decisive argument is consistency rather than the reading alone: the bullet is the same construction as `Vertical slices`, both asserting an absolute against a discipline carrying a section-level exception, and one test cannot pass one and fail the other. This repository is standing in the case the bullet forbids, since it declares no checks, so read literally this effort's own gate 3 brief could never say the work is done. Fix dispatch 1 on `standards-audit`, to a new Builder instance per phase 3 step 5 | accepted |
| 587 | 2026-08-29 | Supersedes 586, whose second half was unbuildable as written. It said the Reviewer re-derives its four verdicts before reading the Builder's and then compares the two sets, which would require handing over the Builder's return; `two-axis-review` line 84 forbids exactly that, and it is the line this same effort just audited. The mechanism is repaired rather than dropped: the Reviewer derives its own four verdicts from the files alone and never sees the Builder's return, and the Architect compares the two sets, which is the Architect's job anyway under step 5. 586's first half stands unchanged, that a citation quotes the discipline's sentence rather than naming a line. Found at dispatch, before the Reviewer went out | accepted |
| 586 | 2026-08-29 | Two mitigations against a false clean verdict, distinct from a true one, which needs no mitigating and would only be manufactured by pressing for defects. 585's citation is tightened from a line number to the discipline's sentence quoted beside the bullet's claim, because a line number proves the file was opened where a quotation proves the two were compared. And the Reviewer is dispatched to re-derive all four verdicts before reading the Builder's, rather than to grade them: `two-axis-review` already withholds the Builder's reasoning, so this buys a second independent judgment at no cost and without a second agent touching the file. 579's known omission stays a working probe: `agents/builder.md` never reads the decision log and the dispatch brief will not carry it, so a return listing no omissions is evidence of skimming independent of the verdicts. Neither mitigation makes a shared error impossible; both make disagreement visible | superseded by 587 |
| 585 | 2026-08-29 | The Builder's return carries a citation per bullet: the file and line where the claim is found, or the statement that it is absent and what was searched. The failure mode one slice carries is one Builder going soft on the fourth comparison, and splitting into four would distribute that rather than fix it. Citations make each verdict re-derivable by a Reviewer who has none of the Builder's reasoning, which is where the accuracy on this slice actually comes from. Added pre-dispatch, so no slice was amended mid-build | accepted |
| 584 | 2026-08-29 | Four clean verdicts is a valid result and closes 558. 558 asks whether the other four bullets carry the defect, so "they do not, and here is the comparison" answers it as completely as a list of fixes would. Recorded because the slice's red-at-base evidence covers only the missing pointer, and without this row a Builder finding nothing would read as a Builder that failed to look | accepted |
| 583 | 2026-08-29 | One slice, not fanned out. `slicing`'s single-artifact rule governs: the deliverable is one list in one file, `DESIGN.md` is touched only where the audit finds its row wrong, and splitting the two would create a false edge, since the audit is what decides which half of each pair is wrong. Saying one slice plainly is the correct plan rather than a failure to parallelise | accepted |
| 582 | 2026-08-29 | The other open rows are ruled out of scope: 561 is `courier.md`, 568 is `PHASE-4-DELIVER.md`, 541 is the operator's global configuration and 540 already declined it on ownership, and 13 needs a CLI session rather than an effort. None is the `## Standards you enforce` list. 535's precedent, which refused to add `brief` to an effort about `decision-record` on symmetry alone | accepted |
| 581 | 2026-08-29 | `DESIGN.md`'s discipline rows are in scope when the mismatch turns out to be theirs rather than the bullet's. 557 established that either side of the pair can be the wrong one, and finding a wrong row in the human-facing document and leaving it is the defect 557 filed rather than a scope boundary | accepted |
| 580 | 2026-08-29 | No Scout is sent on hooks during this effort. A Scout return is filed in `<working copy>/.capstan/effort/scout/`, which the Courier deletes at delivery, so a return filed here is destroyed before 577's effort begins. 573 already split them | accepted |
| 579 | 2026-08-29 | Observations that 574 clears as omissions are parked in one `open` row naming the class, with the instances the audit found listed on it. The first instance is already known: the `test-first` bullet drops "at pre-agreed seams only", which `skills/test-first/SKILL.md` makes its central constraint. Parking costs one line; discarding means somebody rediscovers them | accepted |
| 578 | 2026-08-29 | 574 governs the `Independent review` bullet's verdict, so it records no 558 defect. `two-axis-review` line 84 states the claim the bullet makes, that the reviewer never has the builder's reasoning, so the bullet commits nothing. Its missing `per two-axis-review` pointer, the only one absent across the five, is fixed as its own change rather than reported as an audit finding, which keeps 574's test honest while acting on what 575 put in scope | accepted |
| 577 | 2026-08-29 | Whether Capstan gains hooks, and the operator's position is that a hook enforcing beats a discipline missing. This contradicts `effort/SKILL.md`'s closing line, which says no hooks flatly, and `DESIGN.md`'s opening, which makes the absence of an operating layer the project's thesis and which 76 calls the only sentence giving a stranger a reason to choose Capstan. Its own effort per 573, and open rather than assumed because no default is in force: nothing is built either way until it is settled | open |
| 576 | 2026-08-29 | The audit fixes what it finds rather than filing findings for a later effort. 549 fixed as it found, and a findings-only pass would leave three lines of editing to a second run | accepted |
| 575 | 2026-08-29 | The `Independent review` bullet is in scope, by the operator's explicit instruction. It is the only one of the five carrying no `per` pointer, and the discipline it summarises is `two-axis-review`, whose defining property is two axes never blended, which the bullet does not mention. Whether it fails 574's test is a separate question, since its defect reads as omission rather than commission, and 574 clears omissions. Carried to round 2 | accepted |
| 574 | 2026-08-29 | A bullet fails when it states something the discipline does not. Commission only: a bullet that merely drops the constraint distinguishing its discipline passes. Narrower than the Architect's recommendation, which would have failed omissions too, and the narrowing has teeth rather than being a wording preference. `Test-first for anything with code` omits `at pre-agreed seams only`, which `DESIGN.md` carries and which is the whole of what makes the discipline not just "write tests", and under this test that omission is not a defect | accepted |
| 573 | 2026-08-29 | Two efforts, not one. This effort takes 558's audit; the hooks question becomes 577 and is settled separately. 30's precedent, where the README rewrite was split from the path migration because mixing them hides which change broke what | accepted |
| 572 | 2026-08-29 | Supersedes 570. The move off the `BytesNation` organisation was deliberate, confirmed by the operator. The repository stays at `MokeyBytes/capstan`, so the five addresses repointed at 569 stand and the organisation remains the named publisher in both `owner` blocks. No reason for the move is recorded, because none was given and inventing one is worse than the gap | accepted |
| 571 | 2026-08-29 | Version goes to 2.15.1 for the repoint at 569. Patch rather than minor by 1 and 35's precedent, which reserve a minor for a skill or the glossary changing: here nothing a user types or reads in a skill moved, only the address metadata and the README. Taken to keep the number meaningful rather than to name new work, which is what 567 argued the number is for, and it is another instance for 568, though not one that settles it, since this bump sat outside an effort entirely | accepted |
| 570 | 2026-08-29 | Whether the repository stays on the personal account or returns to the `BytesNation` organisation. The move was found in a push redirect rather than recorded, so nothing here says it was intended or is permanent, and every address just repointed assumes it is | superseded by 572 |
| 569 | 2026-08-29 | Supersedes 15. The GitHub repository is `MokeyBytes/capstan`. It moved off the `BytesNation` organisation, which still exists, so only the repository path changed and the redirect from the old address still resolves. 15's other half survives unchanged: the marketplace id stays `bytesnation`, which README line 146 already explains is taken from `marketplace.json` rather than from the repository path, so the install identifier is unaffected. Four addresses repointed: the git remote, `plugin.json`'s `repository`, the clone line, and the two `marketplace add` lines. Both `owner` blocks are left alone, because the organisation is live and still the publisher. `decisions/0001` names the old path in passing and stands unedited, per the rule against editing an accepted record | accepted |
| 568 | 2026-08-29 | Whether the version bump becomes a numbered step in `PHASE-4-DELIVER.md` rather than something an effort is trusted to remember. 7 puts it inside the effort and it has now been missed twice running, at `qualified-paths` and at `architect-disciplines`. Raised by 567 | superseded by 609 |
| 567 | 2026-08-29 | Version goes to 2.15.0, bumped on its own rather than inside an effort, against 7. Two efforts merged since the last bump and neither carried one, and `qualified-paths` already reached origin under the 2.14.0 label, so what a user holds as 2.14.0 is not what that number named. Minor rather than patch by 1 and 35's precedent: a skill changes and the glossary gains rows, and nothing a user types breaks. Carried as 568 | accepted |
| 566 | 2026-08-28 | The note's fix dispatch is verified by the Architect rather than sent for a second review, and the note took one, against the two and three its predecessors took. All six fixes were the Reviewer's own moves prescribed verbatim and applied faithfully, including the one new paragraph, which 563 asked for rather than the Architect adding scope; that distinction is what made 551 send a second review last time and it does not apply here. Third data point for 492, and the count is falling: three, two, one | accepted |
| 565 | 2026-08-28 | Two note corrections actioned together. "Both fix rounds" becomes "both fix dispatches", per 519, which settled one term and says in the glossary that "fix round" and "fix task" mean it; 508 is the precedent for correcting a term against the glossary and 521 for the glossary governing a neighbouring document's habit. The log itself slips the same way at 556, which is a reason to keep the permanent record right rather than to inherit the slip. And the records-absence sentence, stated in both the opening and the closing line, is held to the closing line alone, matching the live neighbour post-521 | accepted |
| 564 | 2026-08-28 | The note's `occurred` frontmatter names a time roughly forty minutes after anything that happened: the merge, the effort's last commit and the note's own write all precede it. Both live neighbours set `occurred` before their write time and carry second-level precision, where this is a round minute pointing into the future. `courier.md` puts dates in the derived class, facts about this effort, and this value derives from nothing in the record | accepted |
| 563 | 2026-08-28 | Two omissions from the note, actioned together. The slice's fix-dispatch count is absent, though 556 recorded it deliberately and both neighbour notes narrate theirs against 472 and 492; leaving it out costs 492's reopen condition a data point the log already holds. And 554 is absent from what was decided, though it records a spec line outliving the decision that changed it, the second instance this session after 407's shape, which the predecessor note gave its own paragraph as a process defect | accepted |
| 562 | 2026-08-28 | Three note claims actioned together, all stronger than their sources. The placement is called "the only spot in the file every phase's reading path crosses", which `load-point-1.md` contradicts: everything above `## The later phases` is crossed by every run, and the earlier candidates were declined on 534 and 542 rather than on reach. The leak sentence attributes the table's prohibition to all three rows where the spec attributes it to one, 524's shape. And the "fourth time" sentence reads the four instances as one class, where 549 says 493 and 540 turned on a different artifact or a different owner | accepted |
| 561 | 2026-08-28 | Whether `courier.md` needs a rule against inheriting a predecessor note's sentences. Three notes running have carried a claim verbatim from the note before them, true there and false where it landed: 522 caught it once, 560 catches it again, and both were the blocking finding of their review. `courier.md` names the mechanism for frontmatter, that a neighbour's value is always plausible and nothing surfaces the mistake, and the body has no equivalent. Reading neighbours for convention is required and is exactly what carries the sentences | open |
| 560 | 2026-08-28 | Blocking. The note reports three glossary rows where two shipped, `Load point` at 550 and `Run` at 555, and its own body then names only those two. The sentence is a near-verbatim carry from the predecessor note, where three was true of 508, 513 and 514, and the count came along with the wording. 522's shape one note later, and the third consecutive note to inherit a false claim from the one before it. Carried as 561 | accepted |
| 559 | 2026-08-28 | `Tier 2` gains a glossary row. `decision-record` and `DESIGN.md` both use it as real project vocabulary and `CONTEXT.md` holds no row, so in a note a person reads it is the one piece of jargon a reader outside the effort cannot resolve. 550 and 555 set the precedent twice already this effort. Raised by the note review as a gap | accepted |
| 558 | 2026-08-28 | Whether the other four bullets in `## Standards you enforce` need the look 549 gave the `decision-record` one. That bullet framed a discipline by its rarest tier and 557 found the human-facing `DESIGN.md` had the framing right all along, so the class is a payload summary drifting from the discipline it names. Nobody has checked the neighbouring four. Raised because the note put the question in the permanent record while `decisions.md` did not carry it, which would have left the two disagreeing about what is open | open |
| 557 | 2026-08-28 | `DESIGN.md`'s discipline table already carried the right framing and names the Architect as a user of `decision-record`, describing it as "a one-line log by default, a full record only when it earns one". So the repository knew, in the document a person reads, what the payload an agent reads got wrong. 549 corrected the payload to match rather than introducing a new claim, and the split is the finding: a framing defect can sit in the agent-facing copy while the human-facing copy beside it is correct, and nothing compares them. Found by the integration sweep for 410's shape | accepted |
| 556 | 2026-08-28 | The second fix round is verified by the Architect rather than sent for a third review. Both fixes were the round 2 Reviewer's own moves, prescribed verbatim in the brief and applied exactly, and both are checkable by reading, which I did against the four neighbouring bullets. Same call as 345, 409, 480, 515 and 526. Worth naming that this stops at two fix dispatches: a third would fire the question `PHASE-3-BUILD.md` now asks, and stopping here is the count coming out where it should rather than the question being avoided | accepted |
| 555 | 2026-08-28 | `Run` gains a glossary row, and it is load-bearing rather than tidy. The whole mechanism rests on "this run's first row" meaning the phase-bounded session; read as the effort, the discipline loads once per effort and 534 is defeated. `CONTEXT.md` currently uses the word in two senses without defining it: `Effort` is "one run of work from concept to delivery" while `Gate` is "a point where the run ends", three per effort. `SKILL.md` is internally consistent and means the session; the glossary is where the ambiguity sits. Raised by the round 2 review as a gap | accepted |
| 554 | 2026-08-28 | `spec.md`'s requirement 2 is repaired to match 546, which cut the sentence the requirement quotes as the property making the instruction recur. The spec was updated for 549 after round 1 and not for 546, so the two disagreed on the record. 546 governs and the branch is right; the spec was stale. The Architect's defect, and the second time this session a spec line outlived the decision that changed it, after 407's shape at `qualified-paths` | accepted |
| 553 | 2026-08-28 | The subsection says "Invoke the `decision-record` skill" rather than the bare backticked form. The file's five other in-body references all carry the noun: "Invoke the `interview` skill", "invoke the `walkthrough` skill", "per the `brief` skill", "per the `decision-record` skill", "per the `verify` discipline". The bare form appears only inside the `## Standards you enforce` list, which is a different construction | accepted |
| 552 | 2026-08-28 | Blocking, and found only because 551 sent a second review at the unreviewed material. The rewritten standards bullet strips every modifier out of its bold lead, leaving "Decisions" where all four neighbours carry the distinguishing word inside the bold, and it leaves "Most efforts earn none" with two live antecedents. Read against the new topic-prominent head, it says most efforts settle no decisions, which the bullet's own preceding clause denies and which is the exact misframing 549 commissioned the edit to remove. One reading of the rewrite is worse than the base line it replaced, in a list every Architect reads every run. One edit closes both: the lead carries its modifier and `none` becomes the explicit noun | accepted |
| 551 | 2026-08-28 | A second review round is dispatched on `load-point`, against the pattern at 345, 409, 480, 515 and 526 where the Architect verified a fix directly. The distinction is not complexity: fixes 1 to 3 are the reviewer's own moves applied verbatim and are checkable by reading. Fix 4 is new scope 549 added after the review, so the `## Standards you enforce` bullet has had no independent eyes, and it sits in the list every Architect reads every run. Verifying it myself would be grading the spec I wrote for it, which is the confirmation bias the crew separation exists to prevent. Round 2 is scoped to the unreviewed material | accepted |
| 550 | 2026-08-28 | `Load point` gains a glossary row: the named moment at which a continuous discipline is invoked, supplied because a duty true at all times attaches to no point in a run. 541 carries the `unslop` instance as open and 544 reopens this one at the next effort that writes rows, so the repository expects to meet the class again and would otherwise rename it. 520's shape, where `Glossed site` earned a row for the same reason. Raised by the `load-point` review as a gap | accepted |
| 549 | 2026-08-28 | Requirement 6 is widened, and the slice also fixes `SKILL.md`'s `## Standards you enforce` line for `decision-record`. That line is the file's only summary of the discipline and it reads "Decision records only when all three gates pass. Most efforts earn none", framing it as governing tier 2, which most efforts never reach, and saying nothing about the log every effort writes constantly. Plausibly part of 533's own causal story, so fixing the load point while leaving the misframing would treat the symptom. Fourth time the 241, 283 and 395 precedent has come up in three efforts, and the first time it is accepted rather than declined: here the finding is causally connected to the defect, where 493 and 540 turned on a different artifact or a different owner. Owns and the graph are unchanged, so 452's run-ending exception does not fire | accepted |
| 548 | 2026-08-28 | The new subsection qualifies `decisions.md` with "in the document home". Five of five other mentions in the file do, at lines 41, 142, 181 and 208 and at 79 as "wherever the document home points", so the new line was the first bare one. 473's class arriving in a new file at a new path, and the reasoning carries: read literally an unqualified filename resolves against whatever directory the session sits in, which the Precondition forbids | accepted |
| 547 | 2026-08-28 | "here" is deleted from the subsection. It anchors the invocation to a position in the document while the rest of the sentence anchors it to an event in the run, and in phase 1 those are different points, since the first row lands at step 3 after the interview. Naming two moments dilutes the only lever 533, 539 and 544 give this change. It is a no-op besides: an agent reading the document is already here | accepted |
| 546 | 2026-08-28 | The subsection's second sentence is cut whole rather than trimmed, closing the worst finding on both axes with one edit. As built the instruction ran 11 words and the reasoning 45. The first why-clause is what the spec sanctioned and earns its place; the second sentence restates what "this run's first row" already carries and ends in authoring rationale for why the instruction was not copied into each phase file, which is 542's reasoning and belongs in this log rather than in payload. 506's shape, one effort later and at a tenth the size | accepted |
| 545 | 2026-08-28 | `SKILL.md` is read whole at the start of every run. Defaulted from how the effort ran today, where invoking the skill loaded the file and each phase file then pointed back into it. An operator who resumes a phase by opening the phase file alone would not load it, and nothing measures which happened. Revisit if a run is observed reaching phase 3 without `SKILL.md` in context | assumed |
| 544 | 2026-08-28 | Naming a moment causes the discipline to load. Defaulted rather than settled, and it is the whole claim of this effort: 533 infers it from three disciplines that loaded at single steps against one that did not, which is an explanation of the past rather than a test of the future. Revisit at the first effort that writes rows after this ships, by asking whether `decision-record` was loaded | assumed |
| 543 | 2026-08-28 | The seam cannot see the thing being fixed, and that is accepted rather than worked around. A grep proves the sentence is in `SKILL.md`; nothing observes an Architect loading a skill and Capstan has no evals, so this is weaker than 505's seam, which at least measured the artifact. A proxy was considered and rejected: requiring the first row of a run to cite the skill would be a ritual proving compliance with itself, which is 423 and 430's shape. Shipping an unverifiable one-sentence change with the limit stated beats a checkable thing that measures nothing | accepted |
| 542 | 2026-08-28 | The instruction is one subsection of `SKILL.md` and the phase files are not touched. `decision-record` is already pointed at from phase 1 step 3 and phase 3 step 9, and those pointers stay; the subsection supplies the moment they lack. One edit covering four phases. Widening `## Every phase begins by re-reading the world` instead would stretch its subject from what changed in the repository to what to load, and would still miss phase 1, which wrote about twenty rows this session | accepted |
| 541 | 2026-08-28 | Whether `unslop` has the same missing load point as `decision-record`. The operator's global `CLAUDE.md` instructs applying it to every piece of prose a person reads, unasked and in full, which is maximally continuous, and it was not invoked once across this session's three gate briefs and every message. Same defect class as 533, found by that diagnosis rather than by looking. Carried from 540 | open |
| 540 | 2026-08-28 | `unslop` is named but not fixed here, and the reason is ownership rather than scope. The `decision-record` load point belongs in `effort/SKILL.md`, which Capstan ships; the `unslop` instruction lives in the operator's personal global configuration, which Capstan neither ships nor should edit. Different artifact, different owner. Carried as 541. Third effort running to decline the 241, 283 and 395 precedent, after 493 named the second: if it keeps happening the precedent is wrong rather than the declines | accepted |
| 539 | 2026-08-28 | The instruction names a moment rather than a duty. "Record decisions as they resolve" is true at all times and so attaches to no point in a run, which is why it never fired. The moment is the first decision row of a run: every run writes rows, every run has a first one, and it recurs per run without needing a differently worded copy in each phase file | accepted |
| 538 | 2026-08-28 | The Courier keeps `decision-record` in its frontmatter although it writes no log rows. It reads the log to write the note and needs the statuses and the vocabulary, so removing it saves nothing and risks a worse note. The asymmetry with the Architect is the evidence that found 533, not itself the defect | accepted |
| 537 | 2026-08-28 | 92 stands. It assumed every discipline is model-invoked with only `effort` as a front door, and this effort is evidence that model invocation failed for a continuous one. Nothing here argues `decision-record` should become a front door the operator types; it argues the Architect should be told when to load it, which 92 does not speak to | accepted |
| 536 | 2026-08-28 | 382's rule is not copied into `effort/SKILL.md`, where the Architect certainly reads. 501 refused a second home for a rule eight days ago and 435 puts a rule where its artifact is defined. The pointer is made to work or its failure is a finding worth having; duplication is not the fallback | accepted |
| 535 | 2026-08-28 | Scope is `decision-record` alone, not `brief`. `decision-record` has three failures today. `brief` attaches to a single step, was read before the first gate brief, and shows no observed failure, so adding it on symmetry would be speculative generality | accepted |
| 534 | 2026-08-28 | The instruction lands once per phase rather than once per effort. A run ends at each gate, so a load in phase 1 is gone by phase 3, which is the same reason `## Every phase begins by re-reading the world` exists and repeats | accepted |
| 533 | 2026-08-28 | The defect is a discipline governing continuous work having no moment to be loaded at, rather than a weak pointer or a missing rule. 385 already put 382's rule into `decision-record` with a four-row table, one row of which forbids exactly the script naming 529 did. Grammar is not the variable either: `slicing` and `verify` say only "per the X skill" and both were invoked, because each attaches to one step at one moment. `decision-record` governs "as they resolve, do not batch them", spread across four phases, so it never had one. Roughly eighty rows were written this session without it ever loading | accepted |
| 532 | 2026-08-28 | Second time this project has published rows 382's rule exists to keep out, and the first time it happened inside the same session that wrote them. 383 and 384 recorded the first, settled at 438 after the operator authorised a history rewrite. Here the repair at 530 landed before the push, so the file at `origin/main` is clean and only the intermediate commit carries the old text. Weaker exposure than the first instance and the same root cause: the rule lives in one precedent row, so nothing carries it into the moment a row is being written. That is the effort 530 names, and it is now overdue rather than optional | accepted |
| 531 | 2026-08-28 | Whether `capstan-document-home` should be written as an absolute path when the answer is the default. 116 requires the key either way, so a project answering "use the default" gets its home directory written into `CLAUDE.md`, which is commonly committed and, for this repository, public. At the default the path is derivable from the repository root, so the absolute form publishes the operator's layout and buys nothing. A configured home is different and needs the absolute value. Raised by reviewing this log for publication | superseded by 699 |
| 530 | 2026-08-28 | Four rows repaired in place for publication, per 382 and the repair rule at 348, so the numbers and statuses stay and nothing is superseded. 529 named two of the operator's own automation scripts by filename; 528 described what else sat uncommitted in the vault; 442 and 441 carried the literal organisation tag and a count of notes in the folder. Each keeps what it taught the project and drops what it found in the knowledge base. 382 made this repair once already on fourteen rows and the leak recurred four times since, twice in rows written today, so the rule needs a carrier the Courier and the Architect both read rather than one precedent row | accepted |
| 529 | 2026-08-28 | New evidence for 392, which stays open. The ` 2` shape 391 recorded in the effort scratch and 448 found inside `.git` also appears in the operator's knowledge base, on files belonging to its own tooling rather than to Capstan. So the mechanism reaches the vault, not only Capstan's working copy. Found while staging the notes, not by looking for it | accepted |
| 528 | 2026-08-28 | Capstan committed the two effort notes to the knowledge base at the operator's explicit instruction, overriding the rule in `courier.md` and `effort/SKILL.md` that Capstan never runs git there and the operator commits. Recorded rather than done silently: the rule is a design decision about whose history it is, and the operator can waive it for their own vault. Only the two notes were staged by exact path, because the vault carried unrelated uncommitted work that does not belong in a commit about these efforts | accepted |
| 527 | 2026-08-28 | This effort's note took two fix dispatches against the predecessor's three, and 492's threshold is three. Second data point for the question 492 carries. The shape differs from the predecessor's: round one was arithmetic and a reason carried over verbatim from the previous note, round two was two citations pointing at the wrong row, one of which exposed a missing row rather than a note defect. Both rounds were found by checking claims rather than reading them, which is 439's lesson holding across three notes now | accepted |
| 526 | 2026-08-28 | The `scratch-paths` fix dispatch was verified by the Architect rather than sent for a second review, and no row said so until now. 515 records that call for `readme` and 516 records only the separate choice not to send a further dispatch over 512's placement, so the note cited 509 for it, which is the classification row and the wrong one. All four fixes were checkable by reading and the seam was rerun in the worktree, including which five sites stayed bare rather than the count alone. Same call as 345, 409, 480 and 515. The note review caught the thin record; the wrong citation followed from it | accepted |
| 525 | 2026-08-28 | The note is renamed off its decision number. No neighbour filename in the folder carries one; every one names what changed in plain words. The review cleared the filename against the `YYYY-MM-DD Title` form, which it matches, but form was not the question. 518 makes it stale as well as unconventional: a title reading "the Gap 473 Left Open" names a row that is now closed. The Architect's call against a clean review finding, recorded because overruling a review needs a reason on the record | accepted |
| 524 | 2026-08-28 | Factual repair to 513 in place, per the log's repair rule, so the number and status stay and nothing is superseded. 513 says this effort writes `<working copy>` "fifteen times across four files". Fourteen are this effort's; the fifteenth predates it, which 473 recorded as the one path already written in the qualified form. The note inherited the loose phrasing and strengthened it with "alone" | accepted |
| 523 | 2026-08-28 | Seven lesser note findings actioned together. The open phase 4 question is cited as 492, the row that carries it, rather than 496, which declined it here. The 500 and 504 carve-out for the README's three bare paths is added to what was rejected, being the item a future sweep is likeliest to re-litigate. The footprint sentence drops "in total", which contradicted the "also" following it. First person is replaced by the role, against eleven of eleven neighbours. The claim about "every fix round since it started keeping count" goes, since 480 and 515 both cite 345 and 409, which predate the counter. 514's `Claim` row and its scope reasoning get a clause. One 47-word sentence is split | accepted |
| 522 | 2026-08-28 | Three blocking note findings, all arithmetic or a carried-over reason. The `Still open` entry for 473 says fixing "the two files this effort's slice happens to own" leaves two of four bare; `scratch-paths` owns all four, and the sentence is a near-verbatim carry from the predecessor note where two of four was true. The two qualified classes are added as thirteen plus four rather than nine plus four, reporting seventeen sites where fourteen shipped. And the glossed class is reported at five, which 509 superseded to four, then repeated in `Still open` as 502's live assumption, which is the damaging half: a future review would test 502's reopen condition against the wrong site count | accepted |
| 521 | 2026-08-28 | `decisions.md` is the log and `decisions/` holds the records, per `CONTEXT.md`'s `capstan_type` row and `courier.md`. The note's closing line calls `decisions.md` "full decision records", and the predecessor note carries the identical sentence, so the folder convention and the glossary disagree. The glossary governs. This note is corrected; the predecessor is delivered and stays as it is, recorded here so a third note does not inherit it | accepted |
| 520 | 2026-08-28 | `Glossed site` gains a glossary row. 499's four classes govern any future sweep of this kind, and "the glossed class" is this effort's working vocabulary for the four sites left bare under a gloss in words. Without a row the next effort renames them. Raised by the note review as a gap | accepted |
| 519 | 2026-08-28 | One term, `fix dispatch`, for a Builder or Courier task sent back on something already built once. The log has used "fix round" at 480 and 515 and "fix task" at 489 and 491 for the same act, while the payload and the glossary both say fix dispatch. 492's revisit condition, phrased as a note reaching a third fix task, means a third fix dispatch. The glossary row is widened to cover the Courier's note, which has no slice | accepted |
| 518 | 2026-08-28 | Closes 473. Its nineteen bare paths are all resolved: fourteen qualified across the four files and five left bare deliberately, being the four under a gloss in words and the `.gitignore` line. 473 asked for its own effort and this was it. It stayed open through the gate 3 brief because nothing in the phase re-reads an open row against the work that answered it, which is worth noticing rather than only fixing | accepted |
| 517 | 2026-08-28 | The Architect actioned both slice reviews without filing either return, which step 4 requires verbatim. Caught by the phase's own `Done when` at close, not by anything earlier: findings were read, logged and dispatched from context, so nothing downstream noticed the files were missing. Both are filed now from the returns as received. The rule exists because a return read once and paraphrased into decision rows loses the citations, and 269's counter shape depends on those files existing | accepted |
| 516 | 2026-08-28 | 512's clause is accepted where the Builder put it, at the very end of the paragraph, rather than after the `setup` sentence as the brief intended. The Builder took "end of its paragraph" literally and flagged it. 512's stated goal is met, the two ensure-the-line sentences are adjacent, and the cost is that "The entry stays bare" now reaches past the git-history sentence for its antecedent. Recoverable within the sentence, since it names `.gitignore` itself. Not sent back: a second dispatch to reorder one sentence is the accretion `third-round` exists to question, and the Architect judging its own wording worth another round is how a slice reaches nine | accepted |
| 515 | 2026-08-28 | The `readme` fix round is verified by the Architect rather than sent for a second review. Two edits, both checkable by reading: a clause deleted and a coordination reordered. The review's proportion finding resolves against a baseline it did not cite: the bullet was already 40 words at base, the longest of the four, so 61 was the defect and 46 is six words of new content on its own starting point rather than an entry out of band. Same call as 345, 409 and 480 | accepted |
| 514 | 2026-08-28 | `CONTEXT.md`'s `Claim` row carries a bare `.capstan/effort/CLAIM.md`, and it is qualified. The glossary is a document an agent consumes, so 473's argument reaches it and 500's carve-out for prose a person reads does not. No slice owns `CONTEXT.md` and the spec never classified it, so the Architect fixes it directly rather than widening a slice's Owns mid-build. It sits outside the seam, which counts `skills/effort/` only | accepted |
| 513 | 2026-08-28 | `<working copy>` gains a glossary row. 501 declined a new artifact for the qualification convention because `CONTEXT.md` holds terms rather than conventions, and `<working copy>` is a term: the glossary's own `capstan-document-home` row is already written in terms of it, and this effort now writes it fifteen times across four files. 501's reasoning does not reach it. Raised by the `scratch-paths` review as a gap | accepted |
| 512 | 2026-08-28 | The `.gitignore` clause moves to the end of its paragraph. Placed mid-paragraph it splits "ensures `.gitignore` carries `.capstan/effort/`" from "`setup` ensures the same line", and "the same line" already had to reach past "an older scratch line" before this effort added "a `.gitignore` line" as a third candidate antecedent. A placement question rather than a wording one | accepted |
| 511 | 2026-08-28 | The `.gitignore` clause stops printing the form it forbids. `writing-for-agents` on Negation: prompt the positive, because steering by prohibition drags the forbidden behaviour into context. The clause leads with "The entry stays bare", which is correct, then spells `<working copy>/.capstan/effort/` out in full as the counterexample, making the broken form the only path written whole in the one paragraph about what goes into `.gitignore`. It states the entry's form positively instead and keeps 498's reason | accepted |
| 510 | 2026-08-28 | `PHASE-3-BUILD.md:18` keeps its qualifier but loses it as grammatical subject. As built the sentence makes `<working copy>/.capstan/effort/` the thing that "is gitignored", while `SKILL.md:234` says that same string in a `.gitignore` would be a broken rule, so the effort asserts both in one commit. Restructured to match the house form already at `SKILL.md:69`: the scratch at that path is gitignored. Line 18 stays out of 504's must-stay-bare class, since it reports a status rather than instructing anyone to write the entry | accepted |
| 509 | 2026-08-28 | Confirms 505 by independent review, which reached the same conclusion without being told it, and supersedes 497's numbers. `PHASE-3-BUILD.md:23`'s third occurrence is qualified, the glossed class drops from five to four, and the seam becomes fifteen or more qualified against exactly five bare, from fifteen and six today. The counterexample string 511 removes and the occurrence 509 adds cancel, which is why the qualified floor moves by one rather than two | accepted |
| 508 | 2026-08-28 | The counter has two names and the glossary is the outlier. `CONTEXT.md`'s `Fix dispatch` row calls it "the rework count", while `PHASE-3-BUILD.md` says "fix-dispatch counter", `SKILL.md` says "fix-dispatch count" twice, and this effort's spec, plan and seam all use "fix-dispatch count". The glossary row changes to match; it is the Architect's own artifact and its wording was the Architect's, so no slice owns this fix. Raised by the `readme` review as a vocabulary gap | accepted |
| 507 | 2026-08-28 | The `readme` line's coordination is restructured, separately from 506. The coordinating "and" sits twelve words from what it joins, so "each live slice's fix-dispatch count" reads as attaching to "rather than the person at the gate". `unslop` 28 covers it: a reader who backtracks to parse a sentence needs it broken or its clauses dropped. The two things `next` carries go next to each other with the shared modifier following both. This survives 506 rather than being fixed by it | accepted |
| 506 | 2026-08-28 | The trailing rationale clause is cut from `README.md:247`, closing the worst finding on both axes with one edit. It is a fourth statement of what `README.md:46`, `PHASE-3-BUILD.md:41` and `SKILL.md:136` already say, and 501 refused a second home for a rule eight rows ago. The entry runs 61 words against neighbours at 24, 31 and 34, and it is the only one explaining why a design works rather than naming what was borrowed. 481 called line 247 provenance rather than specification. 478's shape, at a tenth the size | accepted |
| 505 | 2026-08-28 | The Architect's classification in 499 is wrong on one site, and the seam could not see it. `PHASE-3-BUILD.md:23` carries three occurrences under one gloss, but "in the main working copy" governs `plan.md` and `spec.md` only; the third sits in a later sentence, "and `.capstan/effort/` does not exist inside the worktree it is reading", which is the same claim line 18 makes and which 499 qualified. Two statements of one fact, five lines apart, now written differently. The bare target should be five rather than six. Found by the Architect reading the diff, and unfindable by the seam, since 497's counts were derived from the same classification: a green seam certifies the classification rather than testing it, which is 423 and 430's shape in a new place | accepted |
| 504 | 2026-08-28 | All three of the README's bare `.capstan/effort/` occurrences stay. Line 46 per 500. Line 87 is the same case as `SKILL.md:234`, describing the `.gitignore` entry, so it must stay bare for the reason 498 names. Line 97 already carries "in the repository" as its gloss. 481's fix is line 247 alone, and the `readme` slice touches nothing else in the file it owns | accepted |
| 503 | 2026-08-28 | Qualifying costs nothing on density. Thirteen longer paths across four already dense files is defaulted as acceptable rather than measured; `PHASE-3-BUILD.md` is at 1205 words against siblings at 313 and 414 before this effort adds to it. Revisit if the result reads worse than what it replaced | assumed |
| 502 | 2026-08-28 | The five sites already glossed in words need nothing. A gloss such as "in the main working copy" is defaulted as being as clear to an agent as the symbolic `<working copy>/` prefix, which is untested. Revisit if a review or a build shows an agent missing one | assumed |
| 501 | 2026-08-28 | No new artifact records the qualification convention. 499 in the decision log already does it, and a Reviewer reads that file before grading Standards, which is the documented mechanism for a standard binding on this repository. A glossary row would be a second home for one rule, and `CONTEXT.md` holds terms rather than conventions | accepted |
| 500 | 2026-08-28 | `README.md:46` was checked and is not stale: it says `CLAIM.md` records the phase reached and what is outstanding, which stays true with the fix-dispatch count as part of what is outstanding. Its own bare `.capstan/effort/CLAIM.md` is out of 473's scope, because 473's argument is that an agent resolves a bare path against its session directory, and the README is prose a person reads. Only 481's line 247 is in scope | accepted |
| 499 | 2026-08-28 | Narrows 495 from nineteen sites to thirteen, after inspecting them. The nineteen are four classes: one that must stay bare, `SKILL.md`'s `.gitignore` line, where a `<working copy>/` prefix would write a broken ignore rule, since entries there are repo-root-relative; five already carrying an inline gloss in words; four naming a folder the agent acts on; nine bare instruction sites. The thirteen acted-on and instruction sites are qualified and the other six are left. 495 was agreed before any site was read, which is the defect | accepted |
| 498 | 2026-08-28 | `SKILL.md`'s `.gitignore` line gains a clause saying it is repo-root-relative and stays bare deliberately. Without it a later sweep sees eighteen qualified against one bare, finishes the job, and writes `<working copy>/.capstan/effort/` into a `.gitignore`. A refusal that explains itself is what stops it being re-refused, which is 405's shape applied to a path rather than a fork | accepted |
| 497 | 2026-08-28 | The seam is two greps rather than one, because zero was never the target. Occurrences of `<working copy>/.capstan/effort/` across the four files must reach fourteen or more, against one today; bare occurrences must fall to exactly six, against nineteen today. Both are red at base, and the pair catches over-qualifying, which a single count cannot: qualifying the `.gitignore` line would pass a count-to-zero check while shipping the bug 498 exists to prevent | superseded by 509 |
| 496 | 2026-08-28 | 492 does not ride along with `qualified-paths`, though this effort opens the same file. It is a behaviour change needing its own spec, seam and red-at-base, and bundling it into a mechanical sweep gives one slice two kinds of work a Reviewer then grades as one. It also carries an unsettled assumption first: whether "three fix tasks" means the same thing for a note that has no slices | accepted |
| 495 | 2026-08-28 | All nineteen bare scratch paths are qualified with `<working copy>/`, rather than each file stating the rule once and leaving its paths bare. The failing case for the cheaper option is the one that matters: an agent reaches step 4 of `PHASE-3-BUILD.md` without having read `SKILL.md`'s Document home section in that run. Repeating the qualifier matches the gloss the document home already repeats throughout, so it follows house pattern rather than introducing one. Verbosity on already dense files is the accepted cost | superseded by 499 |
| 494 | 2026-08-28 | 481's README line joins this effort. Same defect class as 473, a description that drifted from what the thing now does, and it lives in a file no other slice touches, so it fans out rather than collides. `third-round` was one slice because its second half was behaviour-free; this effort has genuinely separable artifacts | accepted |
| 493 | 2026-08-28 | 343 and 303 stay out, declining the 241, 283 and 395 precedent for the second effort running, which is worth naming rather than letting become a habit. 343 needs a decision about what qualifies as a durable artifact before any text moves, and 303 needs a chosen name; both turn a countable sweep into a judgement call, where this effort's seam is a count reaching zero. 457's reasoning, applied to a different pair | accepted |
| 492 | 2026-08-28 | Whether the rework check extends to phase 4's note fix loop. Reopened because 472's own revisit condition has been met: 472 declined to extend on the count that no note had reached a third fix task, and this effort's note reached three during its own delivery. 464 accepted 435's shape knowingly and 472 replaced that guess with a count; the count has now moved. For a later effort, and it now has one instance of evidence where it had none | superseded by 611 |
| 491 | 2026-08-28 | Third fix task on the note, and the last. Two claims left that go stale by being written: "now sitting at `main` `230197c`", which names a moving HEAD where the merge commit `20c098e` is the permanent fact, and the statement that phase 4's note loop "has reached two rounds and never three", which this note falsified by taking a third. The pattern behind all three fix tasks is one thing rather than five defects: a permanent record embedding snapshots of moving state. Every factual claim in the note has now been checked rather than read | accepted |
| 490 | 2026-08-28 | Two factual errors in the note, both found by checking a claim rather than reading it, which is 439's shape. It says the effort was "reviewed and merged by the operator alone", where a Reviewer reviewed the slice and the Architect merged it at `20c098e` and the operator approved gates. And it gives this effort's rows as 449 to 481 twice, written before the note review logged 482 to 489. The range becomes open-ended rather than re-pinned: a note that names its own last row cannot stay self-consistent when reviewing that note adds rows. Second fix task on the note, against 472's revisit condition | accepted |
| 489 | 2026-08-28 | The note is at one fix task in the phase 4 loop that 472 deliberately left uncounted. Recorded so 472's revisit condition, a note reaching a third fix task, has evidence accumulating against it rather than being reconstructed later. One is not three and nothing changes yet | accepted |
| 488 | 2026-08-28 | Three small note findings actioned together. The partner-brief sentence returns, per 441, which settled that saying why no brief exists is worth more than its placement costs. The version-suffix sentence splits: 468 records the operator instructing the suffix be discarded, while the no-release-slice half of 462 stands as the Architect's own call, so a bump was refused rather than discarded. And spec requirement 7, the two pointer clauses added to steps 5 and 8, gets reported as an edit rather than implied by naming the two feeds | accepted |
| 487 | 2026-08-28 | Both assumptions are reported as settled facts and must carry their reopen conditions. 466 and 467 are `assumed`, and the glossary defines that status as carrying the condition that would reopen it, so the condition is exactly what a future reader needs and exactly what the note dropped. They move to `Still open` with their conditions, matching what the Tracker and Setup notes do with theirs | accepted |
| 486 | 2026-08-28 | "It touched two files" understates the footprint. Two is the slice's Owns; the effort also added two glossary rows to `CONTEXT.md` and created `CLAUDE.md` at 449 carrying the document-home key. `courier.md` requires the body to cover where the work lives, and a reader sizing this effort from the note would miss a new file at the repository root | accepted |
| 485 | 2026-08-28 | The note's one citation is wrong on both the number and the status: it cites 295 as open, where `decisions.md` has 295 superseded by 473, and the nineteen-instance measurement it describes is 473's own content. 473 is the open row, belongs to this effort, and is missing from `Still open`, which lists only 481 | accepted |
| 484 | 2026-08-28 | Blocking. The vocabulary this effort settled is absent from the permanent record. Two glossary rows were added, `Fix dispatch` and `Live slice`, and 479 answered a question a future reader cannot reconstruct: a merged slice stays live because step 8 reopens it, and its count resumes rather than restarting, since 456 resets only on a recut. Spec requirement 8 is written in that noun. The note uses `live slice` once as though it were pre-existing vocabulary. 440's shape exactly | accepted |
| 483 | 2026-08-28 | Blocking. The note carries three body sections where eleven of eleven prior effort notes carry four, and the missing one is where neighbours put verification evidence. It says nothing about verification at all, though the spec declares three seams and 455 required an observed check. `courier.md` says to put what you observed in the record. The section returns carrying the observed check, what `plugin validate` and the two greps did and did not cover per 370, and the shape of the round-one finding at 474 | accepted |
| 482 | 2026-08-28 | Blocking. The note cites one decision row across 750 words where its folder's convention is fourteen to sixteen, and 440 graded that exact defect blocking on the previous note here. 446 sets the far end at 45 as an overcorrection, so the band is real and this note sits on 440's side of it. The content is already correct; only the numbers are missing, so this is attaching rows to claims the note already makes | accepted |
| 481 | 2026-08-28 | `README.md:247` describes the claim's `next` line as what the run after this one picks up, which stays true and is now incomplete, since `next` also carries each live slice's fix-dispatch count. It sits in the borrowed-ideas list as provenance rather than as a specification, and 461 refused README work in this effort. Found by the verify sweep for 410's shape, and the only thing that sweep turned up | open |
| 480 | 2026-08-28 | The rework-check fix round is verified by the Architect rather than sent for a second review. All six findings were specific and checkable by reading: an ordering rewrite whose new trigger sentence I read in full, two pointer clauses, a restored directive, a naming match and a deletion. I ran the three seam checks myself in the worktree at `06aa77f` rather than taking the Builder's report for them. Same call as 345 and 409 | accepted |
| 479 | 2026-08-28 | Settles the `live slice` gap the rework-check review raised. A slice is live while it can still receive a fix dispatch: every status but `dropped`, for as long as the effort is open. A merged slice stays live because step 8 sends verify red back onto it, and its count resumes rather than restarting, since 456 resets only on a recut and a verify reopening is not one. Glossary row added | accepted |
| 478 | 2026-08-28 | The closing 88-word history paragraph is cut from the block. The Architect's brief asked for it and the brief was wrong: the spec's nine requirements do not include a historical catalogue, every rationale elsewhere in the three phase files is a clause attached to the instruction it justifies, and `PHASE-3-BUILD.md` went 859 to 1247 words against siblings at 313 and 414. Paragraph 2 already spends 269's evidence where it does work, and the log holds the rest checkably. Same shape as 407: the spec was imprecise, so the spec is at fault rather than the file | accepted |
| 477 | 2026-08-28 | Two small review findings actioned together. The block names `decisions.md` in the document home rather than saying "decision-log row", matching what steps 5, 8 and 9 already do three times in the same file. And the claim template's `next` placeholder stops asking a phase-1 Architect for counts on slices `plan.md` has not cut yet | accepted |
| 476 | 2026-08-28 | `SKILL.md`'s `next` paragraph gets its imperatives back. The base carried three instructions and the rewrite folded two into a subordinate noun phrase inside a 60-word explanatory sentence, where "the slice names as `plan.md` names them" parses as something `next` contains rather than a rule to follow. The rationale sentence stays; the directive returns as its own sentence | accepted |
| 475 | 2026-08-28 | The block names where the counter is written, the claim's `next` line, rather than leaving the definition in one file and its storage in another with no pointer between them. 459 put it there and nothing in the block said so, which is the co-location rule the same file already honours by naming `decisions.md` at steps 5, 8 and 9 | accepted |
| 474 | 2026-08-28 | Blocking, rework-check. The question fires before the dispatch that would make three, not after the count reaches it. As built the counter incremented on send, so the recut answer ended the run with a Builder already working in a worktree step 7 only removes after a merge, a state nothing in the file covered. Phrasing the trigger on the dispatch about to go out removes the in-flight case rather than documenting it, which is the cheaper of the two fixes the review named | accepted |
| 473 | 2026-08-28 | Supersedes 295 with its measured scope. Nineteen bare `.capstan/effort/` paths sit across the four `skills/effort/` files: seven in `SKILL.md`, seven in `PHASE-3-BUILD.md`, three in `PHASE-4-DELIVER.md`, two in `PHASE-2-PLAN.md`, against one written as `<working copy>/.capstan/effort/`. Read literally by an agent, a bare path means relative to the session's own directory, which the Precondition forbids. Its own effort, and the count confirms 457: `rework-check` owns two of the four files, so fixing two leaves the same inconsistency it was refused for | superseded by 518 |
| 472 | 2026-08-28 | Closes 465. The rework check is not extended to phase 4's note loop. The record has a note reaching two rounds, a blocking round at 440 and an overcorrection at 446, and never three, so extending the rule to a place that has never reached its own trigger is Speculative Generality, which this repository's own review baseline names. 464's scoping now stands on a count rather than on the guess it was. Revisit when a note reaches a third fix task | accepted |
| 471 | 2026-08-28 | This effort's worktrees live under `~/.capstan-worktrees/`, outside iCloud's Desktop and Documents sync, rather than beside a working copy that sits inside it. 448's two bad sha1 objects are no longer present and the tree holds no sync duplicate today, so the mechanism is live while its artefacts are not. 392 stays open: the repository itself remains exposed, and moving it is the operator's under the Authority table | accepted |
| 470 | 2026-08-28 | One slice, not two, though the change touches two files. The `SKILL.md` half on its own is a count nothing reads, which is behaviour-free and the horizontal cut `slicing` exists to prevent; the whole change is one bolded block, two pointer clauses, a template line and a paragraph, which fits one context window. A one-slice plan is a correct outcome per phase 3, not a failure to parallelise | accepted |
| 469 | 2026-08-28 | Narrows 454. The rule lands as a bolded lead-in block between step 10 and `Done when`, not as a `##` subsection. The three phase files carry exactly one `#` heading each and no `##` anywhere, so a heading would be the only one of its level in `skills/effort/`'s phase files, which is the shape 397 called blocking. The bold lead-in matches steps 1, 7 and 8 and `Done when` | accepted |
| 468 | 2026-08-28 | Reverses the leave-it-untouched half of 462 at the operator's instruction. The `+codex.20260825035653` build-metadata suffix is discarded from the working tree and `plugin.json` returns to the committed `2.14.0`. Nothing explained the suffix and no row asked for it. The no-release-slice half of 462 stands | accepted |
| 467 | 2026-08-28 | Widening the claim's `next` line to carry a dispatch count per live slice does not degrade it. Defaulted so 459 could proceed; `next` was deliberately one line and this makes it several. Revisit if a later effort finds `next` has become something an Architect skims rather than reads | assumed |
| 466 | 2026-08-28 | Three fix dispatches is the threshold. Defaulted rather than settled: 430 named a design fault at three, while 269 and 58 ran to nine and seven, so three is the earliest point the record supports and nothing tests it. Revisit if slices trip at three routinely and the answer is always "the implementation" | assumed |
| 465 | 2026-08-28 | Whether phase 4's note fix loop gets the same rework check. 118 sends a blocking finding back as a fix task on the same note, and this effort's own evidence includes a note that took a blocking round at 440 and an overcorrection at 446. Carried from 464 | superseded by 472 |
| 464 | 2026-08-28 | The rule is scoped to phase 3 and does not extend to the Courier's note. The note is one artifact with no slices, no worktrees and no fixed point, so its economics differ enough that extending the rule now would be a guess. This is 435's shape and the risk is accepted deliberately rather than unseen; the phase 4 question is carried as 465 rather than dropped | accepted |
| 463 | 2026-08-28 | The answer is logged every time the question fires, not only when it changes. A row reading "asked again at six, still an implementation defect" is the record that would have made 269's nine rounds legible while they were running, and logging only on change makes an asked round indistinguishable from a skipped one | accepted |
| 462 | 2026-08-28 | The uncommitted `+codex.20260825035653` version suffix in `plugin.json` predates this effort and no row explains it. It is left untouched, every commit this effort makes is staged explicitly so nothing sweeps it up, and it is reported at gate 1 as the operator's to resolve. No release slice: a version bump stacked on an unexplained suffix buries it | superseded by 468 |
| 461 | 2026-08-28 | The README gains nothing. Line 37 covers phase 3 in one paragraph and enumerates no steps, so a rework subsection sits below its altitude. Recorded in the spec's out-of-scope rather than left out, because 410 was an integration defect between the README and `SKILL.md` that neither slice's review could see | accepted |
| 460 | 2026-08-28 | `SKILL.md` is in scope, following 459. Its claim block and the paragraph governing the `next` line both change, and no tracker transition does: 452 routes a recut or a drop through the operator, and phase 3 already moves a dropped row | accepted |
| 459 | 2026-08-28 | The dispatch count per live slice lives in the claim's `next` line. 450 counts dispatches rather than review files, phase 3 spans runs, and a count held only in a context window means the question fires only when one Architect makes three dispatches in one sitting. `next` is the artifact written for the Architect who resumes, which is 269's reasoning. Cost named rather than hidden: `next` stops being one line | accepted |
| 458 | 2026-08-28 | The question fires at every fix dispatch from the third on, not once at the third. 269 records a slice that reached nine rounds, and a check that fires once and goes quiet is how nine happens. One question per round is the cost | accepted |
| 457 | 2026-08-28 | 295 stays open and this effort does not fix it, against the 241, 283 and 395 precedent that a slice owning a file outright fixes what it finds there. Those three concerned defects contained by the file in hand; 295 spans `SKILL.md` and all three phase files, so repairing one of four leaves the repository less consistent rather than more. Same reasoning as 441 | accepted |
| 456 | 2026-08-28 | A recut slice resets its counter to zero. The recut is a different slice, and inheriting three trips the question again on its first dispatch. The log keeps both counts, so the history survives the reset | accepted |
| 455 | 2026-08-28 | Verification is `claude plugin validate`, a grep assertion red at its base commit, and one observed check: a constructed slice at three fix dispatches, read against the new text, to see whether the question is asked. Per 370, the spec states the limit rather than letting a green validate imply that an agent's runtime behaviour was tested | accepted |
| 454 | 2026-08-28 | The rule lives in its own subsection of `PHASE-3-BUILD.md` below the numbered steps, with steps 5 and 8 pointing at it. Writing it into step 5, where slice-review findings are dispatched, is 435's exact shape: a rule about the artifact scoped to the first branch that happened to need it, when 451 has it fed by two | accepted |
| 453 | 2026-08-28 | Settles 408. Builder briefs stop asserting "no em dashes anywhere". `unslop` governs prose a person reads and carries the rule; `writing-for-agents` governs prose an agent reads and does not. The 13 em dashes in agent-facing payload are not defects under the written standard, and whether the rule belongs in `writing-for-agents` stays undecided rather than assumed | accepted |
| 452 | 2026-08-28 | At the third fix dispatch on one slice the Architect asks whether the slice's design is wrong rather than its implementation, answers it unattended and logs it, because a third round is ambiguity rather than consequence. The exception is an answer that changes a gate-2 slice's shape, recutting or dropping it: that ends the run and reports, because the operator locked that plan. Continuing to patch does not end the run. Adds no gate, per 118 | accepted |
| 451 | 2026-08-28 | Slice reviews and verify returns feed one counter per slice. The worst recorded case is a verify loop: four verify rounds on `setup`, with 430 naming the design fault behind three of them. Phase 3 step 8 already sends a verify failure back as a Builder task on a named slice, so those are fix dispatches on that slice. Counted apart, `home-setup` sits at two and two and never trips | accepted |
| 450 | 2026-08-28 | The rework counter counts fix dispatches to a Builder on one slice, not review returns filed. 269 counts `<slice>-<n>.md`, but 257, 304, 305, 306, 345, 346 and 409 each merge a slice after a round the Architect verified directly, which files no review. A counter reading filenames misses exactly the rounds those seven decisions record | accepted |
| 449 | 2026-08-28 | The document home is the default, `<working copy>/.capstan`, and the key is written into a new `CLAUDE.md` in `setup`'s format. The project has run on the default for 448 decisions with no key on disk, which is precisely the state 116 says the key exists to tell apart from a project nobody ever asked | accepted |
| 448 | 2026-08-24 | New evidence for 392, which stays open: the sync duplication reaches inside `.git`. Two files named `<sha> 2` sit in `.git/objects/` and `git fsck` reports them as bad sha1 files. Harmless at 8 KB and not valid objects, so git ignores them for reads, but 392 was written about the scratch and the mechanism is one layer deeper than that. A document home in a synced vault is the same exposure with a worse blast radius | accepted |
| 447 | 2026-08-24 | The missing-home report must distinguish a default that has never been created from a configured home that has vanished. As built it fires on both, so the most common first run of all, a fresh repository choosing the default, is told that if the home held anything before it is gone now. Hedged and therefore true, and still wrong: a warning that fires on every fresh run means nothing when it fires on a real loss, which makes 437 worthless rather than merely noisy. The distinction is whether a key was ever set. Found by the Architect tracing the fix rather than by any fixture, because no verify round ever ran a fresh repository choosing the default | accepted |
| 446 | 2026-08-24 | The note's rewrite overcorrected STD-1. It went from zero decision citations to 45 against neighbours at fifteen and sixteen, and from 983 words to 1614 against neighbours at 1012 and 1010. The finding was that the note broke house convention by citing nothing; citing three times the convention breaks it in the other direction, and one citation per 36 words reads as a log index rather than a note. Its content is now correct, which is the part that mattered, so this is a tightening pass rather than another blocking round. Folded into the pass 443 makes necessary anyway | accepted |
| 445 | 2026-08-24 | Step 3's "the key is wrong" option is offered to projects that have no key, where the home in force is the default rather than anything anyone configured. Unanswerable as written. Observed in verify 4 case N | accepted |
| 444 | 2026-08-24 | The Architect judged step 3's existing ask sufficient for a vanished home reached by a different path, on the grounds that it stops and asks rather than proceeding silently. Verify 4 shows the likely answer is "nothing exists yet", which completes the run green. Too generous a reading of a question whose options do not include what actually happened | accepted |
| 443 | 2026-08-24 | Fifth instance of 435, and it would have shipped in the release whose note names that pattern as the lesson. 437's report is scoped to the branch where the confirmed path equals the home in force, rather than attached to the fact it is about: the home in force does not exist. So an operator whose vault was deleted and who then picks a different path finishes green and is never told. The skill holds the distinction between gone and empty at that moment and discards it, because step 3 lists contents rather than checking existence. The report moves to the fact: when the home currently in force does not exist, say so, whichever path is confirmed | accepted |
| 442 | 2026-08-24 | Whether the organisation tag is the right one for a Capstan note. `courier.md` says the tag comes from the project, never the organisation that owns the folder, and the folder's notes are not consistent on this. Searching the organisation tag today returns two projects at once, which is the symptom that rule names. Folder-wide and not this note's to fix | open |
| 441 | 2026-08-24 | Three note findings dismissed. The partner-brief paragraph stays: `courier.md`'s list of what a body covers is not exhaustive and saying why no brief exists is worth more than its placement costs. The organisation tag is not corrected here, because the folder's other notes carry it and changing one leaves the folder less consistent, not more; carried as 442. `occurred` is left alone, since the neighbours disagree with each other and no convention exists to match | accepted |
| 440 | 2026-08-24 | The note goes back to the Courier, blocking on both axes. `What was decided` carries feature shape and build defects while omitting eight rules this effort settled, several of them the narrowed forms 349 exists to protect. It cites no decision numbers at all where its two neighbours cite fifteen and sixteen, so nothing in it is checkable. And it says four fix rounds followed gate 3, when gate 3 was posted after the last of them, which would record a verified build being reopened after a delivery gate | accepted |
| 439 | 2026-08-24 | `16c799e` is a plain commit with one parent, not a merge, so this effort shipped four merges and one commit rather than five merges. Caught by the note review checking a claim rather than reading it | accepted |
| 438 | 2026-08-24 | Settles 384 and narrows 383, both of which went stale and stayed `open` while the note faithfully reported them. Both were written before the history rewrite; the operator then authorised it, the Architect performed it, and neither row was revisited. Observed now: `origin/main` carries zero leaked strings and zero unpushed commits carry the pre-redaction text, so 384's republication risk is gone and 383 reduces to whether GitHub still holds unreferenced objects by SHA until it garbage-collects. The Architect's defect, and the second time this effort a record outlived the fact it described | accepted |
| 437 | 2026-08-24 | Closes 436 at the operator's instruction, before shipping. `setup` says plainly when the confirmed path is the home already in force and did not exist: it was missing and now holds nothing, so whatever was there is gone. Creating it stays right, since `setup` is the repair tool `effort` sends people to, but a recreation reported as success is silent data loss. Built as a slice rather than patched into phase 4, because it is payload and payload gets built and reviewed | accepted |
| 436 | 2026-08-24 | `setup` recreates a configured home that has vanished and reports success, and the corrected `Done when` passes with zero artifacts because the clause is vacuously true. `effort` stops on that exact state, calling a missing source of truth not ambiguity. Creating it is defensible, since `setup` is the repair tool `effort` sends people to, but nothing tells the operator the home was missing, so a deleted vault reads as a clean first run. One sentence closes it. Left open rather than fixed: four fix rounds in, whether to hold the release for a reporting gap is the operator's call | superseded by 437 |
| 435 | 2026-08-24 | Names the pattern behind 412, 422 and 431, three defects of one shape in one slice: a rule written for the branch that first needed it, then read as belonging to that branch. Append-versus-replace was scoped to the two-file mismatch, the no-merge rule was scoped to a full collision, and key removal was scoped to a disagreement. Each read correctly in place and failed everywhere else. A rule about the artifact belongs where the artifact is defined, not in the first branch that happened to need it | accepted |
| 434 | 2026-08-24 | Small verify-3 findings actioned together: "stops the phase" is glossed or replaced, since nothing in the skill calls setup a phase; "give a different path" names which of The ask's two questions re-runs; a missing file is distinguished from a file carrying no key; step 4's refusal stops asserting "the four" when its own listing may have found fewer; and a folder step 3 created is removed when a later step stops | accepted |
| 433 | 2026-08-24 | Step 3's existence guarantee cannot be jumped. Step 2's same-path shortcut and the full-collision Proceed both continue at step 5, so a re-confirmed key pointing at a deleted folder survives the whole run, which is the exact failure step 3 exists to prevent. Observed on a fixture whose configured home did not exist and whose `Done when` passed anyway | accepted |
| 432 | 2026-08-24 | `agree` gets defined, and the third state gets named: both files existing with only one carrying the key is neither the disagreement branch nor the already-agreed one, and currently falls into an Otherwise by elimination | accepted |
| 431 | 2026-08-24 | Key removal is unconditional wherever a second line would otherwise survive, not scoped to the disagreement branch. Two files already carrying the same value ended a by-the-book run with two lines, failing `Done when`. Same too-narrow scoping that 412 caught in the append rule, in a different sentence of the same step | accepted |
| 430 | 2026-08-24 | `Done when`'s artifact clause states the wrong invariant and is rewritten rather than patched a third time. "All four or none" is false for any project holding some but not all, which step 2 itself calls ordinary, and it passes on a configured home that does not exist. The invariant is that the configured home exists and every one of the four that exists anywhere sits there. Three rounds of this criterion certifying the defect its own run produced is a design fault, not three bugs | accepted |
| 429 | 2026-08-24 | Small verify-2 findings actioned together: step 7 covers a `.gitignore` that does not exist yet, which four of five fixtures had; "Currently:" fires in the disagreement branch too, where the text claimed it always fires and it did not; step 2 stops naming "proceed" as an answer it never offers; and the written spelling of the default path is fixed rather than left to the writer | accepted |
| 428 | 2026-08-24 | Step 4 gains a branch for a key that points where the artifacts are not. It lists what sits at the home in force, and the home in force is defined solely by the key, so a key that lies produces nothing to move and no clean `Done when` clause. Case 3 passed only because its fixture's key was truthful, and nothing in the procedure verifies that | accepted |
| 427 | 2026-08-24 | The read gains a format contract and rejects three states it currently accepts: a file carrying the key twice, an empty value, and a relative value already on disk. Step 1 rejects a relative path from the operator while nothing rejects one from a file. The duplicate case is not hypothetical: it is exactly the wreckage 412 wrote into a real repository before it was caught | accepted |
| 426 | 2026-08-24 | Removing the losing key from a file that carried nothing else must say what happens to the file. Case 4 committed a zero-byte `CLAUDE.md`, which nothing in the text anticipated | accepted |
| 425 | 2026-08-24 | Step 5 stops asking which file to write into when the disagreement branch already settled it. Read literally, an operator with two disagreeing files picks the correct one in The ask and is asked to pick again at step 5, which case 4 hit | accepted |
| 424 | 2026-08-24 | The prescribed stale-copy sentence is replaced with wording that names which location holds what. As written it is false twice over: backwards on a partial collision, where the stale file is at the destination and the live ones are at the source, and nonsense when the confirmed path is the home in force, where it claims a stale copy sits at the directory just declared correct | accepted |
| 423 | 2026-08-24 | `Done when` must require the four artifacts to sit in one place, rather than accepting that a sentence was said. Case 5 satisfied it with the glossary and the decision log orphaned. A completion criterion dischargeable by speaking is not a criterion, which is the second time this run that `Done when` certified the defect the same run produced | accepted |
| 422 | 2026-08-24 | Reverses the partial-collision half of 420, which was my dismissal and was wrong. Observed in case 5: answering "keep this one and move nothing further" against a destination holding one stale `tracker.md` left the key pointing there while the glossary and log stayed unreachable in `.capstan/`. Refusing to merge by leaving three artifacts behind is the same split state 372 forbids, reached by omission instead of by overwrite. "Keep this one" is coherent only when the destination holds all four; on a partial collision the answers are a different path, or stop so the operator clears it by hand. Caught by a fixture the first verify never ran | accepted |
| 421 | 2026-08-24 | Small verify findings actioned together: step 3's two answers gain their consequences and their route into step 4's stale-copy sentence, step 9 attributes both sides of a move rather than the destination alone, the commit gains a subject line, and the `.capstan/`-stays justification stops resting on a scratch that may not exist | accepted |
| 420 | 2026-08-24 | Dismissed twice over. Partial collisions stay all-or-nothing, because 372 forbids merging and a partial merge is the most dangerous kind; the text says why rather than growing a branch. And `.gitignore` placement needs no guidance, since where a line sits in that file has no functional consequence | superseded by 422 |
| 419 | 2026-08-24 | "Two steps. Never a three-way menu" is reworded rather than the branch removed. 367 governs the layout ask and nothing else; the both-files-exist question 406 added is a different ask, and the claim as written denies a question the same skill mandates | accepted |
| 418 | 2026-08-24 | Paths are normalized before any comparison. The skill spells the default with a trailing slash and its examples without, and two spelling-sensitive comparisons rest on that: the read at the top and step 4's skip test. The verify run wrote a slash in one case and none in two others | accepted |
| 417 | 2026-08-24 | The destination check moves ahead of folder creation. Step 2 created the folder before step 3 could reject it, so answering "give a different path" leaves an empty orphan inside the operator's vault permanently, indistinguishable from a configured project folder | accepted |
| 416 | 2026-08-24 | Refusing the move must stop the key write too. Steps 5 to 8 ran unconditionally, so a refusal left the key pointing at an empty home while the real decision log sat at the old one, and `Done when` blessed it. Worse than either doing the move or doing nothing | accepted |
| 415 | 2026-08-24 | `setup` gains an `argument-hint` and a Precondition that establishes the working copy rather than verifying one. `effort` carries an argument-hint and `setup` does not, so an operator typing the command supplies nothing, and in all three verify cases the path came from the harness rather than the skill | accepted |
| 414 | 2026-08-24 | `Done when`'s artifact clause is unsatisfiable on a fresh repository, where no artifacts exist and neither disjunct holds. The fresh first run is the skill's central case, so the criterion failed on the scenario it most needed to cover. It gains a none-exist-yet branch | accepted |
| 413 | 2026-08-24 | `Done when` counts files rather than key lines, so it reads as true over 412's corruption. Tightened to exactly one `capstan-document-home` line across both files. A completion criterion that cannot see the defect the same run produced is the premature-completion shape 403 was meant to close | accepted |
| 412 | 2026-08-24 | Blocking, found by the observed check 370 required and by nothing else. 401's regression is closed: the vault-to-vault move was run and observed, the old vault ended empty and all four artifacts landed at the new path. But step 5 says append and scopes its only removal to the two-file mismatch, so a re-run wrote a second contradictory key into `AGENTS.md` and committed it. 239 stops every later effort on two differing values, so running `setup` twice bricks the project. Every static check passed over it | accepted |
| 411 | 2026-08-24 | The `release` slice merges on the Architect's own verification rather than an independent review. A one-line version bump carries no judgment for a Reviewer to be independent about, its seam covers it completely (the grep pair, a JSON parse and `plugin validate`), and I read the whole diff. Recorded as a deliberate deviation from the independent-review standard rather than a skipped step, same shape as 409 | accepted |
| 410 | 2026-08-24 | Integration defect between two merged slices. `README.md` credits `setup` alone with adding the `.gitignore` line, while `skills/effort/SKILL.md` states what 398 settled, that both ensure it. An operator who answers "use the default" never runs `setup`, so the README tells them a line gets added by a command they will not run. Neither slice's review could see it, because each reviewed one file. Found by the Architect's integration sweep and sent back to the readme slice | accepted |
| 409 | 2026-08-24 | The effort-branch fix round is verified by the Architect rather than sent for a second review. The blocking finding was heading scope, checkable by printing heading and paragraph line numbers, which I did: all six paragraphs sit between `## Document home` at 57 and the H3 at 81. Same call as 345 | accepted |
| 408 | 2026-08-24 | Settles 102, which asked where the em dash rule lives and was never answered. It lives in `unslop`, which governs prose a person reads, and nowhere in `writing-for-agents`, which governs prose an agent reads. So `README.md`'s two were a defect and 395 was right, and the 13 in agent-facing payload across six files are not defects under the written standard. Every Builder brief this effort issued said "no em dashes anywhere", which the standards do not support. Either the rule is written into `writing-for-agents` or briefs stop asserting it | superseded by 453 |
| 407 | 2026-08-24 | `spec.md`'s "runs steps 1 through 8 unchanged" is corrected to nine. Step 9 is conditional on a path outside the repository and belongs on a change into a vault. The Builder wrote nine and was right; the spec was imprecise, so the spec is fixed rather than the file. The Architect's defect, the second this effort traced to a spec line | accepted |
| 406 | 2026-08-24 | `setup` handles `CLAUDE.md` and `AGENTS.md` carrying different values, reporting both and having the operator pick one. 239 stops every effort on that state and `setup` is the only tool that could repair it, so leaving it unhandled means the repair tool cannot repair the one state that blocks everything. A gap the spec left, found by the setup-skill review | accepted |
| 405 | 2026-08-24 | Step 3's fork names its options rather than ending at "ask the operator how to proceed". Merging is forbidden by 372, so the options are a different path, or keeping the existing home and moving nothing. An unnamed option set at the one point a procedure forks is an agent inventing one | accepted |
| 404 | 2026-08-24 | Narrows 363. `setup` makes one commit at the end, covering every change it made to the working copy: the removal, the key line, and the `.gitignore` line. 363 put the commit before the key write, so the skill ended leaving two files dirty, and 363's own argument against committing nothing was that it leaves an uncommitted change behind. Inherited from the spec rather than a Builder defect | accepted |
| 403 | 2026-08-24 | `setup` gains a `Done when`. Ten skills in this repository carry one and every skill running an ordered procedure does; the ones without are reference documents. Nine ordered steps with no bound on the last is the premature-completion shape `writing-for-agents` names | accepted |
| 402 | 2026-08-24 | Dismissed. The nine numbered `###` step headings in `setup` stay, though no other skill numbers its headings. 397 was about a heading's scope swallowing content that did not belong under it; here each step's content sits under its own step. The steps are multi-paragraph and the order is load-bearing, which a numbered list handles badly | accepted |
| 401 | 2026-08-24 | Blocking, setup-skill. Step 4 sourced the move from `<working copy>/.capstan/` rather than from the home currently in force, so re-running to move from one vault to another reported nothing to move, wrote the key to the new home and stranded the log in the old one. Nothing downstream catches it, because `effort`'s condition 2 fires only when `.capstan/` holds artifacts. The slice's headline claim, that a re-runnable command closes 256, was false as built | accepted |
| 400 | 2026-08-24 | `effort`'s condition 1 points at `setup`'s rule for which file receives the key rather than restating it. The Builder left it unstated and 374 predicted exactly that: a Builder with no rule invents a placement. A pointer keeps the branch short per 380 and the rule in one home | accepted |
| 399 | 2026-08-24 | Answering "run `setup`" ends the run. 378's "not a hard stop" means the question does not force everyone through `setup`, not that both answers continue: the Architect cannot invoke `setup` itself under 381, so the operator runs it and starts the effort again. A spec gap the effort-branch review found, settled here rather than left to a Builder | accepted |
| 398 | 2026-08-24 | Narrows 380. The `effort` skill ensures `.gitignore` carries `.capstan/effort/`, because it creates the scratch, and `setup` ensures it too on first run. One idempotent rule with two callers rather than two rules. Deleting the effort skill's line, which 380 read literally would require, leaves nobody adding it for an operator who answers "use the default" and never runs `setup`. Raised by the reviewer as the Architect's call, and it was | accepted |
| 397 | 2026-08-24 | Blocking, effort-branch. The new `### When no document home is configured` was placed mid-section, so it ran to the next `##` and swallowed six paragraphs that are not about an unconfigured home, including the unreachable-home rule whose premise is a home that is configured, and 242's commit-authority paragraph, which 242 put under that heading on purpose. It is also the only `###` in `skills/effort/`. The branch moves to the end of `## Document home`, which keeps the seam's literal heading string green | accepted |
| 396 | 2026-08-24 | 392's synced-vault caveat goes into this release's README as one factual sentence rather than waiting. A scope widening beyond the spec, taken because `setup` is what starts steering people toward vaults and 391 is direct evidence the duplication is real rather than theoretical. Stated as untested, not as a recommendation, since 392 is still open and the operator has not chosen a mitigation. Surfaced at gate 3 so it can be struck | accepted |
| 395 | 2026-08-24 | The two pre-existing em dashes at `README.md` line 9 are fixed rather than left. Per 241: the slice owns the file outright, so scope discipline that leaves them means no slice ever fixes them. The reviewer graded them `noted` because the diff did not add them; the house rule is unconditional | accepted |
| 394 | 2026-08-24 | Dismissed. The README compressing 361's three branches to two, omitting that both files existing makes `setup` ask, is right for a README. The skill carries the full rule and a reader who hits that case is inside `setup` when they do. Recorded because a dismissal needs its reason on the record as much as an action does | accepted |
| 393 | 2026-08-24 | Blocking, readme, on both axes at one sentence. `README.md` line 54 still called `effort` "the front door", which 377 made false, and `setup` was absent from the only skill table, so the README shipped with no link to `skills/setup/SKILL.md` anywhere. The sweep 338 demanded missed the single site outside the spec's bullet list, which is 338's own failure mode recurring | accepted |
| 392 | 2026-08-24 | Widens 222 from the vault to the scratch. The working copy sits under `~/Documents/`, which is iCloud-synced, and 391 is the mechanism firing on Capstan's own files: an external process writing while a sync service watches produces a second copy rather than a conflict anyone sees. A document home in a synced vault is the same path, and `setup` is about to steer people there. Untested, unmitigated, and the README says nothing | superseded by 759 |
| 391 | 2026-08-24 | 350 is wrong that the tracker effort's scratch was deleted cleanly. Sync-duplicated copies of its `CLAIM.md`, `spec.md` and `scout/` survived phase 4 as ` 2` variants and were sitting beside this effort's live spec when phase 3 dispatched. Moved aside rather than deleted, so nothing is lost. A stale spec from a closed effort next to a live one is the exact staleness phase 4 step 4 exists to prevent, and the delivery that claimed to prevent it did not | accepted |
| 390 | 2026-08-24 | Slice names in `tracker.md` are words rather than numbers, unlike the tracker effort's three rows above them. The tracker outlives the effort and answers what shipped, and `setup-skill` answers that where `slice2` does not. A convention change, logged rather than drifted into | accepted |
| 389 | 2026-08-24 | The `guard` slice gets a `tracker.md` row at `merged` carrying `16c799e`, even though it shipped before the plan that names it. A tracker that omits what shipped because the paperwork ran late cannot answer what shipped. Same shape as 336's bootstrap row | accepted |
| 388 | 2026-08-24 | `release`'s Builder edits one line and neither pushes nor runs `plugin update`. The marketplace clones from the remote, so the observable half of that slice needs a push, which is the operator's. Phase 3's verify runs it after the push, per 370 | accepted |
| 387 | 2026-08-24 | Four slices, three of them on the frontier. `effort-branch` and `readme` are not blocked by `setup-skill`: both can be written from the spec and both are verified by checks against their own file, so the constraint is merge order rather than a blocking edge, and declaring it one would idle two Builders. `release` is genuinely blocked, since its demonstration is the installed cache holding the new skill | accepted |
| 386 | 2026-08-24 | Supersedes 366. 2.13.0 ships the 385 guard alone, and `home-setup` takes 2.14.0 when it delivers. `plugin update` compares version numbers rather than content, so folding a fourth change into an installed 2.12.0 leaves it uninstallable, which is exactly what 276 warned the silent folding would cost | accepted |
| 385 | 2026-08-24 | `decision-record` gains `### What a line records`, the rule 382 was missing: a line about work done on a surface outside the working copy records what it taught the project, and leaves that surface's contents where they are. Folded into `home-setup` as a fourth slice rather than given its own effort, at the operator's call, since it is one section and the effort already touches adjacent payload | accepted |
| 384 | 2026-08-24 | All 76 unpushed commits carry the pre-redaction text in their `decisions.md` blob, so pushing publishes the nine rows 382 redacted even though the tip is clean. Git pushes history, not the current state. Keeping them off GitHub means rewriting or squashing that range before the next push, which is destructive and the operator's. Recorded because the Architect first reported the tip fix as sufficient, and it is not | superseded by 438 |
| 383 | 2026-08-24 | Four of the rows 382 redacted are already on `origin/main`, pushed 2026-08-22. Editing the working tree does not unpublish them: the history still carries them, and a force-push rewrite may not clear GitHub's unreferenced objects or any fork or cache. Destructive and the operator's alone, so nothing was done to history | superseded by 438 |
| 382 | 2026-08-24 | This log is published, so it records Capstan's decisions and never the contents of the operator's knowledge base. Fourteen rows naming vault internals, ventures, taxonomies, note counts and home directories are redacted in place, keeping each Capstan lesson and dropping the specifics. A factual repair per 348 rather than a decision change, so nothing is superseded and no number moves. Raised by the operator at gate 1 of `home-setup` | accepted |
| 381 | 2026-08-24 | `setup` carries `disable-model-invocation: true`, matching `effort`. Both are front doors per 377, and a model relocating someone's decision log on its own reading of a sentence is worse than the operator typing one command. Recorded rather than left to a Builder, per 374 | accepted |
| 380 | 2026-08-24 | Bookkeeping for 376. Decisions 353, 358, 361, 362, 363, 367, 372, 373 and 374 keep their substance and move into the `setup` skill, which is now the only place that creates a folder, checks the destination, moves the four artifacts, writes the key into `CLAUDE.md` or `AGENTS.md` and adds the `.gitignore` line. Recorded so the move does not quietly drop one, the way 317 had to close five | accepted |
| 379 | 2026-08-24 | Narrows 375. On a mismatch the `effort` skill reports what it found and points at `setup`, rather than performing a move mid-effort. The condition survives; the response moves. A re-runnable command is what 375 was working around | accepted |
| 378 | 2026-08-24 | The `effort` skill's unset branch is one short question with the default available, not a hard stop: use `.capstan/` here, or run `setup` to choose. Answering default writes the key per 352 and the run proceeds, so it never asks again. A hard stop is the cleaner rule and blocks someone who wants the default and wants to start working, which is not worth the purity | accepted |
| 377 | 2026-08-24 | Narrows 92. `effort` is no longer the only front door: `setup` is a second, operator-invoked. 92 settled how `spike` and `walkthrough` get reached, and those are Disciplines the model pulls in mid-work. Configuration a person performs once per project is not one, and 360 applied 92 to a case it never covered | accepted |
| 376 | 2026-08-24 | Supersedes 360. Setup is `setup`, its own operator-invoked skill, rather than a step inside `## Document home` in `effort/SKILL.md`. It can be re-run, which is what 375 was inventing a mismatch trigger to approximate; it asks before an effort rather than inside its round 1, which removes the ordering wrinkle where the log is read under the default before the home is chosen; and it gives the README an install story. Not a walkthrough, which is the one part of 360 that holds. Raised by the operator at gate 1, which is the turn gate 1 exists for | accepted |
| 375 | 2026-08-24 | Closes the half of 256 that 358 left. Setup also fires on a mismatch: the configured home holds none of the four artifacts while `<working copy>/.capstan/` holds some. Reuses 358's report-and-move and 372's collision check for one extra condition, with no change detection and no stored history. A half-finished move, where both sides hold some, does not fire and stays the stale copy 358 already warns about | accepted |
| 374 | 2026-08-24 | The key is appended as a bare line at the end of the file under no heading, matching the literal-key form 233 settled. A file Setup creates under 361 holds that line and one sentence saying what it is. Mechanical and low consequence, so recorded rather than asked, and recorded because a Builder with no rule here invents a placement | accepted |
| 373 | 2026-08-24 | Setup adds `.capstan/effort/` to `.gitignore` when it is missing. A widening of the operator's ask, put to them and approved. It prevents a committed spec, the staleness 63 and the phase 4 deletion both exist to stop, and README line 85 currently makes it a manual step people skip | accepted |
| 372 | 2026-08-24 | Setup reports what it finds and asks when the destination already holds any of the four artifacts. It never merges or overwrites silently. Two working copies sharing one home means one log numbered by two Architects, and `CLAIM.md` cannot catch it because the claim lives in the repository and cannot see the other one. The legitimate case, the same project after a repository move, is indistinguishable to Capstan and so belongs to the operator | accepted |
| 371 | 2026-08-24 | Phases 2 to 4 never re-ask. Where phase 1 left an `assumed` default they read that line and proceed, per 61, which settled the same shape: a line written to stop a question recurring has to be read by the thing that asks it. Recorded rather than put to the operator, because the record already answers it | accepted |
| 370 | 2026-08-24 | 229's approach carries: `claude plugin validate` plus a per-slice grep assertion red at its base commit. This effort has a sharper version of 229's gap, since what ships is an agent asking a question at runtime and no grep observes that. Verification adds one observed check, the `effort` skill pointed at a scratch repository with no key set, per 216. The spec states the limit rather than letting a green validate imply more than it covers | accepted |
| 369 | 2026-08-24 | The three layouts are described, not coined: "in the repository", "one vault per project", "one folder per project in a shared vault". They appear in README prose and the Setup question and nowhere else. Short nouns would become vocabulary needing glossary rows for a distinction 354 says Capstan stores nowhere | accepted |
| 368 | 2026-08-24 | Setup asks projects already running on the default too, rather than skipping and recording `assumed`. Every 2.12.0 user has an unset key, so skipping would mean the one group with real files in `.capstan/` is the group Capstan silently decides for. They answer in one word and are never asked again | accepted |
| 367 | 2026-08-24 | Setup asks in two steps rather than offering a three-way menu. The fork first, `.capstan/` here or a folder outside the repository; the two vault layouts and the path only if they answer outside. Someone who wants the default should not read two paragraphs about vault layouts to get there | accepted |
| 366 | 2026-08-24 | Version 2.13.0, shipped inside this effort per 7. 276 notes 2.12.0 already silently covers three efforts; this one changes what happens on a user's first run, which is the most visible change since the namespace moved | superseded by 386 |
| 365 | 2026-08-24 | Whether the knowledge-base location becomes a literal key alongside `capstan-document-home`. The Courier reads it as prose from the same two files. Out of scope here per 207, and the inconsistency gets more visible the moment Setup starts writing a literal key | superseded by 632 |
| 364 | 2026-08-24 | The README's `## Document home` names three layouts with one line each on who each suits, replacing the prohibition 351 removed. The scratch sentence from 357 moves above the choice rather than below it, where it is read after the decision is already made | accepted |
| 363 | 2026-08-24 | Capstan commits the removal of the four artifacts from the working copy and never touches the vault side, which the operator commits. The Authority table already grants the crew changes to the working copy, and 230's never-run-git rule governs the home rather than the repository. The alternative, Capstan commits nothing, leaves an uncommitted deletion in someone's repository | accepted |
| 362 | 2026-08-24 | The move covers `CONTEXT.md`, `decisions.md`, `decisions/` and `tracker.md`. `.capstan/effort/` stays per 357, and `.capstan/` itself stays because the scratch lives there and `.gitignore` points at it | accepted |
| 361 | 2026-08-24 | Setup writes the key into whichever of `CLAUDE.md` and `AGENTS.md` exists. Both existing means it asks, because 239 stops the phase when they disagree and Setup must not create that state. Neither existing means it creates `AGENTS.md`, which Claude Code and Codex both read and which 234 already treats as equal to `CLAUDE.md` | accepted |
| 360 | 2026-08-24 | Supersedes 329. Setup is a step in `## Document home` in `effort/SKILL.md`, not a walkthrough and not a second front door: a walkthrough is for what only a person's hands can do, and 92 makes `effort` the only front door. It adds no stopping point. When the key is unset the choice rides the interview's round 1 and the gates stay at three. Unanswered falls to the default and records `assumed`, per the interview's own stall route | superseded by 376 |
| 359 | 2026-08-24 | Closes 324. Its revisit condition fired: the tracker ran a real effort and 350 records its rows surviving delivery. 318 through 323 are confirmed rather than provisional, and 321 in particular, which this effort leans on | accepted |
| 358 | 2026-08-24 | Settles 256. Setup reports which document-home files already exist under the default and asks. The operator approves the move and Capstan performs it, or nothing moves and setup says a stale copy remains. 256 called this the gap an operator hits first, and the prompt this effort adds is what makes them hit it | accepted |
| 357 | 2026-08-24 | The effort scratch stays at `<working copy>/.capstan/effort/` under every configuration. 208 stands: a vault filling with dead specs is worse than a gitignored folder phase 4 deletes, and the scratch never reaches git history. The README says this where the choice is made rather than after it | accepted |
| 356 | 2026-08-24 | Whether one repository can carry a document home per sub-project. A monorepo has one `CLAUDE.md` and 234 reads only the working copy root, so frontend and backend in one repository cannot split their logs today. Separable as rows in one tracker via the Effort column, which may be enough | open |
| 355 | 2026-08-24 | One working copy, one document home. Restates 213: two roots for one codebase is two logs and two glossaries to keep in sync. Two repositories already get two homes with no new work. The monorepo case is 356 | accepted |
| 354 | 2026-08-24 | The chosen layout is not stored beyond the path and a line of prose. No Capstan behaviour differs between a dedicated vault and a folder inside a shared one: both are configured away from the default, which already means write the files and never run git. 329 refused a setting nothing reads and this is the same shape | accepted |
| 353 | 2026-08-24 | Setup creates a document home folder that does not exist, after the operator confirms the path. An unreachable configured root stops the phase, and a project folder someone is naming for the first time inside a vault is unreachable by definition, so without this every vault choice fails on its first run | accepted |
| 352 | 2026-08-24 | Setup writes `capstan-document-home` in every case, including when the operator picks the default. Otherwise never-asked and asked-chose-default are the same state on disk, so the prompt fires every run or Capstan assumes. Assuming is what this effort exists to stop | accepted |
| 351 | 2026-08-24 | Supersedes 224. One vault per project is a supported layout. Nothing ever enforced 224: the setting has always been a bare absolute path, so the constraint lived only in README prose, which becomes a recommendation. Operators who keep projects fully separate are the case 224 rejected and they are entitled to it | accepted |
| 350 | 2026-08-24 | The tracker survived its own delivery. Phase 4 step 4 deleted `.capstan/effort/` with the spec, the plan and five filed returns, and `tracker.md` is still there carrying three merged rows. The rule slice 2 wrote was tested by the first delivery to run under it | superseded by 391 |
| 349 | 2026-08-24 | The note's `## Still open` must carry 343, the only row this effort left open, and its `## What was decided` must state the commit rule as 340 narrowed it rather than as the spec first wrote it. A permanent record carrying a superseded rule in the section that says what was settled is the staleness this repository keeps naming | accepted |
| 348 | 2026-08-24 | Decisions 317 to 347 were logged with the wrong date. The session crossed midnight and the Architect kept writing 2026-08-23 while every commit landed on the 24th. Thirty-one rows corrected in place, which is a factual repair rather than a decision change, so nothing is superseded. Caught by the note review, not by the Architect | accepted |
| 347 | 2026-08-24 | Verification is green and degraded, and says which. One executable check, and it validates a manifest, so six seams and four whole-repository sweeps are the evidence. The real evidence is not a check: `tracker.md` holds three merged rows written under the rules while those rules were being added | accepted |
| 346 | 2026-08-24 | Slice 2 merges after one review and two fix rounds, verified by the Architect. The blocking finding was a missing location at three named sites and is checkable by reading; the second round was one clause the Architect had left out of the first brief | accepted |
| 345 | 2026-08-24 | Slice 3 merges after one review and one fix round, verified by the Architect. Three blocking findings, all incomplete-sweep or false-claim, all mechanical and readable in the diff. Same call as 257 | accepted |
| 344 | 2026-08-24 | The contraction finding rested on a false premise: `README.md` carries `you've` at line 9, so it was not the only one. The change to "it is" stands rather than being reverted, since both read fine and churn is worse. Recorded because the record should say why a finding was actioned | accepted |
| 343 | 2026-08-24 | `durable artifact` is a counted noun with no glossary row, now hard-coded as "four" across two files. The next artifact makes it five and nothing says what qualifies, which is how the three-becomes-four sweep happens again. Raised by the slice 3 review | open |
| 342 | 2026-08-24 | `cut` is dropped as the verb for removing a slice, in favour of `drop`, matching the status name. Phase 2 says "cut the work into vertical slices", meaning divide, and phase 3 said "a slice you cut", meaning remove; the same Architect reads both in one run. Raised by the slice 2 review as a vocabulary gap and left out of the fix brief by the Architect | accepted |
| 341 | 2026-08-24 | `dropped` covers a slice cut at any point in phase 3, not only instead of dispatch. The Builder filled a gap the spec and plan both left, reasonably, but too narrow for the criterion beside it: a slice dispatched and then cut had no legal transition and the done-when would have forced a false `merged` | accepted |
| 340 | 2026-08-24 | The spec's blanket "ride existing commits" is wrong at two of the three moves. Phase 2 writes only gitignored scratch and instructs no commit; phase 3 makes none before the merge. The rule binds at merge; elsewhere the row lands with the next commit the crew makes to the document home. The Architect's spec line, not the Builder's reading | accepted |
| 339 | 2026-08-24 | Both write sites name the document home. Every other durable artifact resolves its location at every mention in those same files, three times in the one under review. At a configured home phase 2 is the first write and an Architect with no path forks the artifact into the working copy | accepted |
| 338 | 2026-08-24 | The plan's Slice 3 paragraph is rewritten to state a file-level goal rather than a list of rows. A row-by-row brief cannot cover a sweep: it missed three sites in files the slice owns outright and the Builder, correctly following it, reported only the one it was positioned to see. The Architect's defect, the third this effort traced to a dispatch brief | accepted |
| 337 | 2026-08-24 | `README.md` claimed the tracker is the one file in `.capstan/` that outlives an effort. False: the glossary, the log and the records all do. The spec makes the tracker their peer and contrasts all four against the scratch | accepted |
| 336 | 2026-08-24 | The tracker cannot open its own first slice's row at phase 2, because the rule that says to did not exist yet. `tracker.md` is created after slice 1 merges, carrying slice 1 at `merged` and slices 2 and 3 at `planned`. A one-time bootstrap, not a precedent: every later effort opens rows at phase 2 per 325 | accepted |
| 335 | 2026-08-24 | The `tracker.md` example uses this effort's own name and slice 1's real commit hash. `skills/` is payload running in a user's repository, where that history does not exist, which is the reasoning behind 94 and the no-claims-about-this-repository rule. `decision-record`'s blocks use invented domain examples and this one should too | accepted |
| 334 | 2026-08-24 | The section names the Tracker as the surface and `tracker.md` as the default one. 318 rests on that distinction and the deferred Obsidian effort needs it; without it "the default" is a dangling comparative in a file naming no alternative. The wording came from the spec, so it is the Architect's defect | accepted |
| 333 | 2026-08-24 | `skills/effort/SKILL.md`'s Architect line gains the tracker, and the glossary's `Architect` row joins slice 3's scope, which the plan is amended to say. Both carry the same three-artifact enumeration. Ownership is not timing, so the skill line stays slice 1's | accepted |
| 332 | 2026-08-24 | The "legibility is the discipline" clause is deleted. It states no rule, and it names a Discipline no skill in `skills/` provides while the glossary defines the word | accepted |
| 331 | 2026-08-24 | The commit rule gains its configured-home half where an Architect updating a status reads it. 242 settled this exact scattering in this exact file and the new section reintroduced it: at a configured home there is no phase commit and the operator commits | accepted |
| 330 | 2026-08-24 | `tracker.md` is shown as a literal fenced block, not described in prose. Every other durable artifact in this repository is specified by showing it, and an append-only file whose value is being one scannable table cannot survive two Architects inventing two header rows. This also closes the unclosed field set, since a shown header row closes it | accepted |
| 329 | 2026-08-24 | Supersedes 322. The setup walkthrough is deferred with the Obsidian surface rather than built here. 322 and 323 contradicted each other: a walkthrough writing a setting no code reads is machinery for its own sake. Deferred, not cancelled, the same shape as 228 | superseded by 360 |
| 328 | 2026-08-24 | `CLAIM.md` keeps its `next` line. A status column covers part of what it carried, but not the prose about what is outstanding and which findings were dismissed | accepted |
| 327 | 2026-08-24 | Four statuses: `planned`, `building`, `merged`, `dropped`. Closed and kept, per 209 | accepted |
| 326 | 2026-08-24 | `tracker.md` and `plan.md` are not two sources of truth. `plan.md` holds Owns, Demonstrated, Seam, Red at base and Blocked by, is authoritative during the run and dies at delivery; `tracker.md` holds effort, slice, status and merge commit and outlives it. Only the slice name overlaps | accepted |
| 325 | 2026-08-24 | A row appears at phase 2 when the plan is written, carrying `planned`, and moves as the slice does. Not at merge: 201 puts work stalling between gates in scope, and a row that appears only on merge cannot show anything in flight | accepted |
| 324 | 2026-08-24 | The operator accepted the whole recommended set in round 1 provisionally, to be revisited once the tracker has run on a real effort. This is the revisit condition for 318 through 323 | superseded by 359 |
| 323 | 2026-08-24 | This effort builds the default tracker and stops. The Obsidian surface is the next effort. 203 already holds that one adapter is a hypothetical seam, and a second surface means nothing until the first exists and has been used | accepted |
| 322 | 2026-08-24 | Setup is a `walkthrough`. It exists for operator-driven procedures, is discarded once run, and the `effort` skill rules out anything needing code to stay alive | superseded by 329 |
| 321 | 2026-08-24 | The tracker follows the document home: one root, one setting. Two settings is two things to keep in sync, which 213 refused for the same reason. Restates 207, whose protection disappears once tracking is always on | superseded by 651 |
| 320 | 2026-08-24 | One row per slice, carrying the effort, the slice, its status and the commit that merged it. Not one row per effort: 209 wants "what shipped" answerable and slices are what ship | accepted |
| 319 | 2026-08-24 | The durable artifact is `tracker.md`, a fourth alongside the glossary, the log and the records, one table, relocating with the document home. 223's reasoning for one scannable file applies unchanged | accepted |
| 318 | 2026-08-24 | Supersedes 200. Tracking is always on and configuration picks the surface, rather than a tracker being absent until configured. The `Tracker` glossary row loses "authoritative when configured" | accepted |
| 317 | 2026-08-24 | Bookkeeping. 259, 260 and 264 were delivered by `scratch-shape` and 265 and 316 were settled by 315 and the worktree removal, but all five stayed `open` in the log. Closed here so the next interview does not put settled questions to the operator. 266 stays open pending a check the operator runs | accepted |
| 316 | 2026-08-23 | A stale agent worktree in the knowledge base held a full second copy of every note, registered with git. It doubled every path count during the tag investigation and is the likely cause of a partial revert. No further knowledge-base writes until it is resolved | superseded by 317 |
| 315 | 2026-08-23 | Supersedes 119 and settles 146. The routing reference is correct and the Capstan notes are the defect. Tag conventions are per-folder and differ across a knowledge base: some folders tag flat, others hierarchically, and the append rule is scoped to the ones that do. 119 read one folder's convention as another's and invented a tag matching neither. The Courier reads the folder it is writing into rather than generalising from one example | accepted |
| 314 | 2026-08-23 | Settles 266 and supersedes 144. The notes with the wrong status are corrected at the operator's instruction. 144 deferred them as the operator's call rather than judging them correct, and that call has now been made. Not verified: the knowledge base's own health check could not be re-run, so its zero-error bar is expected rather than observed | accepted |
| 313 | 2026-08-23 | Settles 311. The Capstan notes move to the correct branch of the knowledge base's taxonomy: the tag was wrong rather than merely imprecise. No child node invented, because creating a taxonomy node in someone's knowledge base is the operator's and never the Courier's | accepted |
| 312 | 2026-08-23 | The Architect's own check on the note fix flagged two correct sentences: a rule naming where returns go must name the path. Fourth string-proxy misfire in this effort after 290 and 292, and the first by the Architect while verifying a fix rather than while writing a criterion. The pattern is now recorded three times and should be designed out rather than logged a fourth | accepted |
| 311 | 2026-08-23 | A taxonomy tag was copied unchecked across every Capstan note. Closer branches exist. The operator's to settle, and it settles every one of those notes at once | superseded by 313 |
| 310 | 2026-08-23 | The note cannot cite `.capstan/effort/review/` as evidence, because phase 4 deletes that directory in the same close-out. It cites the adjudication in the log instead, which survives. The strongest evidence this effort had was sitting in the one place delivery erases | accepted |
| 309 | 2026-08-23 | The note restores the four headings every neighbour uses, including `## Still open`, which it dropped along with seven unsettled decisions. That section is how an open question survives the effort that raised it: this effort's own spec came from two lines the previous note carried there | accepted |
| 308 | 2026-08-23 | The counts in the gate 3 brief and the Courier dispatch were the Architect's and were wrong: 28 graded findings rather than 26, three Architect-brief defects rather than four, two defective seam criteria rather than one. 196 governs, since the proportion was the argument. The fifth count drift this repository has recorded | accepted |
| 307 | 2026-08-23 | Verification is green and degraded, and says which. The repository declares one executable check and it validates a manifest, so the ten seams and three whole-repository sweeps are the evidence. The strongest evidence is not a check: this effort ran under its own new rules while building them, and eight returns are filed rather than lost | accepted |
| 306 | 2026-08-23 | Slice 4 merges after one review and one fix round, verified by the Architect. Seven specific findings, all actioned, output the Architect can judge by reading. Same call as 257 | accepted |
| 305 | 2026-08-23 | Slices 2 and 3 merge after two reviews and two fix rounds each, verified by the Architect rather than a third review. Both round-2 Spec axes were clean and every remaining finding was `noted`. Slice 2's sub-bullet move to step 4 was the Builder's judgement call and it picked the phase 1 shape, which resolved the forward-reference without a reword | accepted |
| 304 | 2026-08-23 | Slice 1 merges after two reviews and two fix rounds, without a third review. Round 2's Spec axis was clean, both Standards findings were `noted`, and the three fixes were specific enough for the Architect to verify directly. Same call as 252 and 257 | accepted |
| 303 | 2026-08-23 | The knowledge-base note is called three things across the files this effort touched: the knowledge-base note, the permanent note, the permanent record. A glossary row names it; the three sites are not rewritten here | open |
| 302 | 2026-08-23 | `DESIGN.md`'s `Never` column gains "Commits the record." 286 declined the same clause in agent frontmatter on model-cost grounds, and that does not transfer: `DESIGN.md` is human-facing, costs nothing per turn, and its `Never` column exists for exactly the thing a reader would otherwise assume | accepted |
| 301 | 2026-08-23 | `skills/slicing/SKILL.md` gains one sentence telling the Architect to name the files each slice owns. Its done-when now demands `Owns` and the skill's body never asks for it, so an agent invoking `slicing` alone cannot tell done from not-done | accepted |
| 300 | 2026-08-23 | The `Owns` glossary row is reworded to carry coverage, not only partition. The Architect supplied the wording and it named collision as the sole rationale, but 287's defect was that five files sat in nobody's partition. A row that only forbids overlap would have the next Architect cut the next plan exactly the way this one was cut | accepted |
| 299 | 2026-08-23 | `plan.md` gains its Slice 4 entry and its Graph gains a fourth node. 287 added the slice and nobody amended the plan, so the effort specifying what a plan must contain ran a slice absent from its own. Found by the slice 4 review | accepted |
| 298 | 2026-08-23 | Phase 1's done-when names the six sections rather than counting them. Phase 2's naming was the model, and a count is satisfiable by counting headings without reading them | accepted |
| 297 | 2026-08-23 | `skills/effort/SKILL.md` gains one clause where the knowledge base is already the subject. Its Authority table grants the crew `commit to a branch, push a branch` unattended and carves out only a configured document home, so the file read alone permits the commit 211, 275 and 278 forbid | accepted |
| 296 | 2026-08-23 | The role-table row in `SKILL.md` loses `committed` and does not gain the operator in its place. The `For` column discriminates at dispatch, and who runs git after the note is written is not a dispatch fact. Same reasoning as 286. Recorded because 289 covers only the glossary's twin of this row | accepted |
| 295 | 2026-08-23 | Scratch paths are written bare (`.capstan/effort/...`) throughout the phase files while the Precondition forbids assuming the session's working directory. Repository-wide and older than this effort, raised by the slice 2 review. Out of scope here | superseded by 473 |
| 294 | 2026-08-23 | The filing rule bolds the actor rather than the rationale, and `agents/reviewer.md` names the Architect as the filer. Phase 1 bolds **You** because who files is the load-bearing fact; a passive "is filed" hands a Reviewer a belief and the means to act on it with no path to act on | accepted |
| 293 | 2026-08-23 | "A Reviewer has no write tools" is false: `agents/reviewer.md` grants `Read, Bash, Skill` and Bash writes. The Scout it was mirrored from grants `Read, WebSearch, WebFetch` and genuinely cannot. The Architect's dispatch brief told the Builder to carry the reason across and the reason does not transfer. Second defect this effort has traced to a brief rather than a Builder, after 285 | accepted |
| 292 | 2026-08-23 | Slice 3's seam is amended to forbid the conditional rather than the phrase. `fixed point` naming an absence is correct; naming a branch was the defect. Same shape as 253 and 290, and the third criterion in this effort that graded a string instead of a meaning | accepted |
| 291 | 2026-08-23 | Phase 4 says plainly that the note review has no fixed point and is degraded, rather than leaving it to a hop through `two-axis-review`. The Architect wrote that sentence into a dispatch by hand this afternoon because no file told it to, which is the rule-living-in-someone's-head defect this effort exists to fix | accepted |
| 290 | 2026-08-23 | Slice 4's `grep -c 'commits it'` criterion was defective: the phrase appears in the corrected sentence too, so a right answer stayed red and the Builder reworded around the string rather than toward the meaning. The criterion's defect, not the Builder's, the same finding as 253 and 258. Three efforts running, three bad greps; a criterion whose target phrase can appear in the fixed text is the recurring shape | accepted |
| 289 | 2026-08-23 | The glossary's `Courier` row drops "commits it" and its `Seam` row moves the agreement from the spec to the plan, per 268. `Owns` and `Red at base` get rows: both are now required, named parts of a plan and neither was defined. Returned as a vocabulary gap by the slice 1 review | accepted |
| 288 | 2026-08-23 | Phase 1's done-when gains the six sections, and phase 3's gains the filing of returns. Both were stated in a step and enforced by no criterion, which is this effort's own defect one level up. Raised independently by the slice 1 and slice 2 reviews | accepted |
| 287 | 2026-08-23 | Slice 4 is added: the sweep. The plan applied "no hunk left behind" within files and never across them, so seven hunks in five files nobody owned survived the cut. `agents/builder.md` is the sharpest, being the last file contradicting 268 and the one a Builder reads at dispatch | accepted |
| 286 | 2026-08-23 | The widening of `agents/courier.md`'s frontmatter description was correct, recorded here because the repository's habit is that a widening gets its own line. The added prohibition is reverted: the body carries the rule, and an always-loaded description that discriminates no role is cost without routing value | accepted |
| 285 | 2026-08-23 | The read-only git carve-out in `agents/courier.md` is deleted. It reinstated the determination 275 removed, in softer costume: `git log` only means anything once someone has worked out whether the knowledge base is a repository. The instruction was in the Architect's dispatch brief, not the Builder's invention | accepted |
| 284 | 2026-08-23 | All three slices reviewed in one wave: 4 findings on slice 1, 4 on slice 2, 6 on slice 3 counting the unowned hunks. Every one actioned, none dismissed. Two were blocking and both were the same paragraph | accepted |
| 283 | 2026-08-23 | `skills/effort/SKILL.md` goes wholly to slice 1, including the Files-tree annotation that is slice 2's concern. 241's precedent: scope discipline that leaves one hunk behind means no slice ever fixes it, and two Builders in one file is an ownership question review will raise even when git merges it cleanly | accepted |
| 282 | 2026-08-23 | Supersedes 281. Slicing found three demonstrable cuts inside the one effort, so the three defects stay one effort. The cut fans out three ways with no blocking edges, which the document home could not do | accepted |
| 281 | 2026-08-23 | The three defects are one effort rather than three. They share a cause, the scratch having no specified shape, and splitting them means three claims for prose edits to the same four files. Revisit if slicing cannot find a demonstrable cut | superseded by 282 |
| 280 | 2026-08-23 | Specifying sections changes what an agent writes. Untested, and the same class of gap as 229. Revisit if a later effort produces a spec that ignores the sections | assumed |
| 279 | 2026-08-23 | `agents/reviewer.md` is told its return is filed verbatim and read later without the session around it. A return written to be read aloud and one written to be filed are different documents, and saying so costs a sentence | accepted |
| 278 | 2026-08-23 | The operator commits the knowledge-base note once, after the note review passes. Phase 4's second-commit correction path and its fixed-point language go: with 275 in force the note review is permanently the degraded kind | accepted |
| 277 | 2026-08-23 | Sections are specified as a name plus one line saying what the section must answer. No worked example, which gets copied verbatim rather than read, and no bare heading list, which is how a section becomes a heading with nothing under it | accepted |
| 276 | 2026-08-23 | No version bump. This folds into the unreleased 2.12.0 per 7 and 197. Logged because the document home folded in without a line, so the number silently covers three efforts and nothing but a tag will separate them | accepted |
| 275 | 2026-08-23 | Capstan never runs git in the knowledge base, whatever it is. The operator commits. One rule beats a vault-detection step ahead of two, and the degraded note review that 263 forced ran cleanly | accepted |
| 274 | 2026-08-23 | Scope widened by one file: verify findings are filed too, as `review/verify-<n>.md`. Same defect as 260, and 258 is a verify finding that reshaped every acceptance check in an effort and survived only because it was copied into the log by hand | accepted |
| 273 | 2026-08-23 | `plan.md` requires, per slice, Owns, Demonstrated, Seam, Red at base and Blocked by, plus a Graph and a preamble saying why the cut is this shape. The preamble earns its place: the document-home plan used it to say the work does not fan out and that this was correct rather than a failure to parallelise | accepted |
| 272 | 2026-08-23 | `spec.md` requires Problem, What is being built, What it must do, Explicitly out of scope, Test seams and Assumptions. A spike section appears only when there was a spike, since most efforts have none and 271 forbids a section nobody can fill | accepted |
| 271 | 2026-08-23 | The shape carries no frontmatter, no schema, no validation, and no section whose emptiness is an error. A spec with nine headings and three of them reading N/A is worse than the prose it replaced | accepted |
| 270 | 2026-08-23 | A review file holds the Reviewer's return and nothing else. Actioning or dismissing a finding is written in `decisions.md`, where it already goes. One fact, one place | accepted |
| 269 | 2026-08-23 | The Architect writes each Reviewer's return verbatim to `.capstan/effort/review/<slice>-<n>.md`, mirroring the Scout rule phase 1 already carries. The counter is there because one slice has taken nine rounds | accepted |
| 268 | 2026-08-23 | The seam is split rather than placed. `spec.md` states what checks the repository declares at all; `plan.md` states each slice's seam and its red-at-base evidence. This is what the document-home effort did in practice, and it resolves the contradiction in phase 2 between step 4 and its done-when | accepted |
| 267 | 2026-08-23 | The shape of `spec.md` and `plan.md` is required sections in the phase prose, not template files to copy. 250 established a subagent cannot reach a file in the plugin version cache, and a file to keep alive is what the skill's closing section refuses | accepted |
| 266 | 2026-08-23 | A Courier told to satisfy a knowledge base's own health check can be pushed into editing notes an earlier decision forbids it to touch. Surfaced to the operator, because the health rule lives in their configuration rather than in Capstan. Redacted per 382; the specifics were theirs | open |
| 265 | 2026-08-23 | 146 bit a seventh delivery. The routing reference still contradicts 119 and claims precedence over an agent prompt, so every Courier run re-decides it in silence. It stays open because the fix is in the operator's configuration, not in Capstan | superseded by 317 |
| 264 | 2026-08-23 | `agents/courier.md` step 3 and `PHASE-4-DELIVER.md` step 2 still instruct a commit in the knowledge base and a reported SHA, which 211 forbids. Found while dispatching phase 4, after gate 3, so it is corrected in the effort 261 opens rather than by reopening a verified build | superseded by 317 |
| 263 | 2026-08-23 | The Courier does not commit in the vault. 211 is unqualified about the vault, and the knowledge base is that vault, whose `AGENTS.md` rule 7 forbids an automatic commit. The note review runs degraded against the note's absolute path, which is the branch phase 4 already defines for a knowledge base with no repository | accepted |
| 262 | 2026-08-23 | The vault layout inside a Project folder waits for the tracker effort 228 defers to, and is designed there alongside the feature that reads it. A layout nothing consumes goes stale before it ships | accepted |
| 261 | 2026-08-23 | 259 and 260 ship as one small effort of their own, opened once this one closes. Both are edits to `skills/effort/`, and neither belongs in an effort that is one run from delivery | accepted |
| 260 | 2026-08-23 | `review/` is named in the Files tree and nothing ever writes to it. A Reviewer has no write tools, and no phase file tells the Architect to file its findings the way phase 1 does for a Scout return. Review output has been living in context and dying with the run | superseded by 317 |
| 259 | 2026-08-23 | `spec.md` and `plan.md` have no specified shape, while Builders and Reviewers are dispatched to be graded against them. Every run invents the structure, and phase 2 already contradicts itself on whether the test seam lands in the spec or the plan | superseded by 317 |
| 258 | 2026-08-23 | `verify`'s first real use found the seam check brittle rather than the code: `grep -r .` emits paths without a leading `./` here, so a path exclusion silently matched nothing and reported five phantom failures. Every acceptance check in this effort leans on greps | accepted |
| 257 | 2026-08-23 | Slice 3 merges after one review and one fix. The blocking finding was specific and verifiable, and its output is prose the Architect can judge directly | accepted |
| 256 | 2026-08-23 | Whether Capstan should offer a migration for a project switching its document home. Raised by the slice 3 review, out of scope here, and the gap an operator hits first | superseded by 358 |
| 255 | 2026-08-23 | Capstan resolves paths and never moves files. The README says so plainly, and says switching an existing project is the operator's job | accepted |
| 254 | 2026-08-23 | The five references inside `decisions.md` and record 0003 are accepted residuals. Historical text quoting the path as it stood is a record, not a live pointer, the same reasoning as 226 | accepted |
| 253 | 2026-08-23 | The sweep allows markdown link targets in README and DESIGN. The seam matched hrefs and drove out two working links, which was the criterion's defect rather than the Builder's | accepted |
| 252 | 2026-08-23 | Slice 2 merges after two reviews and two fixes, without a third. Both findings were the same failure class and it is now closed by construction, verified across all twelve files. The duplication a third review would raise is settled as 249 | accepted |
| 251 | 2026-08-23 | `the vault-wide typing rule` is reworded rather than glossed. A phrase used twice that names a rule nobody can look up is not vocabulary, and the prefix is visible in the templates beside it | accepted |
| 250 | 2026-08-23 | The Architect hands the resolved document home to every Builder and Reviewer at dispatch, the way it already hands scratch paths. A subagent cannot reach a skill file that lives in a plugin version cache | accepted |
| 249 | 2026-08-23 | Every file naming a durable artifact names its filename and its default alongside the document home. Sharpening the pointer failed, so `writing-for-agents` licenses inlining the material, minimally | accepted |
| 248 | 2026-08-23 | `per the effort skill` is an inert pointer: `effort` carries `disable-model-invocation`, so no skill or agent can invoke it. The Architect specified that remedy in the fix brief and it was wrong | accepted |
| 247 | 2026-08-23 | `spec.md`'s out-of-scope line calling frontmatter a phase-2 question is stale, superseded by 227 which settled it. Corrected in place: the spec was written at gate 1 before that decision existed | accepted |
| 246 | 2026-08-23 | `capstan_type` is applied to this repository's own three durable artifacts, added to slice 3. 227 says one shape whether relocated or not, and a schema nothing follows is the staleness this effort exists to fix | accepted |
| 245 | 2026-08-23 | `<working copy>` in the default document home means the effort's main working copy, never a Builder's worktree. Restores what `agents/builder.md` lost when it dropped the main-copy qualifier | accepted |
| 244 | 2026-08-23 | Every file naming the document home carries a pointer to where the term resolves. Ten of twelve lacked one, three of those being subagent prompts that never load `effort/SKILL.md`. A pointer is not a restatement of 231 | accepted |
| 243 | 2026-08-23 | `capstan_type` is the property naming which durable artifact a note is, values `glossary`, `decision-log`, `decision-record`. Returned as a vocabulary gap by the slice 2 Builder and settled here rather than left invented | accepted |
| 242 | 2026-08-23 | The default's committer is named under the `Document home` heading rather than only in the Authority table. Half a concept's commit rule living two sections away is the scattering `writing-for-agents` warns about | accepted |
| 241 | 2026-08-23 | `<repo>` is dropped from all of `effort/SKILL.md`, including the git examples outside the diff hunks. Slice 1 owns the file outright, so scope discipline that leaves them means no slice ever fixes them | accepted |
| 240 | 2026-08-23 | A `capstan-document-home` value that is not an absolute path stops the phase. Resolving a relative one would mean assuming the session's working directory, which the Precondition section forbids | accepted |
| 239 | 2026-08-23 | `CLAUDE.md` and `AGENTS.md` carrying different values stops the phase. Same family as an unreachable root: a missing source of truth, not ambiguity. Precedence was the alternative and is rejected, since a silently wrong root is the split the section exists to prevent | accepted |
| 238 | 2026-08-23 | The ladder finding is dismissed. Pushing the four configured-only items behind a pointer costs a file and a pointer to save a dozen lines in a section every run already reads most of | accepted |
| 237 | 2026-08-23 | `<working copy>` is the term; `<repo>` is dropped wherever this effort touches. Two spellings of one thing, and the glossary names neither | accepted |
| 236 | 2026-08-23 | 210 was written about the Tracker during the interview and the Architect carried it into the document-home spec. It does not govern this feature. Scope corrected rather than the Builder blamed for widening it | accepted |
| 235 | 2026-08-23 | A missing default document home is normal, not a stop. `.capstan/` does not exist on a repository's first effort and is created on first write. The stop applies only to a root configured away from the default | accepted |
| 234 | 2026-08-23 | Only `<working copy>/CLAUDE.md` and `<working copy>/AGENTS.md` are read for the setting. A user-level file covering every project is a later question, and the Precondition section forbids assuming the session's directory | accepted |
| 233 | 2026-08-23 | The configuration entry is a literal key, `capstan-document-home:`, holding an absolute path, documented with an example. The courier's prose-only convention is not enough here: its unset branch asks a human, this one falls through to a silent default | accepted |
| 232 | 2026-08-23 | Slice 1 review: two blocking findings and six lesser, all actioned, none dismissed. Sent back as a fresh Builder task on the same slice, never to the instance that wrote it | accepted |
| 231 | 2026-08-23 | The act of reading the configured root belongs to slice 1, not slice 2. The plan gives slice 1 the mechanism, and 25 references in slice 2 would otherwise inherit a term that never resolves to a path | accepted |
| 230 | 2026-08-23 | The never-run-git rule is scoped to a document home configured away from the default. 211 is vault-scoped and does not license an unqualified rule: in the default the crew commits the log, per the Authority table | accepted |
| 229 | 2026-08-23 | No new test seam. The only executable check this repository declares is `claude plugin validate`, so each slice is graded on that plus a grep assertion that is red at its base commit. Nothing verifies that an agent resolves the root correctly, which needs evals Capstan does not have | accepted |
| 228 | 2026-08-23 | 202's scaffolding belongs to the tracker effort. Scaffolding a Project is project management, and this effort moves documents. The spec's `no scaffolding command` means deferred, not cancelled, and is corrected to say so | accepted |
| 227 | 2026-08-23 | The three durable artifacts carry minimal `capstan_` frontmatter whether relocated or not. One shape beats two, and it buys back the cross-project query 223 gave up for the log | accepted |
| 226 | 2026-08-23 | `CREDIT.md` files keep their literal `.capstan/` references. They record what was repointed at vendoring time, and rewriting them falsifies the credit | accepted |
| 225 | 2026-08-23 | Adoption is documentation only in this effort. The pitch is that decisions live somewhere readable, which suits the people it suits. Anything more ambitious is a separate effort | accepted |
| 224 | 2026-08-23 | One vault, one folder per Project. Three vaults means three windows and no view across them, which discards most of the reason to leave the repository | superseded by 351 |
| 223 | 2026-08-23 | The log stays one file in a vault. Scanning every decision on one screen is its value, and one note per decision is the bloat `decision-record` exists to prevent. The cost: in a vault the log is a document, not queryable data | accepted |
| 222 | 2026-08-23 | The spike covered a local vault on one machine. A vault behind iCloud, Dropbox or Obsidian Sync is untested, and so is Capstan writing a note while the operator is typing in it. Assumed safe; revisit before anyone points this at a synced vault | superseded by 762 |
| 221 | 2026-08-23 | Obsidian reflects external writes instantly: a new note appears without a restart, an open tab updates live, a reopen shows outside edits, and a delete vanishes. Supersedes 205. The forum evidence the scout found does not hold here | accepted |
| 220 | 2026-08-23 | The 205 spike is a `walkthrough`, not a Builder task. Every observation in it is visual and inside Obsidian, so the operator runs it and the run pauses mid-phase rather than at a gate | accepted |
| 219 | 2026-08-23 | `Project` gets a glossary row, overturning 114. The operator answered before the route in 217 took effect, so 217 is superseded too: the word is doing real work and this effort is named after it | accepted |
| 218 | 2026-08-23 | `Document home` and `Tracker` get glossary rows now. Writing vocabulary down as it resolves is the Architect's job rather than a question, and asking for permission was an over-ask | accepted |
| 217 | 2026-08-23 | 114 is routed rather than asked a third time. `Project` stays ordinary English with no glossary row, revisited when the tracker effort starts, since this effort is scoped to the document home and does not lean on the word | superseded by 219 |
| 216 | 2026-08-23 | The 205 spike runs against a scratch repository outside the knowledge base, operator-observed, before the spec is written | accepted |
| 215 | 2026-08-23 | Every frontmatter property Capstan writes carries a `capstan_` prefix. Obsidian types a property vault-wide by name, and collision-proof beats pretty when strangers point this at existing vaults | accepted |
| 214 | 2026-08-23 | The configured root lives in `CLAUDE.md` or `AGENTS.md`, beside the knowledge-base setting. It sits outside `.capstan/`, so the circularity of configuring a relocation from inside the thing being relocated disappears | accepted |
| 213 | 2026-08-23 | One configured root, resolved everywhere, defaulting to `<repo>/.capstan`. Never two copies: files in both places is a sync problem, and a sync problem needs the operating layer 86 forbids | accepted |
| 212 | 2026-08-23 | The durability risk in 211 is surfaced to the operator rather than buried in a design note. An authoritative tracker can sit uncommitted for days and they have to be told | accepted |
| 211 | 2026-08-23 | Capstan writes files into the vault and never runs git there. The operator commits. Supersedes 145, settling it in the vault's favour | accepted |
| 210 | 2026-08-23 | A missing or unreachable tracker degrades loudly and stops the phase. The one deliberate exception to stopping only for consequence: a missing source of truth is not ambiguity | accepted |
| 209 | 2026-08-23 | Slice items are closed and kept rather than deleted. A tracker that erases completed work cannot answer what shipped. The cost is accumulation, which has to stay legible | accepted |
| 208 | 2026-08-23 | The glossary, the log and the records can relocate. The effort scratch stays in the repository, since a vault full of dead specs is staleness (b) | accepted |
| 207 | 2026-08-23 | Document home and tracker are two features, named separately. Conflating them forces someone who wants their docs rendered into work tracking they did not ask for | accepted |
| 206 | 2026-08-23 | Whether Capstan namespaces its frontmatter property names. Obsidian types a property vault-wide by name, so a bare `status` or `type` collides with whatever the operator's vault already assigned | superseded by 215 |
| 205 | 2026-08-23 | Whether Obsidian reliably reflects notes written by an external process while the vault is open. No primary source found, and a community plugin exists because it does not. An authoritative tracker Capstan writes to and the operator reads is exactly this path | superseded by 221 |
| 204 | 2026-08-23 | Staleness (c) is answerable at pull time. Bases is first-party and documents `now()` with date arithmetic, so a note can render what has not moved in N days. The scout could not resolve the refresh trigger, and it does not bear on this: either way the query runs inside Obsidian and Capstan ships no process | accepted |
| 203 | 2026-08-23 | Obsidian is built concretely. Linear, Plane and GitHub come later and design the seam then. One adapter is a hypothetical seam | accepted |
| 202 | 2026-08-23 | A new project is scaffolded from a template rather than from a layout Capstan hardcodes | accepted |
| 201 | 2026-08-23 | All three staleness failures are in scope: a tracker item that outlives the work, a note describing a superseded decision, and work that stops between gates unnoticed | accepted |
| 200 | 2026-08-23 | When a tracker is configured it is authoritative, and it holds slice and seam state through to completion. `.capstan/` is not the source of truth in that configuration | superseded by 318 |
| 199 | 2026-08-23 | The motivation is relocating documentation and configuration out of the repository into a markdown renderer, for operators who do not want it in the repo. Not an extension of the delivery note | accepted |
| 198 | 2026-08-23 | Every part of this is optional. Capstan's core is unchanged and the default stays `.capstan/` in the repository, since the core is running on three live projects | accepted |
| 197 | 2026-08-21 | No second version bump. This folds into the unreleased 2.12.0, per 7 | accepted |
| 196 | 2026-08-21 | Counts that only say how many items are in the list beneath them are deleted rather than corrected, and the rule goes into `AUDIT.md`. A count that constrains or sets a bar stays, since it does work no list does. Four drifts in one day, the last within minutes of naming the pattern | accepted |
| 195 | 2026-08-21 | The README stops counting single-file skills. Adding `verify` broke that number within minutes of it being written, and the count carried nothing the heading and the tree below it do not | accepted |
| 194 | 2026-08-21 | Plugin version goes to 2.12.0 for the fifteenth discipline | accepted |
| 193 | 2026-08-21 | `verify` reports and never fixes, like a review. Red becomes a Builder task carrying `diagnosing-bugs`, since a red check is most of a feedback loop already | accepted |
| 192 | 2026-08-21 | The baseline for a failing check is `head` from `CLAIM.md`, re-run in a throwaway worktree only when something is red. A check already failing before the effort is not the effort's finding | accepted |
| 191 | 2026-08-21 | `verify` goes to the Architect rather than a sixth seat. It already owns integration, and the crew has five seats by design | accepted |
| 190 | 2026-08-21 | `verify` is added as the fifteenth discipline. Builders test in isolated worktrees, `two-axis-review` skips what a typechecker would catch on the grounds that CI catches it, and nothing was CI | accepted |
| 189 | 2026-08-21 | Plugin version goes to 2.11.0. Removing a check that stopped a run is a behaviour change, not a patch | accepted |
| 188 | 2026-08-21 | The pre-2.1.0 detection is removed from `effort`. Supersedes 40, 48, 50, 58 and settles 186. It guards a layout only this repository ever had, and it cost context on every effort start for a case that can no longer occur | accepted |
| 187 | 2026-08-21 | Plugin version goes to 2.10.0. The README is rewritten rather than corrected, so it is a minor rather than a patch | accepted |
| 186 | 2026-08-21 | The pre-2.1.0 migration section is dropped from the README for the same reason. The detection in `effort` still runs and is now unexplained, which is a separate call | superseded by 188 |
| 185 | 2026-08-21 | Every `claude-tools` reference leaves the README. The operator was its only user, so the rename section documented a migration nobody will run. Decision 0001 keeps the history | accepted |
| 184 | 2026-08-21 | The README gains `Your first run`: what happens at each phase and what the operator does at each gate. It documented install and upgrade and never once said how a run goes | accepted |
| 183 | 2026-08-21 | Every skill name in the disciplines table links to its `SKILL.md` by relative path, which resolves on GitHub and in a clone | accepted |
| 182 | 2026-08-21 | Plugin version goes to 2.9.3 | accepted |
| 181 | 2026-08-21 | `Unformed` joins the glossary beside `Open` and `Assumed`. The status shipped in 2.9.0 and the glossary that defines the other two never got it | accepted |
| 180 | 2026-08-21 | The README's install material merges into one section, with by-hand, namespacing and multi-file skills as subsections under it. `Install` and `Install detail` had the disciplines table wedged between them | accepted |
| 179 | 2026-08-21 | Four README counts were wrong and all four had drifted the same way: a set grew and its count did not. Three skills were eight, most skills were seven of fifteen, two borrowed ideas were four, and `unslop` was missing from the tree | accepted |
| 178 | 2026-08-21 | Plugin version goes to 2.9.2. README ships inside the plugin, so a documentation change is still a release | accepted |
| 177 | 2026-08-21 | The `Upgrading` section documents remove-and-re-add, leading with the uninstall it causes. Run alone, the remove leaves the operator with no Capstan | accepted |
| 176 | 2026-08-21 | Re-pointing the marketplace source is documented as optional. The redirect works, and the argument for doing it is that the old name is not ours to hold | accepted |
| 175 | 2026-08-21 | Plugin version goes to 2.9.1, the first patch release. An editing pass offers nothing new | accepted |
| 174 | 2026-08-21 | Three completion criteria widened to cover `unformed`. The grafts added a status and left every bound in the repository still naming two | accepted |
| 173 | 2026-08-21 | `Standards you enforce` names the owning discipline instead of restating its definition. The Architect needs to know a standard is enforced, not how to run it | accepted |
| 172 | 2026-08-21 | The `writing-for-agents` audit was run against `interview`, `decision-record`, `brief` and `effort`. Net five lines shorter, and the flab was almost all recently added | accepted |
| 171 | 2026-08-21 | Plugin version goes to 2.9.0. Two grafts, no new discipline, count stays at fourteen | accepted |
| 170 | 2026-08-21 | `interview` gains two moves from `domain-modeling`: challenge a term against the glossary, and invent an edge-case scenario when a relationship stays vague. Its file layout is left behind, since `CONTEXT-MAP.md` and `docs/adr/` fight the 2.1.0 move | accepted |
| 169 | 2026-08-21 | An `unformed` line is rewritten in place when it graduates to `open`. It is the one exception to superseding, because nothing was decided and there is no history to protect | accepted |
| 168 | 2026-08-21 | The log gains an `unformed` status for an area nobody can phrase a question about yet. Written as `open` it stalls the interview twice; written nowhere it surfaces in phase 3. From `wayfinder`, without the tracker | accepted |
| 167 | 2026-08-21 | Plugin version goes to 2.8.0. No new discipline, so the count stays at fourteen | accepted |
| 166 | 2026-08-21 | Where `next` and the repository disagree, the repository wins. The line was written before the gap and cannot know what happened during it | accepted |
| 165 | 2026-08-21 | `handoff` is not vendored and nothing is grafted into `brief`. A brief is for the person at the gate, and an agent-facing resumption section is the two-audience failure the design doc warns about. `brief` already says to link rather than paste, which is the other half of upstream | accepted |
| 164 | 2026-08-21 | `CLAIM.md` gains a `next` line: what the run after this one picks up. `phase` says where the effort got to, `next` says what is outstanding inside it, which git cannot reconstruct in phase 3 | accepted |
| 163 | 2026-08-21 | Plugin version goes to 2.7.0 for the fourteenth discipline | accepted |
| 162 | 2026-08-21 | Upstream's `never --abort` gains one exception: a conflict resolvable only by inventing behaviour is a slicing defect, and forcing the merge destroys a Builder's work to hide a planning error. Aborting costs the merge and nothing else, since every Builder's work is on its own branch | accepted |
| 161 | 2026-08-21 | `resolving-merge-conflicts` is vendored as the fourteenth discipline, invoked from step 6 of phase 3. A hunk's intent comes from `plan.md`, not from Builder commit messages written for their own branches | accepted |
| 160 | 2026-08-21 | Plugin version goes to 2.6.0 for the thirteenth discipline | accepted |
| 159 | 2026-08-21 | No full record for this one. Deleting the folder reverses it, nothing about it is surprising, and the only trade-off is an overlap now written down. Two of the three gates fail | accepted |
| 158 | 2026-08-21 | `brief` states the split: briefs carry settled decisions out, questionnaires carry open questions out. A brief that asks the reader something has a questionnaire inside it | accepted |
| 157 | 2026-08-21 | `to-questionnaire` is vendored onto the Architect as the thirteenth discipline, reached from the interview's third route out. Its answers resolve the `open` lines that prompted it | accepted |
| 156 | 2026-08-21 | No second version bump. This folds into the unreleased 2.5.0 rather than shipping 2.5.1, per 7 | accepted |
| 155 | 2026-08-21 | A repository declines an imported standard with a line in its own `.capstan/decisions.md`, and the Reviewer reads that file before grading Standards. Silence means the standard applies | accepted |
| 154 | 2026-08-21 | Plugin version goes to 2.5.0 for the twelfth discipline | accepted |
| 153 | 2026-08-21 | Upstream's TypeScript examples stay as written. The principles behind them are language-agnostic, and rewriting per language turns every refresh into a merge | accepted |
| 152 | 2026-08-21 | `DESIGN-IT-TWICE.md` is vendored too. Its parallel sub-agents are the Architect's existing fan-out, not a new seat | accepted |
| 151 | 2026-08-21 | `codebase-design` is vendored onto the Reviewer as the twelfth discipline, and Capstan takes a position on code structure for the first time. See [0003](decisions/0003-adopt-deep-modules-as-the-design-standard.md) | superseded by 810 |
| 150 | 2026-08-21 | Plugin version goes to 2.4.0 for the eleventh discipline. The bump ships inside this change, per 7 | accepted |
| 149 | 2026-08-21 | The Builder gets a `Bug slices` section naming the handover: `diagnosing-bugs` owns a bug slice up to the fix, `test-first` from the fix. Two preloaded skills with no stated order is a coin toss | accepted |
| 148 | 2026-08-21 | Upstream's `scripts/hitl-loop.template.sh` is not vendored. Both references repoint at `walkthrough`, whose wizard library already covers the job and whose `ask_secret` keeps a credential out of the transcript | accepted |
| 147 | 2026-08-21 | `diagnosing-bugs` is vendored from mattpocock/skills onto the Builder as the eleventh discipline. Three lines repointed, everything else upstream | accepted |
| 146 | 2026-08-21 | A routing reference maps an organisation to a tag and appends sub-ventures, which contradicts 119 and would produce a doubled tag. That file states it wins over an agent prompt. The exception is documented nowhere | superseded by 315 |
| 145 | 2026-08-21 | The vault's `AGENTS.md` rule 7 forbids automatic commits; decision 117 makes committing the Courier's job and phase 4 depends on that commit existing. The Courier is sent to `AGENTS.md` for the schema, so it reads rule 7 on the way past, and nothing says which wins | superseded by 211 |
| 144 | 2026-08-21 | Pre-existing notes carrying the wrong status are the operator's call, not this effort's. They predate it, and mass-editing committed knowledge-base notes is not a delivery step | superseded by 314 |
| 143 | 2026-08-21 | Review caught the frontmatter defect only by running the knowledge base's own validator. Reading frontmatter and finding it plausible is what failed five times; the check that worked was executing the repository's tooling | accepted |
| 142 | 2026-08-21 | A note's status must match whatever the knowledge base makes its lifecycle authority, and the Courier wrote one that folder's schema forbids. Corrected by a second commit, per 127 | accepted |
| 141 | 2026-08-21 | Supersedes 138. The close-out's derived-versus-copied statement goes to the Architect and the operator, never into the Reviewer's dispatch. It is the Courier's reasoning, and a Reviewer holding the writer's reasoning is the bias the separation exists to prevent. What the Reviewer gets is the standard, `agents/courier.md`, not the account of how it was applied | accepted |
| 140 | 2026-08-21 | An acceptance criterion counting `grep -c` results is counting lines, not occurrences. Four criteria in this effort were defective, all mine; the Builder that refused to pad prose to satisfy a wrong number did the right thing | accepted |
| 139 | 2026-08-21 | `two-axis-review` says to grade against a writing skill without applying it. `unslop`'s stated job is to rewrite text, and a Reviewer that loads it while forbidden to fix anything is being handed a contradiction the file resolves only implicitly | accepted |
| 138 | 2026-08-21 | The Courier's close-out states which frontmatter values it derived and which it copied. The tested Courier volunteered exactly this; a behaviour the review depends on is required rather than volunteered | superseded by 141 |
| 137 | 2026-08-21 | Phase 4 hands the Reviewer `agents/courier.md` as the note's standard, alongside the spec and the decision log. A standard is not the Builder's reasoning, so independence holds | accepted |
| 136 | 2026-08-21 | Found by running it, with every static check green: the review would have re-introduced the tag bug. The Reviewer graded the derived tag as a blocking defect and would have sent the Courier back to copy the organisation's, because the copied-versus-derived rule lives only in `agents/courier.md`, which no Reviewer reads | accepted |
| 135 | 2026-08-21 | A change to Capstan cannot be verified by dispatching Capstan's own agents. `capstan:courier` and the rest resolve from the installed plugin cache, not the working copy, so a live test in the same session runs the previous release. Test the changed prose by handing it to a generic agent instead | accepted |
| 134 | 2026-08-21 | "Knowledge base" gets no glossary row. It is ordinary English naming a setup-specific location, on the same reasoning as "project" | superseded by 635 |
| 133 | 2026-08-21 | Phase 4's delete step carries the rationale that binds how exhaustive the delete must be, not the history of which role used to own it. A stale spec is read as current by the next agent; that changes behaviour, and where the step moved from does not | accepted |
| 132 | 2026-08-21 | Naming a writing standard is not enough: the discipline tells the Reviewer to invoke the skill and grade against its body. `writing-for-agents` describes itself as for creating or editing, so nothing in a review fires that pointer and the agent-side standard would silently go ungraded | accepted |
| 131 | 2026-08-21 | The Courier reports the commit SHA and the paths that commit touched, observed with `git log --name-only -1`. "Report the commit" is not observable, and a fixed point that cannot be resolved is not one | accepted |
| 130 | 2026-08-21 | Phase 4 gains a fifth act: report to the operator what review found. Spec behaviour 6 landed in no slice because the plan listed four acts against the spec's six. A plan omission, mine | accepted |
| 129 | 2026-08-21 | `two-axis-review` names two forms of fixed point plus a case where there is none, rather than three forms one of which is an absence. A defined term does not widen to admit its own absence | accepted |
| 128 | 2026-08-21 | `Fixed point` keeps its existing definition and stops being stretched. The Courier reports a commit; the fixed point is that commit's parent and the term stays with the review. Acceptance criterion 4 tested for a term my own spec misused, so it is replaced by one testing the positive staging form | accepted |
| 127 | 2026-08-21 | A blocking finding on a note already committed is corrected by a second commit. Never `--amend` in a repository the operator owns and may already have pushed | accepted |
| 126 | 2026-08-21 | `two-axis-review` names the writing standard instead of the Reviewer's frontmatter granting it: `unslop` for prose a person reads, `writing-for-agents` for prose an agent consumes. The Reviewer holds the Skill tool and invokes whichever applies, so code review is untouched | accepted |
| 125 | 2026-08-21 | Version goes to 2.3.0. A new step in phase 4 and a new tool grant are behaviour rather than a typo | accepted |
| 124 | 2026-08-21 | A knowledge base that is not a git repository degrades rather than skips. The Reviewer reads the note by absolute path and reports that no fixed point was available. Skipping would ship a feature that only works where the knowledge base happens to be versioned | accepted |
| 123 | 2026-08-21 | The Standards axis grades the note's schema and its prose, conditional on the Courier being granted `unslop` in the same change. Grading prose against a rule the writer never received is acceptance criteria testing for something the spec never supplied | accepted |
| 122 | 2026-08-21 | `two-axis-review` is extended rather than joined by an eleventh discipline. Its fixed-point section names one repository and one command form, so it changes either way, and the axes map onto a note without strain | accepted |
| 121 | 2026-08-21 | Teardown moves from the Courier to the Architect in phase 4. The Courier holds no Task tool so it cannot dispatch its own Reviewer, and its step 4 deleted the spec the Spec axis grades against. The Courier becomes three steps | accepted |
| 120 | 2026-08-21 | Rows 21 and 20 move to their sorted position. They sat above 114 at the top of the file, so the newest-first reading order broke at the first two lines a reader sees | accepted |
| 119 | 2026-08-21 | Supersedes 113. An effort tag names the project, not the individual effort. Confirmed on a second folder, where every note across its project subfolders carries the parent tag | superseded by 315 |
| 118 | 2026-08-21 | The note review stays in phase 4 and adds no fourth gate. The Architect actions the findings and reports them in the close-out; a blocking finding goes back as a fix task on the same note | accepted |
| 117 | 2026-08-21 | The Courier commits the note to the knowledge base as one commit touching one path, and reports the commit as the fixed point. A knowledge base with unrelated work in its tree is the normal case, so the commit is never staged with `git add -A` | accepted |
| 116 | 2026-08-21 | A Reviewer reviews the note, not the Architect. The Architect wrote the spec the note summarises, so it holds the Courier's context by construction, which is the bias the separation exists to block | accepted |
| 115 | 2026-08-21 | Only the knowledge-base note is reviewed. The package reports verification already performed, the operator reads every brief before sending, and teardown removes what is not allowed to survive. The note is the only Courier output that is permanent and read by nobody when it is written | accepted |
| 114 | 2026-08-21 | "Project" in the Courier is ordinary English rather than Capstan vocabulary and gets no glossary row, on the same reasoning as "operating layer" | superseded by 219 |
| 113 | 2026-08-21 | An effort tag names the project, not the individual effort. Counted rather than assumed, and my first diagnosis was wrong: the Courier had filed a project under its parent organisation | superseded by 119 |
| 112 | 2026-08-21 | The Courier distinguishes copied frontmatter fields from derived ones, keyed on whether the value is a property of the folder or a fact about this effort. Not on counting neighbours: a folder is uniform until the moment the rule has to fire | accepted |
| 111 | 2026-08-21 | "Two skills are more than one file" becomes three. `walkthrough/SKILL.md` points at two siblings, so lifting it alone leaves the dangling pointers that section exists to warn about | accepted |
| 110 | 2026-08-21 | "Operating layer" lives in `DESIGN.md` only, where the paragraph around it defines it. No glossary row: a term used once, in the place it is explained, is not vocabulary | accepted |
| 109 | 2026-08-21 | The first screen keeps a concrete positive claim. Forty-five words of negation would fit any repository; "plain markdown" fits this one and a stranger can check it in two seconds | accepted |
| 108 | 2026-08-21 | The licence section counts three vendored skills. The slice made "Nothing else here is vendored" false while line 7 of the same file called `walkthrough` vendored, a contradiction inside one commit | accepted |
| 107 | 2026-08-21 | Two of my own acceptance criteria were wrong again. Criterion 3 demanded `CREDIT.md` state nothing changed when it correctly records one; criterion 7 was looser than decision 88 and graded a lost requirement green | accepted |
| 106 | 2026-08-21 | A spike flips its line to `accepted`, not `assumed`. An `assumed` line is re-raised in the next interview's first round, which is the silent return this discipline exists to stop | accepted |
| 105 | 2026-08-21 | A spike names its branch in the decision line it settled. The rewrite fixed which line the answer goes into and dropped what that line has to carry | accepted |
| 104 | 2026-08-21 | A walkthrough's confirm prompt prints the target the script actually resolves rather than a hand-typed literal. Asserting at authoring time what resolves at run time is a false assurance, which is worse than saying nothing | accepted |
| 103 | 2026-08-21 | A walkthrough's confirm is a construction, not a call. `if confirm ...; then ... fi`. Reproduced: a bare call under `set -e` exits the run on a decline, before the summary, leaving written values unrecorded | accepted |
| 102 | 2026-08-21 | `AUDIT.md` is Capstan-authored, not vendored, and my claim otherwise was wrong. The em dash is not graded. If zero em dashes is the rule for agent-facing prose it gets written into `writing-for-agents` rather than enforced case by case | superseded by 408 |
| 101 | 2026-08-21 | `.capstan/CONTEXT.md` gains a `Stage` row. It is the unit a walkthrough is built from, appears twelve times, and carries its own rules. `Route` is not added: with 100 in force the collision disappears | accepted |
| 100 | 2026-08-21 | "Route" is reserved for `interview`'s three stall exits. `spike` says path. My own spec used "three routes for a UI" against my own criterion 9 and is corrected | accepted |
| 99 | 2026-08-21 | No committed `scripts/` path for a walkthrough. One that persists is an operating layer, which 86 says Capstan does not have. The line was inherited from upstream rather than derived from Capstan's position | accepted |
| 98 | 2026-08-21 | Spike branches are `spike/<slug>`, outside the effort namespace so the Architect's merge loop cannot pick one up, and pushed and never deleted so the reference survives a fresh clone | accepted |
| 97 | 2026-08-21 | A spike writes its answer into the `open` line the stall route already created, flipping its status. Appending a second line leaves the first open forever and the next interview re-asks a question already answered | accepted |
| 96 | 2026-08-21 | The agent never runs a walkthrough it authored. With stdin not a tty every prompt returns empty, an empty secret overwrites a real one, and the script still prints success and exits zero | accepted |
| 95 | 2026-08-21 | A walkthrough stage that writes outside the local `.env`, or that cannot be undone, is gated by `confirm` and names its target first. The Authority table reserves third-party writes for the operator, every time | accepted |
| 94 | 2026-08-21 | Shipped skills never cite decision numbers. `skills/` is the plugin payload and `.capstan/decisions.md` lives in the user's repository, so a number points at whatever line they happen to have | accepted |
| 93 | 2026-08-21 | `spike` is named inside `interview`'s existing stall route rather than replacing it. The route already asks who builds the throwaway; it gains a name and a discipline | assumed |
| 92 | 2026-08-21 | Both new skills are model-invoked, like every other discipline. Only `effort` is a front door | assumed |
| 91 | 2026-08-21 | `effort`'s human-task-step section becomes a pointer to `walkthrough`. Four paragraphs for a branch most runs never take | accepted |
| 90 | 2026-08-21 | Version 2.2.0. Two disciplines added, nothing moved, nothing broken | accepted |
| 89 | 2026-08-21 | `spike` keeps two branches, recast by the question rather than the technology: does it behave right, does it feel right. Capstan runs efforts on documents and infrastructure, not only code | accepted |
| 88 | 2026-08-21 | Throwaway code lives on a branch that is never merged, named in the decision line it settled. The answer persists in the log; the code persists only as a reference | accepted |
| 87 | 2026-08-21 | No sixth role. A Builder carries a spike with a different definition of done. A role is a permanent widening of the crew for something that runs occasionally | accepted |
| 86 | 2026-08-21 | Supersedes 83. "No scripts" is qualified rather than dropped. The rule Capstan actually holds is no operating layer: nothing that must be maintained to keep the workflow alive | accepted |
| 85 | 2026-08-21 | `template.sh` is vendored with `CREDIT.md` and `LICENSE`, per the `writing-for-agents` precedent. See [0002](decisions/0002-vendor-the-walkthrough-library.md) | accepted |
| 84 | 2026-08-21 | The two disciplines are `walkthrough` and `spike`. Nautical names were considered and rejected: `sounding` is apt and nobody would know what it meant | accepted |
| 83 | 2026-08-21 | Whether "Plain markdown. No scripts, no schemas, no hooks, no scheduler" reads as a reason to choose Capstan or as an implementation note | superseded by 86 |
| 82 | 2026-08-21 | The Architect's does-not-move-delete-or-commit guarantee is restored explicitly. It thinned one word per round across three rounds without anyone deciding to drop it, and the commit half had no carrier left | accepted |
| 81 | 2026-08-21 | The first screen says the disciplines run through to delivery, not through to the review. Review stopped one phase short of the headline two lines above it, and of the Courier named in the same sentence | accepted |
| 80 | 2026-08-21 | `DESIGN.md` is verbatim with two recorded exceptions: the disciplines lead line, which carries both the count fix per 75 and the preload correction, and one blank line at the seam where two sections became neighbours for the first time | accepted |
| 79 | 2026-08-21 | The no-scripts line sits below the counts rather than above them. It lands harder once the reader knows what there is to have no scripts for | accepted |
| 78 | 2026-08-21 | The spec and plan are corrected rather than the deliverable. Six spec lines and two criteria went stale as decisions 73 to 77 landed mid-build, and criterion 4 was grading correct work as wrong | accepted |
| 77 | 2026-08-21 | The unverified-sequence hedge returns, scoped to the 2.1.0 file move, which has not been run. The 2.0.0 uninstall and reinstall was verified live at that effort's delivery, so the old blanket hedge was stale | accepted |
| 76 | 2026-08-21 | "Plain markdown. No scripts, no schemas, no hooks, no scheduler" returns to the first screen. It is the only sentence giving a stranger a reason to choose this, and decision 67's "four things" governed structure rather than withholding the reason | accepted |
| 75 | 2026-08-21 | `effort` is not a discipline. Both tables read eight disciplines plus the front door, and `CONTEXT.md` gains a row for the operator-invoked front door that is neither a Discipline nor an Effort | accepted |
| 74 | 2026-08-21 | The headline names the platform. Decision 65's own wording carried "Claude" and my sentence dropped it, so the platform first appeared inside a shell command | accepted |
| 73 | 2026-08-21 | "Five roles", not "five agents". `agents/` holds four files and the Architect is the operator's session. A spec defect of mine, implemented faithfully and tested for by criterion 4 | accepted |
| 72 | 2026-08-21 | No version bump. Documentation only, and the README people read is the one on GitHub rather than the copy in their plugin cache | accepted |
| 71 | 2026-08-21 | No competitor is named. Position by what Capstan does; naming one invites a comparison we do not control and ages the moment either project moves | accepted |
| 70 | 2026-08-21 | `Known limits` and `Configure` stay in the README, below the fold. Limits are a buying signal rather than a caveat, and configuration belongs beside the install | accepted |
| 69 | 2026-08-21 | Decisions 8, 13, 19, 26 and 64 were put to the operator twice and went unanswered twice. Defaulted rather than asked a third time, per the interview's stall route | accepted |
| 68 | 2026-08-21 | The `Upgrading` section stays, compressed and below the fold. Two efforts made it correct and deleting it strands everyone on 1.x and 2.0 | accepted |
| 67 | 2026-08-21 | Install lands in the first screen. What it is, what you get, how to install, then depth | accepted |
| 66 | 2026-08-21 | The design material moves to `DESIGN.md`, linked once. Too good to delete, too long for a page whose job is getting someone to install | accepted |
| 65 | 2026-08-21 | The README is written for someone choosing a full Claude skills and agent environment for taking work from concept to delivery. Deciding, not operating | accepted |
| 64 | 2026-08-21 | The three italic bullet leads become bold, matching the repository's 39 others. The revisit condition on this line fired: this is the README effort | accepted |
| 63 | 2026-08-21 | `.effort/` is deleted from this working copy at delivery. Nothing else deletes it, and left in place it fires the trigger built in this effort and stops the next one | accepted |
| 62 | 2026-08-21 | `.effort/` is never moved. The operator deletes it. Moving it would put a stale `CLAIM.md` where the next run reads a live one | accepted |
| 61 | 2026-08-21 | The step reads the recorded `assumed` line before asking. A line written to stop a question recurring has to be read by the thing that asks it | accepted |
| 60 | 2026-08-21 | Any path confirmed as Capstan's stops the run. Mixed answers resolve to stop rather than to nothing | accepted |
| 59 | 2026-08-21 | The claim check runs before the provenance question. A claim is a fact about the repository, not an opinion the operator supplies, and asking first lets a "not ours" answer step past live work | accepted |
| 58 | 2026-08-21 | The Architect detects a pre-2.1.0 layout and **stops**. The operator moves the files. Supersedes 36, 43, 46, 49, 51, 54 and 57, and deletes `MIGRATION.md`. Seven review rounds on a procedure the crew was going to run against a user's files is the signal the README already names: simplify rather than keep it alive | superseded by 188 |
| 57 | 2026-08-21 | Not-Capstan's and Capstan's-but-declined are different answers. The first proceeds normally, the second stops the run per 47. Folding the two asks into one confirmation had merged them | superseded by 58 |
| 56 | 2026-08-21 | The `Claim` glossary row is not widened for the pre-2.1.0 path. Settled: `CONTEXT.md` describes the current convention, and `MIGRATION.md` is deletable once served. Raised five times; recorded so it stops recurring | accepted |
| 55 | 2026-08-21 | A trigger firing on a repository that is not Capstan's is recorded as an `assumed` line, so it does not ask again on every later effort | accepted |
| 54 | 2026-08-21 | Deleting `.effort/` is gated on the same per-path confirmation as the move and named in what the operator approves. The Authority table puts deletes with the operator every time | superseded by 58 |
| 53 | 2026-08-21 | Nothing confirmed means the migration returns control and the run proceeds normally. The trigger is a candidate signal, not proof | accepted |
| 52 | 2026-08-21 | Scope widened: the README's upgrade note is corrected. Its premise that the reader has a root `.effort/` is false for any repository that has delivered an effort | accepted |
| 51 | 2026-08-21 | `.effort/` is deleted before the migration commit, not after, so it is never both unignored and untracked while something is staged | superseded by 58 |
| 50 | 2026-08-21 | Taking over a pre-2.1.0 claim is not supported. Finish or abandon that effort under 2.0.0, or delete `.effort/` and start fresh | superseded by 188 |
| 49 | 2026-08-21 | Provenance is confirmed with the operator before any path moves. This is what makes broad detection safe, and it closes a guard that covered the trigger but never the move | superseded by 58 |
| 48 | 2026-08-21 | Supersedes 33. Detection triggers on any root artifact, not on `.effort/`. That directory was never tracked and the Courier deletes it at delivery, so a delivered or freshly cloned 2.0.0 repository has none and the migration would never have fired | superseded by 188 |
| 47 | 2026-08-21 | Supersedes 45. Declining the migration ends the run. There is no decline mode, because honouring one means every phase file and skill consults a flag, which is the dual-read fallback decision 32 refused | accepted |
| 46 | 2026-08-21 | The operator deletes `.effort/` as the last step of migration. Nothing else does, so leaving it makes the trigger fire on every later effort | superseded by 58 |
| 45 | 2026-08-21 | A declined migration is recorded in `CLAIM.md`, which survives the run boundary, and governs reads and writes for all three paths. "For the rest of this run" expires at a gate; an effort spans four | superseded by 47 |
| 44 | 2026-08-21 | The migration pointer sits between the claim check and the claim write. One reorder closes three findings at once | accepted |
| 43 | 2026-08-21 | The migration lives in `skills/effort/MIGRATION.md` behind a pointer, not inline in `Before you start`. It is a branch most runs never take, and when it has served its purpose it can be deleted whole rather than unpicked | superseded by 58 |
| 42 | 2026-08-21 | Drop "live claim" as a term. A claim exists or it does not, matching the wording already in the file | accepted |
| 41 | 2026-08-21 | The claim check has one home. The existing step absorbs the old-path case rather than a second rule sitting above it | accepted |
| 40 | 2026-08-21 | The migration confirms each of the three paths rather than assuming all three are Capstan's. A repository can hold `.effort/` and an unrelated `decisions.md` | superseded by 188 |
| 39 | 2026-08-21 | `CONTEXT.md` gains a `.capstan/` row. The word was doing two jobs, the folder and the `capstan:` namespace prefix, which the interview skill says to challenge | accepted |
| 38 | 2026-08-21 | The `.gitignore` rewrite for an upgrading repository belongs to slice 2's detection step. A gap in the plan, found in review, that would have shipped as unignored scratch | accepted |
| 37 | 2026-08-21 | No marker file inside `.capstan/`. `CONTEXT.md` sits there and introduces the vocabulary better than a README nobody updates | accepted |
| 36 | 2026-08-21 | The migration lands as its own commit at the moment of the move, never folded into a slice commit | superseded by 58 |
| 35 | 2026-08-21 | Version 2.1.0. Nothing a user types changes; only the on-disk layout of their artifacts, and the Architect moves it | accepted |
| 34 | 2026-08-21 | Detection checks for a live `CLAIM.md` before moving anything, and reports rather than relocating a running effort | accepted |
| 33 | 2026-08-21 | The Architect detects a pre-migration layout and offers to move it. Prose in a skill, not a script, and not release notes nobody reads | superseded by 48 |
| 32 | 2026-08-21 | Hard cut. The skills read the new paths only. No dual-read fallback, because it would never be removed | accepted |
| 31 | 2026-08-21 | `.effort/` becomes `.capstan/effort/`. The word stays, since `Effort` is a defined term in `CONTEXT.md` | accepted |
| 30 | 2026-08-21 | The README rewrite is a separate effort, sequenced after the path migration. Mixing them hides which change broke what | accepted |
| 29 | 2026-08-21 | Capstan's own repo follows the same convention. Its root `CONTEXT.md`, `decisions.md` and `decisions/` move too | accepted |
| 28 | 2026-08-21 | `.capstan/` is committed. Only `.capstan/effort/` is gitignored. A blanket ignore would stop decisions entering git and contradict the thesis | accepted |
| 27 | 2026-08-21 | Every artifact Capstan writes lives under `.capstan/` in the working copy. The repo root stays the user's, and `CONTEXT.md` stops colliding with the same filename in other plugins | accepted |
| 26 | 2026-08-21 | `Upgrading` documents the orphan-on-refresh in one clause. Defaulted after two unanswered rounds; revisit if the section should stay shorter | assumed |
| 25 | 2026-08-21 | `plugin.json` holds the canonical description. `marketplace.json` and the README copy it, and the next editor changes all three | accepted |
| 24 | 2026-08-21 | The `Namespace` row in `CONTEXT.md` is forward-looking rather than a retrofit against decision 4, because the prefix itself resolved in this effort | accepted |
| 23 | 2026-08-21 | Scope widened again: `Known limits` says `/capstan:effort`, and the manual-install section gets its own start line | accepted |
| 22 | 2026-08-21 | Supersedes 21. The manifest description stays as agreed, but the README keeps a subtitle that names what Capstan is. A gallery card has a category and an install button for context; a README has neither | accepted |
| 21 | 2026-08-21 | Scope widened two lines: README line 98's `/effort` becomes `/capstan:effort`, and the line 3 tagline adopts the new description | superseded by 22 |
| 20 | 2026-08-21 | Both descriptions carry identical text. The longer gallery variant is dropped: "stops for your approval at three points along the way" contradicts `CONTEXT.md`, which defines a Gate as never a pause | accepted |
| 19 | 2026-08-21 | Dropped "and waits for your go" from the description. It duplicated the closing line. Revisit if the approved wording is wanted verbatim | assumed |
| 18 | 2026-08-21 | The description is plain language. No role names, no internal vocabulary | accepted |
| 17 | 2026-08-21 | `displayName` is "Capstan" with no descriptive tail | accepted |
| 16 | 2026-08-21 | Version goes to 2.0.0. The skill namespace is the public interface and it breaks | accepted |
| 15 | 2026-08-21 | The GitHub repo renames to `BytesNation/capstan`. The marketplace id stays `bytesnation` | superseded by 569 |
| 14 | 2026-08-21 | Rename to `capstan`. Both the skill namespace and the install identifier change. See [0001](decisions/0001-rename-to-capstan.md) | accepted |
| 13 | 2026-08-21 | The in-session equivalent of `claude plugin update <plugin>@<marketplace>`. Not verified; the CLI form is documented instead | open |
| 12 | 2026-08-21 | The `Upgrading` section documents the full two-step sequence. Refreshing the marketplace alone does not install a new version | accepted |
| 11 | 2026-08-21 | The `Upgrading` section documents both refresh forms, in-session and CLI, since `## Install` is written for the CLI | accepted |
| 10 | 2026-08-21 | No release tags while the core is still moving. Revisit when someone needs to pin a version, or when the marketplace entry stops tracking the default branch | accepted |
| 9 | 2026-08-21 | Scope widened by one line: the `claude plugin update` mention in `## Install` is reconciled with the new `Upgrading` section | accepted |
| 8 | 2026-08-21 | The README recommends leaving auto-update off rather than presenting both paths neutrally. Revisit if steering the reader is unwanted | assumed |
| 7 | 2026-08-21 | The version bump ships inside this effort, not as a separate release commit | accepted |
| 6 | 2026-08-21 | Both update paths are documented: marketplace auto-update, and the manual `/plugin marketplace update` | accepted |
| 5 | 2026-08-21 | The README `Upgrading` section is rewritten to cover the marketplace install, not only the manual copy | accepted |
| 4 | 2026-08-21 | `CONTEXT.md` is created forward only, from the next term that resolves. Never retrofitted from existing code | accepted |
| 3 | 2026-08-21 | The upgrade explanation lives in the README `Upgrading` section. No `CHANGELOG.md` | accepted |
| 2 | 2026-08-21 | Updating installs need no migration step. The new files appear on their own | accepted |
| 1 | 2026-08-21 | Plugin version goes to 1.1.0 for the glossary and log-status change | accepted |
